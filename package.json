{
  "name": "mcp-registry-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @mcp-registry/desktop dev",
    "dev:web": "pnpm --filter @mcp-registry/desktop dev:web",
    "build": "pnpm -r build",
    "package:win": "pnpm --filter @mcp-registry/desktop package:win",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
