# WORK_LOG.md

## 2026-07-03

- AI Program 프로젝트를 다른 컴퓨터에서 이어받을 수 있도록 GitHub 업로드 준비를 시작했다.
- 로컬 프로젝트 경로는 `C:\Users\Donggeon\Documents\AI Program`이다.
- 로컬 Git 저장소는 존재하지만 `origin` 원격은 아직 설정되어 있지 않았다.
- GitHub 후보 저장소 이름 `AI_PROGRAM`, `AI_Program`, `AI-Program`, `AIProgram`, `ai-program`은 현재 접근 가능한 원격으로 확인되지 않았다.
- `.gitignore`를 정리해 Codex 내부 상태, 캐시, `node_modules`, pnpm store, 빌드 산출물, 민감 파일을 제외했다.
- `README_HANDOFF.md`, `AGENTS.md`, `WORK_LOG.md`, `TODO.md`를 추가했다.
- `electron-desktop-app-example.html`, `mcp-registry-scope-options.html`은 작업 파일로 보이며 커밋 대상에 포함했다.

## 이어서 할 일

- GitHub에서 AI Program용 저장소 URL을 확인하거나 새로 만든다.
- 로컬 저장소에 `origin`을 연결한 뒤 현재 브랜치를 push한다.
- B 컴퓨터에서는 저장소를 clone 또는 pull한 뒤 `pnpm install`을 실행한다.

## 2026-07-03 추가

- GitHub에 `https://github.com/kdg200121-hash/AI_PROGRAM.git` 저장소를 생성했다.
- 로컬 저장소의 `origin` 원격으로 연결하고 push를 진행했다.

## 2026-07-03 추가 2

- `README.md`의 깨진 한글 문장을 UTF-8 기준 문서로 다시 정리했다.
- 현재 실행, 확인 명령, 프로젝트 구조, 다음 단계를 README에 반영했다.
- 앱 소스(`packages/desktop/src`)에도 깨진 한글 UI 문자열이 남아 있어 별도 TODO로 분리했다.
- 기본 PowerShell PATH에서는 `git`과 `node`를 찾지 못했지만, Codex 번들 런타임 경로를 PATH에 추가해 확인했다.
- `git status --short` 결과 변경 파일은 `README.md`, `TODO.md`, `WORK_LOG.md`였다.
- `pnpm test` 통과: 10개 테스트 파일, 34개 테스트.
- `pnpm typecheck` 통과: shared/core/desktop 패키지 모두 완료.

## 2026-07-03 추가 3

- 사용자의 피드백에 따라 상단 상태 요약 강화는 보류했다. 연결 관리는 한 번 설정하면 자주 보는 메인이 아니므로 작업 화면 우선 구조를 유지한다.
- 사이드바가 화면 최상단부터 시작하도록 레이아웃을 조정했다.
- 상단 열린 탭 영역은 사이드바 오른쪽에서 시작하도록 변경했다.
- 서버 화면의 열린 탭 표기를 `MCP Servers` 대신 현재 작업공간 이름(`CAD` 등)으로 표시하게 바꿨다.
- 720px 높이 화면에서 더 잘 들어오도록 상단바, metric, 표, 카드, 상세 패널의 여백과 최소 높이를 줄였다.

## 2026-07-03 추가 4

- 사용자의 정정에 따라 `MCP Servers`를 바꿔야 하는 위치는 상단 탭이 아니라 좌측 메뉴임을 확인했다.
- 좌측 첫 메뉴 라벨을 `MCP Servers`에서 `CAD`로 변경했다.
- 관련 `navigationModel` 테스트 기대값도 `CAD`로 업데이트했다.

## 2026-07-03 추가 5

- 접힌 사이드바(`sidebarCollapsed`)에서는 즐겨찾기 그룹이 보이지 않도록 숨김 처리했다.
- 상단 열린 탭의 서버 화면 표기는 제목 `메인페이지`, 보조 라벨 `CAD`로 정리했다.
- 좌측 메뉴의 `CAD` 표기와 상단 열린 탭의 페이지 제목/메뉴 보조 라벨을 분리했다.

## 2026-07-03 추가 6

- 접힌 사이드바에서 즐겨찾기 항목 자체는 유지하고, `즐겨찾기` 제목 텍스트만 숨기도록 수정했다.
- 좌측 상단 브랜드 설명(`CAD/Revit 연결 관리자`) 아래에 앱 버전 `v0.1.0`을 표시했다.

## 2026-07-03 추가 7

- 즐겨찾기 영역과 일반 메뉴 영역의 펼침 상태가 같은 section id를 공유해 동시에 펼쳐지는 문제를 수정했다.
- 즐겨찾기 전용 펼침 상태(`expandedFavoriteSectionIds`)를 추가해 즐겨찾기에서 펼친 항목은 즐겨찾기에서만 열리게 했다.
- 일반 메뉴는 기존 `expandedSectionIds`를 계속 사용해 두 영역의 펼침 동작을 분리했다.

## 2026-07-03 추가 8

- 간소화 모드에서 상단 탭의 `+` 버튼이 마지막 탭 바로 옆에 붙도록 탭 flex 레이아웃을 조정했다.
- 소메뉴를 데이터 기반 항목(`도구 목록`, `연결 설정`, `추가`)으로 분리했다.
- 소메뉴 우클릭 메뉴에서 즐겨찾기 추가/해제가 가능하도록 했다.
- 메뉴를 즐겨찾기한 경우에는 기존처럼 펼쳐서 소메뉴를 볼 수 있고, 소메뉴를 즐겨찾기한 경우에는 단일 즐겨찾기 항목으로 표시되게 했다.
- 소메뉴 앞에 4점 드래그 핸들을 추가하고, 같은 메뉴 안에서 소메뉴 순서를 바꿀 수 있게 했다.
- 최근 사용한 소메뉴 최대 5개를 프로세스 모니터 위쪽에 표시하도록 했다.

## 2026-07-03 추가 9

- 일반 메뉴와 즐겨찾기 메뉴가 같은 section id를 선택하더라도 동시에 active 표시되지 않도록 선택 출처를 분리했다.
- 즐겨찾기 영역에서도 메뉴를 펼쳐 소메뉴를 볼 수 있게 하고, 즐겨찾기 소메뉴에는 일반 소메뉴와 같은 4점 아이콘을 표시했다.
- 소메뉴의 `+` 항목을 항상 각 소메뉴 목록 맨 아래에 유지하도록 했다.
- 소메뉴 드래그 중에는 부모 메뉴 드롭 효과가 아니라 소메뉴 내부 드롭 라인만 보이도록 조정했다.
- 간소화 버튼과 MCP 연결 상태는 상단 탭 우측에 고정하고, 최근 사용 소메뉴 행 높이를 소메뉴 행과 맞췄다.

## 2026-07-03 추가 10

- 상단 열린 탭 사이에 브라우저 탭처럼 얇은 세로 구분선을 추가했다.
- 소메뉴 `+` 항목은 드래그 아이콘 없이 가운데 정렬되도록 정리했다.
- 소메뉴 클릭 시 부모 메뉴 active 효과가 켜지지 않도록 소메뉴 전용 선택 출처를 추가했다.
- 메뉴/소메뉴 선택 효과가 텍스트 버튼뿐 아니라 아이콘/화살표 영역까지 이어지도록 하고, 선택색을 더 연하게 조정했다.
- 소메뉴 드래그 시 실제 행 전체가 끌리는 느낌이 나도록 drag image와 dragging 스타일을 수정했다.
- 최근 사용 소메뉴에서는 4점 아이콘을 제거하고, 즐겨찾기 소메뉴는 일반 메뉴 높이와 맞췄다.
- 좌측 남색 메뉴 영역 오른쪽에 드래그 가능한 너비 조절 핸들을 추가하고 너비를 로컬 저장소에 보관하도록 했다.

## 2026-07-03 추가 11

- 소메뉴 드래그 중 부모 메뉴의 바깥 드롭 라인이 보이지 않도록 부모 메뉴 drag-over 처리를 차단했다.
- 소메뉴 우클릭 메뉴의 `열기`를 일반 메뉴와 같은 `새 탭에서 열기` 동작/문구로 변경했다.
- 상단 탭 구분선은 전체 border 대신 작은 일자 세로선으로 바꾸고, `+` 버튼 위치를 중앙 정렬로 되돌렸다.
- 메뉴와 소메뉴 우클릭 메뉴에 아이콘 선택 목록을 추가하고 선택값을 로컬 저장소에 보관하도록 했다.
- `Ctrl+S` 기본 브라우저 저장을 막고 현재 메뉴/아이콘/순서 설정을 로컬 저장소에 저장하는 단축키를 추가했다.
- 아이콘 변경 직전 상태를 기억해 입력창 밖에서 `Ctrl+Z`로 최근 아이콘 변경을 한 번 되돌릴 수 있게 했다.

## 2026-07-03 추가 12

- 소메뉴를 클릭하면 상단 탭 제목이 `메인페이지` 대신 선택한 소메뉴 이름으로 표시되게 했다.
- 사용자의 정정에 따라 메뉴/소메뉴 아이콘 수정 기능과 관련 우클릭 메뉴, 저장 상태, 되돌리기 스냅샷을 제거했다.
- Process Monitor 버튼의 우클릭 메뉴를 제거했다.
- 상단 탭 구분선과 `+` 버튼 위치를 살짝 아래로 조정했다.
- 인터넷 탭 기능은 만들지 않고, 상단 탭 왼쪽에 뒤로가기/앞으로가기 아이콘 버튼만 비활성 상태로 추가했다.

## 2026-07-03 추가 13

- 최근 사용 소메뉴 항목을 살짝 오른쪽으로 들여 보조 목록처럼 보이게 했다.
- 최근 사용 소메뉴 우클릭 메뉴에 `즐겨찾기 추가/해제`, `새 탭에서 열기`, `삭제` 기능을 추가했다.
- 소메뉴 우클릭 메뉴 순서를 `즐겨찾기`, `새 탭`, `삭제` 흐름으로 정리했다.

## 2026-07-03 추가 14

- 상단 뒤로가기/앞으로가기 아이콘을 탭 왼쪽에서 우측 유틸리티 영역으로 옮겼다.
- 즐겨찾기에 따로 추가된 소메뉴 항목의 높이와 테두리 기준을 일반 메뉴 행과 맞췄다.

## 2026-07-03 추가 15

- 뒤로가기/앞으로가기 아이콘을 본문 상단 오른쪽 영역으로 다시 옮겼다.
- 메뉴/소메뉴 이동 히스토리를 쌓아 뒤로가기/앞으로가기 버튼이 실제 이동 가능 상태에 따라 활성화되게 했다.
- 즐겨찾기 소메뉴 항목의 글자 크기와 텍스트 시작 위치를 일반 메뉴 행에 더 가깝게 맞췄다.

## 2026-07-03 추가 16

- 좌측 상단 브랜드 문구를 `CAD/Revit 연결 관리자`에서 `MCP 연결관리자`로 변경했다.
- 사이드바 접기 버튼의 테두리를 제거하고 브랜드 텍스트 높이에 맞춰 위치를 조정했다.

## 2026-07-03 추가 17

- 소메뉴 이동 드롭 라인이 소메뉴 아이콘 아래쪽까지 이어지도록 드롭 표시 폭을 확장했다.
- 즐겨찾기에 따로 추가된 소메뉴 항목의 텍스트 크기, 높이, 시작 위치를 일반 메뉴 행과 맞췄다.

## 2026-07-03 추가 18

- 뒤로가기/앞으로가기 버튼을 살짝 왼쪽으로 이동했다.
- 뒤로가기/앞으로가기 버튼의 hover, focus, active 상태에 선택 효과를 추가했다.

## 2026-07-03 추가 19

- 사이드바 접기 버튼을 브랜드 헤더 행 안으로 옮겨 `MCP 연결관리자` 텍스트와 높이를 맞췄다.
- 접기 버튼의 absolute 위치 지정 대신 브랜드 헤더 flex 정렬을 사용하도록 변경했다.

## 2026-07-03 추가 20

- 사이드바 접기 버튼 크기를 다시 키우고 hover 배경 효과를 추가했다.
- 일반 메뉴 행에 hover 시 아이콘, 텍스트, 화살표 전체가 함께 반응하는 효과를 추가했다.
- 소메뉴 hover 효과도 선택 상태처럼 행 전체에 적용되도록 수정했다.

## 2026-07-03 추가 21

- 사이드바 접기 버튼의 기본 배경을 제거하고 접힌 상태에서 펼치기 버튼이 다시 보이도록 분리했다.
- 접힌 사이드바에서 즐겨찾기 소메뉴 아이콘이 다른 메뉴 아이콘과 같은 중앙 기준에 오도록 정렬했다.
- 간소화 모드에서 뒤로가기/앞으로가기 버튼이 본문 상단 오른쪽 빈 영역에 고정되도록 위치를 조정했다.

## 2026-07-03 추가 22

- ESC 키를 누르면 우클릭 메뉴, 설정 창, 프로세스 모니터 창이 닫히도록 전역 키 처리를 추가했다.
- 즐겨찾기에 따로 추가된 소메뉴의 줄 높이와 텍스트 시작 위치를 일반 메뉴 행과 맞췄다.

## 2026-07-03 추가 23

- 최근 사용 헤더 오른쪽에 전체 삭제 버튼을 추가했다.
- 전체 삭제 버튼을 누르면 최근 사용 소메뉴 목록이 즉시 비워지도록 연결했다.

## 2026-07-03 추가 24

- 사이드바가 펼쳐진 상태의 메뉴 닫기 버튼 표시 크기를 키워 접힌 상태의 열기 버튼과 시각적 무게를 맞췄다.

## 2026-07-03 추가 25

- 선택된 열린 탭의 양옆에는 탭 구분선이 보이지 않도록 active 탭과 active 직전 탭의 separator를 숨겼다.

## 2026-07-03 추가 26

- 마지막 열린 탭이 선택된 경우 새 탭 버튼 앞 구분선도 숨기도록 조정했다.
- 펼쳐진 사이드바의 메뉴 닫기 버튼 자체 크기와 기호 크기를 더 키웠다.

## 2026-07-03 추가 27

- 최근 사용 전체 삭제 아이콘을 흰색 단색으로 변경했다.
- 최근 사용 목록을 모두 지워도 최근 사용 제목과 구분선은 유지되도록 렌더링 조건을 바꿨다.

## 2026-07-03 추가 28

- 최근 사용 전체 삭제 아이콘을 휴지통 모양의 흰색 CSS 아이콘으로 변경했다.
- 열린 탭 닫기 버튼의 실제 크기와 글자 크기를 키우고, 간소화 모드에서도 24px로 다시 줄어들지 않도록 수정했다.

## 2026-07-03 추가 29

- 최근 사용 휴지통 아이콘 색을 순백색에서 사이드바 보조 아이콘 톤으로 낮췄다.
- 직전 변경에서 잘못 키웠던 열린 탭 X 버튼은 원래 크기로 되돌리고, 실제 요청 대상인 메뉴 줄이기 버튼을 더 크게 조정했다.

## 2026-07-03 추가 30

- 메뉴 줄이기 버튼의 화살표가 커지지 않던 원인이 `.brand span` 규칙의 `font-size: 12px` 덮어쓰기였음을 확인했다.
- 브랜드 span 규칙을 버전 텍스트 전용으로 좁히고, 메뉴 줄이기 버튼 화살표가 버튼의 큰 글자 크기를 상속하도록 수정했다.

## 2026-07-03 추가 31

- 메뉴 줄이기 버튼이 제목보다 과하게 커지지 않도록 버튼 박스와 화살표 크기를 `MCP 연결관리자` 제목 행에 맞춰 낮췄다.

## 2026-07-03 추가 32

- 전체 UI 변경을 코드리뷰 관점으로 점검하면서 탭별 좌측 메뉴 선택 상태가 전역 state에 남을 수 있는 문제를 확인했다.
- 열린 탭 모델에 `sidebarSource`와 `submenuKey`를 저장해 탭 전환 시 메뉴/즐겨찾기/소메뉴 active 표시가 해당 탭 상태로 복원되도록 수정했다.
- 탭 모델 테스트에 새 선택 상태 기본값 검증을 추가했다.

## 2026-07-03 추가 33

- 다음 단계로 앱 소스의 깨진 한글 UI 문자열을 점검했다.
- `settingsDialog.ts`의 화면 모드 문구를 정상 한국어로 확인하고 `일반 모드`, `다크 모드` 표기로 정리했다.
- 사용자 화면에 남아 있던 `Process Monitor`, `Settings`, `MCP Registry` 라벨을 한국어 기준의 `프로세스 모니터`, `설정`, `MCP 연결관리자`로 정리했다.
- `TODO.md`와 `README_HANDOFF.md`를 현재 상태에 맞게 갱신했다.

## 2026-07-03 추가 34

- 즐겨찾기 소메뉴 텍스트가 일반 즐겨찾기 메뉴와 맞지 않던 원인이 별도 `padding: 0 6px` 및 다른 행 구조였음을 확인했다.
- 즐겨찾기 메뉴/소메뉴 텍스트의 높이, 굵기, 시작점을 같은 기준으로 맞췄다.
- 다크모드에서 사이드바 버튼 배경이 아이콘/메뉴명/화살표 칸별로 따로 보이던 문제를 부모 행 중심 배경으로 재정리했다.
- 다크모드 메뉴 펼침 배경과 탭 색상을 차분한 청회색 계열로 조정했다.

## 2026-07-03 추가 35

- 즐겨찾기 소메뉴를 맞추는 과정에서 일반 메뉴 텍스트 크기까지 줄였던 문제를 되돌렸다.
- 일반 메뉴 텍스트는 원래 상속 기준으로 복구하고, 따로 추가된 즐겨찾기 소메뉴만 `font: inherit`로 메뉴 텍스트 기준을 따르도록 수정했다.

## 2026-07-03 추가 36

- 요청하지 않았던 `Process Monitor`, `Settings`, `MCP Registry` 화면 라벨 한글화를 원래 표기로 되돌렸다.
- 간소화 모드에서 설정 창의 `화면 모드` 탭을 열면 내비게이션 영역이 과도하게 늘어나고 본문 패널이 절반 폭에 묶이던 문제를 수정했다.
- compact dialog에서 상단 설정 메뉴는 내용 높이만 차지하고, 화면 모드 패널은 전체 폭을 사용하도록 조정했다.

## 2026-07-03 추가 37

- 다음 단계 1번으로 MCP 서버 추가/수정/삭제 기능을 진행했다.
- Electron main/preload IPC를 `data/registry.json` 기반 registry store에 연결해 앱에서 서버 목록을 실제 파일로 읽고 쓰게 했다.
- 설정 창의 서버 상세 패널을 편집 폼으로 바꾸고 `추가`, `저장`, `초기화`, `삭제` 버튼을 연결했다.
- 서버 편집 draft/검증/선택 유지 로직을 `registryEditor.ts`로 분리하고 테스트를 추가했다.
- 검증 완료: `pnpm test`, `pnpm typecheck`, `pnpm build` 통과.

## 2026-07-03 추가 38

- 프로젝트 기준을 웹 미리보기가 아니라 Electron 데스크톱 프로그램 우선으로 정정했다.
- 기본 `pnpm dev`는 Electron 프로그램 창을 실행하고, 웹 미리보기는 `pnpm dev:web`으로 분리했다.
- Electron main/preload 전용 Vite 빌드를 추가해 `dist-electron` 산출물이 생성되도록 했다.
- `type: module` 환경에서 CommonJS 번들이 `.js`로 실행되어 main process 오류가 나던 문제를 `.cjs` 산출물로 수정했다.
- 검증 완료: Electron 실행 시 main process 오류가 즉시 발생하지 않고 프로세스가 유지됨, `pnpm test`, `pnpm typecheck`, `pnpm build` 통과.

## 2026-07-03 추가 39

- 다른 사람이 실행할 수 있도록 Windows 포터블 exe 패키징 스크립트를 추가했다.
- `pnpm package:win` 실행 시 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`와 필요한 Electron 런타임 폴더가 생성된다.
- exe 단독 배포가 아니라 `AI_PROGRAM-win32-x64` 폴더 전체를 전달하는 방식으로 정리했다.
- 배포 실행 시 registry 저장 위치를 프로젝트 폴더가 아닌 `%APPDATA%\ai-program\registry.json`으로 변경했다.
- 생성된 `AI_PROGRAM.exe` 실행을 확인했다.

## 2026-07-03 추가 40

- 패키징된 exe를 열면 흰 화면만 보이던 원인을 확인했다.
- Vite 렌더러 산출물의 asset 경로가 `/assets/...` 절대 경로라 `file://`로 로드되는 Electron 배포 앱에서 JS/CSS를 찾지 못했다.
- `packages/desktop/vite.config.ts`에 `base: "./"`를 추가해 배포 HTML이 `./assets/...` 상대 경로를 사용하도록 수정했다.
- 같은 문제가 재발하지 않도록 `rendererBuild.test.ts`를 추가했다.
- `pnpm test`, `pnpm typecheck`, `pnpm package:win` 통과 후 exe를 다시 실행했다.

## 2026-07-03 추가 41

- Process Monitor 버튼 아이콘이 이모지 렌더링에 의존해 이상하게 보일 수 있던 문제를 수정했다.
- `⏱` 텍스트 아이콘을 제거하고 CSS 기반 `monitorGlyph` 아이콘으로 바꿨다.
- `pnpm test`, `pnpm typecheck`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-03 추가 42

- 좌측 메뉴 항목이 많거나 펼쳐진 소메뉴가 많을 때 아래쪽이 잘리는 문제를 수정했다.
- 즐겨찾기와 일반 메뉴 목록을 `sidebarScrollArea`로 감싸고 마우스 휠 세로 스크롤을 추가했다.
- 하단 최근 사용, Process Monitor, Settings 영역은 메뉴 스크롤 영역 밖에 유지했다.
- `pnpm test`, `pnpm typecheck`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-03 추가 43

- 간소화 모드에서 열린 탭이 많아지면 보이지 않는 문제를 줄이기 위해 `...` 탭 목록 버튼을 추가했다.
- `...` 버튼을 누르면 현재 열린 탭 목록이 팝오버로 표시되고, 항목을 선택하면 해당 탭으로 이동한다.
- 간소화/접힌 좌측 메뉴의 글자 약자 대신 CAD, Workflow, Excel, Tekla 성격을 나타내는 앱형 아이콘을 표시하도록 수정했다.
- `pnpm test`, `pnpm typecheck`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-03 추가 44

- 설정 창 헤더 문구를 `Settings`로 단순화했다.
- 서버 요약 카드를 `등록 서버`, `연결 서버`, `미연결 서버` 기준으로 변경해 CAD/Revit 외 MCP 서버도 포괄하도록 정리했다.
- 서버 대상 타입에 `other`를 추가하고 UI 표시를 `Other`로 지원했다.
- `자동 추가` 버튼을 서버 목록 헤더 오른쪽 액션 영역으로 옮기고, 기존 수동 입력은 `수동 추가`로 구분했다.
- 자동 추가는 로컬 MCP 후보 포트를 스캔해 열린 포트를 `Other` 서버로 등록한다.
- `pnpm test`, `pnpm typecheck`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-03 추가 45

- 우측 상단 `MCP 연결상태` 배지를 버튼으로 바꾸고, 클릭 시 설정 창의 `자동 추가`와 같은 로컬 MCP 서버 감지 로직을 실행하도록 연결했다.
- 좌측 메뉴에 `Revit` 단독 메뉴를 추가하고, `CAD` 메뉴는 CAD 서버, `Revit` 메뉴는 Revit 서버를 보도록 분리했다.
- `Revit <> CAD` 메뉴명을 적용하고, CAD/Revit/Workflow 메뉴 아이콘을 패키징 누락 위험이 없는 CSS 기반 앱형 아이콘으로 교체했다.
- Autodesk 공식 아이콘 파일을 직접 포함하지 않고, 배포 가능한 자체 `A`/`R`/연결 아이콘 형태로 구현했다.
- `pnpm test`, `pnpm typecheck`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-03 추가 46

- 메뉴 본문을 서버 관리 중심에서 MCP 툴 정리 중심으로 재구성했다.
- CAD/Revit 메뉴는 MCP 툴 목록, 연결 서버 요약, 작업 구성 카드가 먼저 보이도록 바꿨다.
- 소메뉴 전용 페이지를 추가하고, 소메뉴명과 설명을 화면에서 바로 수정할 수 있게 했다.
- 소메뉴 `+` 버튼을 실제 생성 기능으로 연결해 해당 메뉴 안에 사용자 소메뉴가 추가되고 바로 선택되도록 했다.
- 소메뉴 제목/설명 수정값과 사용자 소메뉴 목록은 localStorage에 저장된다.
- 상단 제목은 별도 문구가 아니라 현재 선택한 메뉴 또는 소메뉴 이름을 기준으로 표시되도록 정리했다.
- `pnpm test`, `pnpm typecheck`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-03 추가 47

- 열린 탭 표시를 `소메뉴명 / 메뉴명`에서 `메뉴명 소메뉴명` 순서로 바꾸고, 메뉴명 앞에 접힌 메뉴와 같은 아이콘을 추가했다.
- 탭이 공간을 넘칠 때만 `...` 탭 목록 버튼을 표시하고, 이때 `+` 버튼은 숨기도록 조정했다.
- 간소화 모드에서 `...` 목록이 상위 탭 영역에 잘리지 않도록 탭 바 overflow 구조와 팝오버 위치를 수정했다.
- 설정 창 헤더의 작은 설명 문구를 `설명`으로 변경했다.
- 설정 창 `MCP 서버 > 서버 목록` 헤더에 서버 등록 안내 문구를 추가하고, 빈 상세 영역 문구를 더 자연스럽게 바꿨다.
- 자동 추가 실행 결과가 상단에 짧게 표시되도록 피드백을 추가하고, 자동 감지 후보 포트를 늘렸다.
- 본문 페이지와 간소화 화면에서 내용이 길어지면 마우스 휠로 스크롤되도록 `contentGrid`/`sectionView` overflow를 조정했다.
- `Revit <> CAD` 표기를 `Revit ↔ CAD`로 되돌리고, workflow 아이콘에 작은 양방향 화살표를 추가했다.
- `pnpm test`, `pnpm typecheck`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-03 추가 48

- 탭 안 메뉴명 배지 배경을 복구했다.
- 탭 메뉴명 배지의 텍스트와 아이콘만 작게 줄이고, 메인페이지/소메뉴명 텍스트 크기는 유지했다.
- 간소화 모드에서도 메뉴 배지가 작게 보이도록 조정했다.
- `pnpm test`, `pnpm typecheck`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-03 추가 49

- Settings의 `MCP 서버` 화면에서 `선택 서버 상세` 영역을 오른쪽 패널이 아니라 서버 목록 아래에 표시되도록 이동했다.
- 서버 상세 폼은 목록과 같은 패널 안에서 구분선 아래에 열리며, 넓은 화면에서는 입력 필드를 2열로 배치한다.
- 최근 사용 소메뉴는 최대 3개까지만 저장/표시되도록 변경했다.
- 최근 사용 소메뉴 텍스트 크기와 행 높이를 줄였다.
- 탭 메뉴명 배지 안에 아이콘을 함께 넣고, 배지 내부 아이콘 크기를 더 줄였다.
- `pnpm test`, `pnpm typecheck`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-03 추가 50

- 새 소메뉴 생성 기본 이름을 `새 소메뉴`에서 `새 페이지`로 변경했다.
- 메뉴/소메뉴 페이지에서 서버 현황 표시를 제거하고 MCP 툴/작업 정리 중심으로 문구와 레이아웃을 정리했다.
- 메뉴/소메뉴 페이지의 상단 서버 요약 카드가 일반 메뉴/소메뉴 화면에서는 보이지 않도록 조정했다.
- 소메뉴명과 설명은 항상 입력칸으로 보이지 않고, 텍스트를 클릭하면 해당 자리에서 수정되도록 변경했다.
- `pnpm typecheck`, `pnpm test`는 통과했다.
- `pnpm package:win`은 실행 중인 `release\AI_PROGRAM-win32-x64` 폴더가 잠겨 `EBUSY`로 실패했다. 앱을 닫은 뒤 다시 실행하면 패키징 가능하다.

## 2026-07-04 추가 51

- 소메뉴 기본 항목을 `Share Tools`, `Custom Tools`, `+` 흐름으로 정리했다.
- 소메뉴 `+`는 새 소메뉴를 직접 만들지 않고 Custom Tools 창을 열도록 변경했다.
- 메뉴 본문과 소메뉴 본문에 Share Tools/Custom Tools 카드 구성을 추가하고 기존 서버 현황 카드가 일반 메뉴/소메뉴에서 보이지 않도록 유지했다.
- 소메뉴 제목과 설명은 페이지의 텍스트를 클릭하면 즉시 수정되는 구조를 유지했다.
- Settings 설명 문구를 `MCP 서버 환경과 화면 표시를 설정합니다.`로 변경하고, 선택 서버 상세는 서버 선택 또는 수동 추가 시에만 목록 아래에 펼쳐지게 했다.
- 탭 오버플로 목록은 화면에 보이지 않는 탭만 표시하고, 목록 하단 `+` 버튼으로 새 탭을 만들 수 있게 했다.
- 간소화 모드 탭은 메뉴 배지/아이콘과 페이지명을 두 줄 구조로 보이게 조정했다.
- MCP 연결 상태 버튼 hover 시 연결 중인 MCP 서버 목록을 툴팁으로 표시한다.
- 커스텀 툴 리스트 창은 이름, 버전, 제작자, 사용횟수, 즐겨찾기 UI와 경로/추가 버튼을 갖춘 1차 UI로 추가했다. 실제 MD 파일 선택/복사 IPC 연결은 다음 단계로 남겼다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 실행했다.

## 2026-07-04 추가 52

- CAD뿐 아니라 Revit, Revit ↔ CAD, Excel, Tekla 메인페이지도 Share Tools / Custom Tools 카드 구조를 공통으로 사용하게 변경했다.
- 메인페이지의 바깥 `메뉴명 MCP Tools` 래퍼 제목을 제거해 Share Tools와 Custom Tools 패널이 바로 보이게 했다.
- 좌측 소메뉴는 `Share Tools`, `Custom Tools` 분류명이 아니라 실제 툴 이름을 표시하도록 바꿨다.
- 기존에 localStorage에 남은 `tools/settings` 소메뉴 순서값이 다시 나타나지 않도록 현재 유효한 툴 id만 적용한다.
- Custom Tools는 메뉴별 `sectionId`를 저장하고, Tools Market에서는 모든 메뉴의 커스텀 툴을 한 번에 볼 수 있게 했다.
- Custom Tools의 즐겨찾기 개념은 상단고정(`pinned`)으로 변경했다.
- Custom Tools 창의 폴더 버튼은 경로 표시 줄 맨 앞으로 옮겼고, 실제 OS 폴더 선택 다이얼로그를 Electron IPC로 연결했다.
- Custom Tools `+` 버튼은 실제 OS 파일 선택 다이얼로그로 `.md/.markdown` 파일을 고르게 하고, 설정된 리스트 경로가 있으면 해당 폴더로 파일을 복사한다.
- 사이드바 하단에 `Tools Market` 버튼을 추가하고, Process Monitor와 Settings는 한 줄 2분할 버튼으로 배치했다.
- 간소화 모드는 기본 접힘 상태로 시작하되 펼치면 일반 메뉴와 같은 브랜드/메뉴/하단 버튼이 보이도록 CSS를 보정했다.
- 탭 오버플로는 탭 영역이 남은 폭을 먼저 사용하도록 조정해 `...`가 너무 빨리 나타나는 문제를 줄였다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 53

- 간소화 모드에서는 좌측 메뉴를 펼치지 않는 접힘 전용 동작으로 변경했다.
- 소메뉴명과 설명 수정 위치를 본문 편집 박스에서 상단 제목/설명 영역으로 옮겼고, 본문 상단의 별도 편집 박스는 제거했다.
- 접힌 메뉴의 즐겨찾기 영역 항목에만 노란 별표 표시를 추가해 일반 메뉴 항목과 구분되게 했다.
- 사이드바의 `Process Monitor` 표기를 `Monitor`로 줄이고, `Tools Market` 버튼에는 4칸 마켓 아이콘을 추가했다.
- 하단 `Tools Market`, `Monitor`, `Settings` 버튼 글꼴 기준을 통일했다.
- 탭 `+` 버튼 옆에 보이지 않는 드롭존이 공간을 차지하던 문제를 제거해 마지막 탭 옆에 붙도록 수정했다.
- Custom Tools의 상단고정은 버튼 컬럼이 아니라 리스트 행 우클릭 메뉴에서 선택하도록 변경했다.
- 상단고정된 커스텀 툴은 리스트와 카드에서 위쪽에 정렬된다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 54

- Custom Tools 창의 별도 리스트 경로 표시 줄을 제거하고, 오른쪽 상단에 폴더 아이콘이 있는 `경로지정` 버튼으로 이동했다.
- Custom Tools의 `+` 버튼 문구를 `+ 등록`으로 변경했다.
- 기본보기에서 탭 `+` 버튼이 오른쪽 끝으로 밀리지 않도록 탭 리스트가 콘텐츠 폭을 기준으로 줄어들게 조정했다.
- Tools Market 아이콘을 4칸 아이콘에서 상점 형태 CSS 아이콘으로 변경했다.
- 접힌 메뉴에서는 최근 사용 영역이 보이지 않도록 숨겼다.
- 소메뉴 상단 제목/설명 영역이 탭 영역과 겹치지 않도록 상단바 높이와 여백을 늘렸다.
- 소메뉴 기본 본문 레이아웃을 좌측 `작동 원리`, 우측 `설정` 2분할 구조로 재구성했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 55

- 탭 오버플로 버튼의 점 3개 표시를 제거하고 `+` 버튼으로 보이게 변경했다.
- Tools Market / Custom Tools 테이블 맨 앞에 `프로그램` 컬럼을 추가했다.
- Custom Tools 창의 상단 버튼 문구를 `경로 지정`, `등록`으로 정리했다.
- Custom Tools 빈 목록 안내 문구도 `등록 버튼` 기준으로 수정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 56

- 탭의 `+` 버튼은 항상 새 탭을 추가하도록 오버플로 목록 동작과 완전히 분리했다.
- 남아 있던 탭 오버플로 상태/목록 렌더링 코드를 제거해 `+` 클릭 시 리스트가 뜨지 않도록 정리했다.
- 간소화 모드에서는 탭 목록과 하단 Monitor/Tools Market/Settings 버튼을 숨기고, 기본 보기로 돌아갈 상단 버튼은 유지하도록 CSS를 조정했다.
- 메인페이지의 Share Tools / Custom Tools 카드를 클릭하면 해당 툴 소메뉴로 이동하도록 연결했다.
- 메뉴별 Custom Tools 창에서는 `프로그램` 컬럼을 숨기고, 전체 Tools Market에서만 프로그램 컬럼을 표시하도록 분리했다.
- Tools Market 아이콘과 `경로 지정`/`등록` 버튼 정렬을 보정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 57

- Tools Market 아이콘을 상점 모양에서 렌치 모양으로 변경했다.
- Custom Tools와 Tools Market의 `등록` 버튼 문구를 `툴 등록`으로 변경했다.
- Custom Tools/Tools Market 테이블 컬럼 제목을 클릭하면 `내림차순 → 오름차순 → 기본` 순서로 정렬되도록 추가했다.
- 메뉴별 Custom Tools 창은 프로그램 컬럼 없이 정렬되고, 전체 Tools Market에서만 프로그램 컬럼을 표시한다.
- 소메뉴 제목과 탭이 겹치던 원인은 `.main.noMetrics`가 상단바 높이를 고정해 긴 설명 줄바꿈을 수용하지 못한 것이어서, 상단바 행을 내용 기반 높이로 변경했다.
- 간소화 모드의 제목/설명 영역은 설명 길이에 따라 시작 위치가 흔들리지 않도록 최소 높이와 여백을 고정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 58

- 소메뉴 설명 수정 textarea가 공통 textarea 높이 규칙에 의해 두 줄 높이로 열리던 문제를 상단 설명 전용 한 줄 편집 높이로 보정했다.
- 간소화 모드에서 앞/뒤 이동 버튼을 숨기고, 기본 보기 버튼과 MCP 상태 표시를 상단 우측으로 내려 불필요한 탭 줄 공간을 제거했다.
- 간소화 모드는 기본보기의 활성 탭/소메뉴를 그대로 따라가지 않도록 `activeCompactSection` 상태를 별도로 두었다.
- 간소화 모드에서 Tools Market과 하단 유틸리티 버튼은 보이지 않도록 정리했다.
- Tools Market 아이콘은 L자 육각렌치 형태로 변경했다.
- 메인페이지의 Custom Tools 목록을 카드형에서 표 형식으로 변경했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 59

- `간소화` 버튼 문구를 `Player view`로, `기본 보기` 문구를 `Full view`로 변경했다.
- Full view에서만 보이는 `웹에서 보기` 버튼을 추가하고 Electron IPC로 기본 브라우저에서 현재 웹 빌드를 열도록 연결했다.
- Player view에서는 `웹에서 보기` 버튼이 보이지 않도록 유지했다.
- Player view에서는 Share Tools / Custom Tools의 설명 문구와 Share Tools 항목 설명을 숨겨 화면 밀도를 줄였다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 60

- 메인페이지 Custom Tools 영역은 컬럼 타이틀이 있는 표가 아니라 Share Tools와 같은 툴 항목 목록으로 표시되도록 되돌렸다.
- Custom Tools / Tools Market의 컬럼 타이틀과 정렬 가능한 표는 `+` 버튼으로 여는 리스트 창에만 남겼다.
- Tools Market 아이콘을 L자 육각렌치에서 몽키스패너 형태로 변경했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 61

- 탭 영역 `+` 버튼을 탭 바 중앙 높이에 맞추고 hover/focus/active 효과를 추가했다.
- `Player view`, `웹에서 보기` 버튼에 hover/focus/active 효과를 추가하고 글자색을 같은 기준으로 통일했다.
- 다크모드에서도 `+`, `Player view`, `웹에서 보기` 버튼 효과와 색상이 일관되도록 보정했다.
- 소메뉴 설명 아래 빈공간의 원인이던 상단바 최소 높이, 패딩, multiline 편집 버튼 높이를 줄였다.
- 메인메뉴 상단 제목/설명 영역도 소메뉴 상단과 같은 낮은 높이 기준으로 맞추고, Share Tools / Custom Tools 항목 내부 여백은 조금 늘렸다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 62

- 소메뉴/툴 탭 앞쪽에 작업 상태 점을 추가했다. 현재 실행 상태 데이터가 없으므로 기본 작업중 상태는 주황색으로 표시한다.
- 작업 상태 점은 완료 초록색, 오류 빨강색 클래스를 함께 준비해 이후 MCP 실행 결과와 연결할 수 있게 했다.
- 고정 탭은 점 표시 대신 탭 배경색을 살짝 다르게 표시하도록 변경했다.
- Custom Tools / Tools Market 창의 `경로 지정` 버튼 텍스트가 아이콘 기준선보다 내려가 보이던 문제를 버튼 내부 span 정렬로 보정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 63

- 고정 탭 배경색이 너무 약해 일반 탭과 구분되지 않던 문제를 해결하기 위해 더 분명한 배경/테두리/상단 라인을 추가했다.
- `경로 지정` 버튼 정렬 문제의 원인이 버튼 내부 구조 차이였음을 확인했다. `경로 지정`은 `span`, `툴 등록`은 텍스트 노드라 이전 CSS가 한쪽에만 적용됐다.
- 두 버튼의 텍스트를 동일한 `.dialogHeaderButtonLabel` 구조로 맞추고, 아이콘이 있는 `경로 지정` 텍스트만 1px 위로 시각 보정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 64

- 고정 탭 표시가 너무 강해 보이던 배경/라인 표현을 제거하고, 탭 테두리만 파란색으로 표시하도록 단순화했다.
- 다크모드에서도 고정 탭은 파란 테두리만으로 구분되도록 맞췄다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 65

- `툴 등록` 버튼이 바로 MD 파일 선택 창을 열지 않고, 별도 툴 등록 폼을 먼저 열도록 변경했다.
- 등록 폼에서 `신규 툴`과 `버전 업데이트` 모드를 선택할 수 있게 했다.
- 등록 폼에 툴 이름, 버전, 제작자, MD 파일 선택 필드를 추가했다.
- 버전 업데이트 모드에서는 기존 커스텀 툴을 선택해 이름/버전/제작자 정보를 불러오고, 저장 시 해당 툴의 파일/버전/제작자를 갱신한다.
- 신규 툴 모드는 입력값과 선택한 MD 파일을 기준으로 새 커스텀 툴을 추가한다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 66

- 탭 영역이 Player view / 웹에서 보기 버튼 때문에 부족해질 때 숨겨진 탭을 감지하는 오버플로우 기능을 다시 추가했다.
- `+` 버튼은 계속 새 탭 추가만 담당하고, 숨겨진 탭 목록은 별도 `...` 버튼으로 분리했다.
- `...` 버튼은 실제로 가려진 탭이 있을 때만 표시되며, 목록에는 화면에 보이지 않는 탭만 나온다.
- `...` 목록에서 탭을 선택하면 해당 탭으로 이동하고 목록은 닫힌다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 67

- 탭 앞쪽 작업 상태 점은 실행/완료 미확인/오류 상태 데이터가 있을 때만 표시하도록 기본 표시를 제거했다.
- 탭 공간이 부족하면 `+` 버튼 대신 `...`만 표시하고, `...` 목록 하단에서 새 탭을 만들 수 있게 했다.
- Custom Tools / Tools Market 목록에 검색 입력을 추가했다.
- 커스텀 툴 목록 정렬은 항상 `등록한 툴 → 고정한 툴 → 일반 툴` 그룹 순서를 우선하고, 컬럼 정렬은 그룹 내부에만 적용되도록 정리했다.
- 커스텀 툴 목록에 `등록/해제`, `삭제` 버튼을 추가하고, 등록한 툴만 메인페이지와 소메뉴에 표시되도록 분리했다.
- 커스텀 툴 우클릭 메뉴에서도 등록/해제, 상단고정, 삭제를 수행할 수 있게 했다.
- 버전 업데이트 모드에서는 기존 툴을 반드시 선택해야 저장되도록 검증 문구와 대상 선택 로직을 정리했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 68

- 탭 공간이 충분한데도 `...`가 표시되던 원인을 확인했다. `+` 버튼이 이미 flex 레이아웃에서 공간을 차지하는데, 숨김 탭 계산에서 42px을 추가로 빼고 있었다.
- `.openTabs`에 남아 있던 `max-width: calc(100% - 170px)` 제한 때문에 상단바 전체에는 빈 공간이 있어도 탭 영역만 좁게 계산되던 문제를 함께 수정했다.
- 이제 탭 영역은 남은 상단바 공간을 자연스럽게 채우고, 실제로 잘리는 탭이 있을 때만 `...`가 표시된다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 69

- 탭 공간이 충분할 때 `+` 버튼이 남은 영역 끝으로 밀리지 않고 마지막 탭 바로 옆에 붙도록 탭 스트립 구조를 조정했다.
- 사이드바의 `Tools Market` 표기를 `More Tools`로 변경하고, 전체 커스텀 툴 창 제목도 `More Tools`로 맞췄다.
- 툴 등록 창의 버전 업데이트 모드에서는 `업데이트할 툴` 선택만 남기고 중복되는 `툴 이름` 입력을 숨겼다.
- 커스텀 툴 등록/업데이트 폼에 설명 입력 칸을 추가했다.
- Custom Tools / More Tools 리스트에서 행을 클릭하면 설명이 아래로 펼쳐지고, 설명은 별도 컬럼으로 늘리지 않도록 구성했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 70

- Custom Tools 경로를 지정하면 해당 폴더의 `.md/.markdown` 파일을 읽어 목록에 자동 동기화하도록 Electron IPC를 추가했다.
- 폴더의 MD 상단 front matter에서 툴 이름, 설명, 버전, 제작자, 프로그램 분류를 읽어 리스트에 표시한다.
- 툴 등록/업데이트 시 입력한 툴 이름, 설명, 버전, 제작자, 프로그램 분류를 MD front matter로 추가해 지정 폴더에 복사하도록 변경했다.
- 지정한 리스트 폴더 안의 MD 파일은 다시 따로 등록하지 못하도록 파일 선택 후 검증을 추가했다.
- 버전 업데이트에서 폴더 연동 툴을 선택한 경우 기존 파일명을 유지해 덮어쓰도록 하여 중복 파일 생성을 줄였다.
- 리스트의 `삭제` 컬럼/버튼을 제거하고, 사용횟수 옆에는 등록/해제 버튼만 남겼다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 71

- Custom Tools / More Tools 리스트에서 등록된 툴 행은 연한 녹색 계열 배경으로 구분되도록 변경했다.
- 등록 버튼은 초록색 배경, 해제 버튼은 빨간색 배경으로 상태를 명확히 표시했다.
- 커스텀 툴을 열 때 사용횟수가 1회 증가하도록 연결했다. 실제 MCP 실행 기능이 붙으면 실행 이벤트 기준으로 이전할 수 있다.
- 커스텀 툴 우클릭 메뉴에서도 삭제 항목을 제거했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 72

- 메인 메뉴의 `Share Tools`와 `Custom Tools` 헤더에 서로 다른 아이콘을 추가해 구분성을 높였다.
- 메인 작업 영역 배경을 흰색 대신 옅은 하늘색 계열로 바꾸고, 우측 여백을 늘려 창 가장자리와 패널이 너무 붙어 보이지 않도록 조정했다.
- 커스텀 툴 소메뉴에 진입하면 제작자와 버전은 툴 이름 오른쪽 배지로 이동하고, 기존 설명 위치에는 툴 등록 시 입력한 설명이 표시되도록 변경했다.
- 다크모드에서도 새 배지와 아이콘 배경이 어울리도록 색상을 보정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 73

- `Share Tools` / `Custom Tools` 구분 아이콘을 메인 카드 헤더가 아니라 좌측 소메뉴 리스트 아이콘 위치에 적용하도록 수정했다.
- 메인 카드 헤더는 이전 구조로 복구했다.
- Share Tools 소메뉴는 기존 4점 아이콘을 유지하고, Custom Tools 소메뉴는 연필 모양 아이콘으로 표시되도록 바꿨다.
- 메인페이지 Custom Tools 카드에서도 툴 이름 옆에 버전/제작자를 표시하고, 아래에는 등록 시 입력한 설명을 표시하도록 변경했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 74

- 커스텀 툴 업데이트 시 기존 MD와 새 MD 본문을 비교해 유사도가 낮으면 경고를 띄우고 저장을 중단하도록 했다.
- 업데이트 저장은 기존 파일을 덮어쓰지 않고 `툴이름_버전.md` 형식의 새 파일로 누적 저장하도록 변경했다.
- 같은 툴 이름/프로그램 분류의 여러 MD 버전을 하나의 툴로 묶고, More Tools 리스트와 메인페이지 Custom Tools 카드에서 버전을 선택할 수 있게 했다.
- 버전 선택 시 해당 버전의 제작자, 설명, MD 경로가 함께 전환되도록 연결했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 75

- 좌측 소메뉴의 Custom Tools 아이콘이 화살표처럼 보이던 문제를 연필 몸통/심 형태로 다시 그렸다.
- Custom Tools 아이콘 방향을 1시에서 7시 방향으로 보이도록 회전 방향을 조정했다.
- 메인페이지 Custom Tools 카드 표시를 `툴이름 v버전 - 제작자` 한 줄과 그 아래 설명 형태로 단순화했다.
- 메인페이지 Custom Tools 카드 안의 버전 선택 셀렉트는 제거하고, 버전 선택은 More Tools 리스트에서만 유지했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 76

- 메인페이지 Custom Tools 카드에서 공통 `.toolItem strong, .toolItem span` 블록 표시 규칙 때문에 `툴이름 v버전 - 제작자`가 한 줄로 나오지 않던 문제를 수정했다.
- 커스텀 툴 카드 제목 줄에만 flex override를 적용해 `AGENTS v1.0.0 - 제작자` 형식이 유지되도록 했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 77

- 메인페이지 Custom Tools 카드에서만 사용횟수 문구를 제거했다.
- More Tools 리스트의 사용횟수 컬럼, 정렬, 내부 카운트 증가는 그대로 유지했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 78

- 커스텀 툴 페이지를 여는 것만으로 사용횟수가 증가하던 임시 로직을 제거했다.
- 실제 MCP 실행 이벤트가 아직 연결되지 않았으므로 Custom Tools / More Tools 리스트의 사용횟수는 0으로 초기화해 표시한다.
- 사용횟수 컬럼과 정렬은 유지하되, 이후 실제 실행/완료 이벤트가 붙을 때만 증가하도록 연결할 예정이다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 79

- 우측 상단 MCP 연결 상태 배지가 전체 서버 상태가 아니라 현재 열린 페이지의 프로그램 기준으로 표시되도록 변경했다.
- CAD 메인/툴 페이지는 CAD MCP, Revit 페이지는 Revit MCP, Revit-CAD 흐름 페이지는 CAD/Revit MCP 연결 상태를 기준으로 표시한다.
- 상태 배지 hover 툴팁도 현재 페이지에 해당하는 실행 중 서버만 보여주도록 조정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 80

- Player view에서 Custom Tools / More Tools 리스트의 제작자, 사용횟수 컬럼을 숨기고 표 폭 안에서 이름, 버전, 등록 상태가 맞도록 조정했다.
- Share Tools / Custom Tools 카드 헤더의 세로 여백을 늘려 제목과 설명 영역이 답답해 보이지 않도록 공통 보정했다.
- Player view에서는 카드 설명을 숨기는 기존 동작에 맞춰 헤더 높이를 작게 유지하도록 별도 보정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 81

- `+` 새 탭의 기본 페이지를 Home으로 변경하고, pinned 탭이 없을 때 초기 화면도 Home으로 열리도록 조정했다.
- 사이드바에 Home 버튼을 추가하고, 메뉴 접힘/Player view 상태에서는 상단 아이콘으로 바로 접근할 수 있게 했다.
- Home 페이지에 공지사항, 신규 커스텀 툴, 오류 리포트 카드를 추가했다.
- Player view에서 실제 표시 중인 메뉴 기준으로 MCP 연결 상태가 계산되도록 수정했다.
- 탭 바 배경을 연한 하늘색으로 바꾸고, 선택된 탭은 흰색으로 보이도록 조정했다.
- 탭 overflow(`...`) 리스트 항목에 드래그 정렬과 닫기 버튼을 추가하고, 리스트 아이콘 크기/정렬을 보정했다.
- Custom Tools 경로는 전역 공통 경로임을 표시하고, 폴더 동기화 실패 시 오류 메시지가 보이도록 보강했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 82

- 우측 상단 MCP 연결 상태 배지는 현재 페이지 기준 서버 상태를 보되, 표시 문구에서는 CAD/Revit 같은 메뉴명을 빼고 `MCP 연결됨/미연결` 형태로 되돌렸다.
- Home 버튼을 메뉴 라벨 오른쪽의 작은 아이콘 버튼으로 이동하고, 메뉴 접힘/Player view에서는 기존처럼 아이콘만 보이도록 분리했다.
- 탭과 overflow 리스트에서 Home 아이콘이 깨져 보이지 않도록 작은 크기 전용 선 두께와 위치를 보정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 83

- Home 페이지의 카드 헤더 영역을 다른 주요 카드와 같은 넉넉한 밀도 기준으로 키웠다.
- Home 페이지 안쪽 텍스트 카드의 패딩, 최소 높이, 줄 간격을 늘려 답답하지 않게 조정했다.
- 앞으로 새로 만드는 정보 카드도 이 넉넉한 헤더/본문 여백 기준을 기본값으로 삼기로 했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 84

- 메뉴 라벨 아래에 중복으로 남아 있던 Home 버튼을 제거하고, 메뉴 라벨 오른쪽의 작은 Home 아이콘 버튼만 남겼다.
- Home 아이콘은 배경 박스 없이 흰색 집 모양으로 보이도록 수정했다.
- 탭/overflow 리스트의 작은 Home 아이콘도 깨지지 않게 크기별 선과 위치를 보정했다.
- Home 페이지 카드 목록의 상단 여백을 늘려 다른 페이지처럼 제목 영역 아래에 충분한 공간이 생기도록 조정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과 후 새 exe를 실행했다.

## 2026-07-04 추가 85

- 탭 안의 Home 아이콘은 사이드바 흰색 아이콘과 분리해 청회색으로 보이도록 조정했다.
- Player view의 접힌 메뉴에서도 Home 아이콘 버튼이 보이도록 compact 전용 예외를 추가했다.
- Home 페이지의 공지사항, 신규 커스텀 툴, 오류 리포트 카드 본문이 각각 마우스 휠로 스크롤되도록 패널 높이와 overflow를 보정했다.
- 메뉴 라벨 옆 Home 아이콘 크기를 별도 보정하고, Home 페이지에서도 버튼이 눌린 상태처럼 보이지 않게 active 배경을 제거했다.
- Home 아이콘을 한 덩어리 집 실루엣으로 다시 만들고, 탭/overflow 리스트에서 더 크게 보이도록 조정했다.
- Player view의 Home 페이지는 카드별 내부 스크롤이 아니라 전체 페이지 스크롤로 내려가도록 compact 전용 overflow를 수정했다.
- Player view에서 실제로 페이지 전체가 스크롤되도록 compact `main` 영역에 세로 스크롤을 부여했다.
- 메뉴 라벨 옆 Home 아이콘의 중심이 맞아 보이도록 위치를 보정했다.
- More Tools 아이콘을 흰색 몽키스패너 실루엣으로 다시 정리했다.
- Player view 본문을 잘라내지 않고 전체 흐름으로 스크롤되도록 compact `main`/`contentGrid` overflow 규칙을 다시 보정했다.
- Home 페이지에서도 메뉴 옆 Home 버튼에 active 테두리가 생기지 않도록 메뉴 헤더 전용 active 스타일을 강화했다.
- 최근 사용 헤더를 클릭하면 최근 사용 목록을 접고/펼칠 수 있게 하고, 라벨 표시를 정상 한국어로 보정했다.
- More Tools 아이콘은 배경 없이 렌치 실루엣 자체만 흰색으로 보이도록 다시 만들었다.
- 메뉴 접힘 상태에서 즐겨찾기 아래쪽에 Home 아이콘 버튼을 추가하고, Player view와 중복되지 않게 분리했다.
- Home 아이콘 중심이 어긋난 원인을 비대칭 clip-path와 강제 translate 보정으로 보고, 실루엣 좌표 자체를 중앙 기준으로 다시 잡았다.
- More Tools 렌치 아이콘은 CSS 조각 대신 단일 SVG 마스크를 사용해 배경 없는 흰색 실루엣으로 교체했다.
- 소제목 페이지의 `작동 원리`/`설정` 패널 헤더도 메인페이지 카드처럼 66px 높이와 넉넉한 제목/설명 여백을 기본 기준으로 맞췄다.
- 최근 사용 접기/펼치기 구조를 `role=button` div에서 실제 텍스트 버튼 + 삭제 버튼 구조로 정리해 이벤트와 접근성 꼬임을 줄였다.

## 2026-07-04 추가 86

- 아이콘 시스템을 `packages/desktop/src/uiIcons.tsx`로 분리하고 `AppIcon` 컴포넌트로 Home, CAD, Revit, Workflow, Excel, Tekla, More Tools, Monitor, Settings, Share Tools, Custom Tools 아이콘을 통일했다.
- 사이드바/탭/More Tools/Monitor/Settings 버튼과 Share Tools / Custom Tools 헤더 아이콘을 공통 SVG 아이콘 컴포넌트로 교체했다.
- 상태 뱃지를 `packages/desktop/src/toolStatus.tsx`의 `ToolStatusRow`로 분리했다.
- Share Tools 카드에는 `공유`, `MCP 준비` 뱃지를 표시하고, Custom Tools 카드에는 `등록됨/미등록`, `상단고정` 뱃지를 표시하도록 했다.
- 이전 `marketGlyph`, `monitorGlyph` CSS 조각은 JSX 참조가 없어 제거했다.
- 남아 있던 `.navIcon-*::before`, `.navIcon-*::after`, `.workflowArrow` 기반 아이콘 CSS를 제거하고, 메뉴/탭/overflow 리스트 모두 `AppIcon` SVG만 사용하도록 정리했다.
- Settings 버튼에 남아 있던 보조 기어 문자와 깨져 보일 수 있는 title/aria-label 조합을 정리했다.
- 반복되는 패널 헤더 구조를 `packages/desktop/src/panelHeader.tsx`의 `PanelHeader`로 분리하고 Share Tools / Custom Tools 헤더부터 적용했다.

## 2026-07-04 추가 87

- 탭 안 프로그램 아이콘 크기가 서로 달라 보이던 원인을 탭 전용 컨테이너와 SVG 크기 불일치로 보고, 탭/overflow 리스트 아이콘 크기를 별도 기준으로 맞췄다.
- 프로그램 문자 아이콘의 글자 크기와 SVG 텍스트 기준점을 키워 CAD/Revit/Excel/Tekla 아이콘이 더 잘 보이도록 조정했다.
- 좌측 메뉴 소메뉴의 Share Tools / Custom Tools 구분 아이콘을 CSS 조각 대신 `AppIcon` 기반 `shareTools` / `customTools` 아이콘으로 교체했다.
- Player view에서는 툴 카드의 `공유`, `MCP 준비`, `등록됨` 같은 상태 뱃지가 보이지 않도록 숨겼다.
- 현재 메뉴에 필요한 MCP 서버가 running 상태가 아니면 Share/Custom 툴 카드를 회색 비활성 상태로 표시하고 클릭을 막도록 했다.

## 2026-07-04 추가 88

- 탭 추가 `+` 버튼과 overflow `...` 버튼이 새 탭 추가 때마다 번갈아 나타나던 문제를 수정했다.
- 원인은 `+` 버튼과 `...` 버튼의 실제 차지 폭이 달라 overflow 계산 결과가 흔들리는 것이어서, 두 버튼을 같은 고정 폭 `tabControlSlot` 안에서 교체되도록 바꿨다.
- 메뉴를 접었을 때도 메뉴 아이콘을 직접 드래그해 순서를 변경할 수 있도록 `navItem`에 드래그 이벤트를 연결했다.
- 고정 탭 강조는 파란 테두리 대신 회색 테두리로 바꿨다.
- 메인페이지 툴 카드의 `등록됨`, `공유`, `MCP 미연결` 상태 문구를 제거하고, MCP 미연결은 회색 비활성 카드와 hover 문구로만 표시하도록 정리했다.
- Settings 버튼에서 공통 아이콘 컴포넌트가 CSS에 의해 숨겨지던 문제를 수정해 톱니바퀴 아이콘을 복구했다.
- Revit-CAD workflow 아이콘은 화살표를 제거하고 Revit/CAD 아이콘이 대각선으로 겹치도록 바꿨다.
- MD 파일이 툴 문서 형태로 보이지 않아도 등록과 실행은 허용하되, 등록 폼 경고, 메인페이지 경고색 카드, 실행 전 확인창으로 사용자가 확인할 수 있게 했다.

## 2026-07-05 추가 89

- 탭 추가 `+` 버튼과 overflow `...` 버튼이 새 탭 추가 때마다 번갈아 나타나던 문제를 다시 추적했다.
- 원인은 overflow 판단이 `.openTabs`의 현재 flex 폭과 레이아웃 타이밍에 의존해 경계 상황에서 숨김 상태가 흔들리는 구조였기 때문에, `tabStrip 전체 폭 - 컨트롤 슬롯 폭`을 기준으로 탭 폭 합계를 계산하도록 변경했다.
- overflow 계산을 `packages/desktop/src/tabOverflow.ts`의 `getHiddenTabIds`로 분리하고 경계값/초과 상태 테스트를 추가했다.
- Settings 톱니바퀴 아이콘은 작은 버튼 안에서 비대칭 path가 찌그러져 보이던 문제를 줄이기 위해 균형 잡힌 원형 톱니 SVG로 교체했다.

## 2026-07-05 추가 90

- Home 페이지의 세 번째 카드였던 `오류 리포트`를 `Other Tools` 카드로 바꾸고, 클릭 시 전체 커스텀 툴 목록인 `More Tools` 창을 열도록 연결했다.
- 기존 오류 리포트 내용은 Monitor 화면 안쪽의 별도 `오류 리포트` 패널로 이동했다.
- 탭 overflow `...` 목록의 아이콘과 텍스트가 살짝 어긋나 보이던 원인을 넓은 `span` CSS 선택자로 보고, 라벨 전용 `tabOverflowItemLabel` 클래스를 추가해 아이콘 칸과 텍스트 시작점을 고정했다.

## 2026-07-05 추가 91

- Custom Tools / More Tools 창의 로컬 `경로 지정` 버튼과 `공통 경로` 표시 줄을 제거했다.
- 상단 액션을 `GitHub` 버튼으로 바꾸고, 버튼 클릭 시 GitHub 저장소 URL을 입력해 앱에 저장하도록 했다.
- Home의 신규 커스텀 툴 설명과 README_HANDOFF의 Custom Tools 설명을 GitHub 공유 기준으로 수정했다.

## 2026-07-05 추가 92

- Codex에서 `/save 툴이름` 요청을 로컬 MD 툴 초안 생성으로 처리하는 사용자 스킬 `save-tool`을 `C:\Users\DONG KIM\.codex\skills\save-tool`에 생성했다.
- 스킬은 GitHub 업로드를 하지 않고 현재 대화/작업 내용을 `tool-drafts/툴이름.md` 형태의 로컬 초안으로 저장하도록 지시한다.
- `scripts/create_tool_draft.py`를 포함해 tool front matter(`toolName`, `version`, `author`, `description`, `sectionId`, `createdAt`, `source`)를 안정적으로 생성하도록 했다.
- 생성 스크립트는 샘플 본문으로 MD 파일 생성까지 확인했다. `quick_validate.py`는 현재 Python 환경에 `yaml` 모듈이 없어 실행이 중단되어, TODO/frontmatter/파일 구조를 별도로 확인했다.

## 2026-07-05 추가 93

- Home의 `Other Tools` 설명을 `/SAVE 툴이름`으로 만든 스킬과 공유 스킬을 내려받아 사용하는 공간이라는 방향으로 변경했다.
- More Tools 전체 목록에서는 컬럼/버튼 문구를 `등록` 대신 `다운로드`로 표시해, 다른 사람이 공유한 스킬을 받아 사용하는 흐름과 맞췄다.
- 메뉴별 Custom Tools 창은 기존처럼 `등록` 문구를 유지한다.

## 2026-07-05 추가 94

- More Tools 창 설명과 테이블 컬럼/버튼 문구를 기존 `등록` 기준으로 되돌렸다.
- Home의 `Other Tools` 카드만 `채팅을 TOOL로 저장` 안내로 바꾸고, `/SAVE 툴이름 명령하면 해당 채팅이 TOOL로 저장됩니다.` 문구와 오른쪽 `다운로드` 버튼을 배치했다.
- 고정 탭의 테두리 색을 파란색에서 회색 계열로 변경했다.
- 메인페이지 툴 카드에서 `등록됨`, `공유`, `MCP 미연결` 상태 뱃지를 제거하고 비활성 상태는 회색 카드 배경만으로 표현하도록 정리했다.
- Settings 아이콘이 보이지 않던 원인은 기존 텍스트 기어 숨김 CSS가 `AppIcon`까지 숨긴 것이어서 해당 규칙을 제거했다.
- Revit ↔ CAD 아이콘에서 화살표를 제거하고, Revit/CAD 두 앱 아이콘이 더 크게 대각선으로 겹치는 형태로 단순화했다.
- 비활성화된 툴 카드에 hover 기본 툴팁과 `aria-label`로 실행 불가 사유를 표시하도록 했다.

## 2026-07-05 추가 95

- 앱 하단에 전역 알림 토스트 영역을 추가했다.
- 알림은 검정 배경과 흰 글씨로 표시되며, 우측 닫기 버튼으로 개별 알림을 숨길 수 있다.
- 현재는 업데이트/중요 안내가 표시될 위치를 확인할 수 있도록 기본 안내 알림을 하나 띄운다.
- 실제 새 버전 확인이나 공지 수신 로직은 아직 연결하지 않았으며, 이후 업데이트 체크 기능에서 `appNotifications` 상태에 항목을 추가하는 방식으로 연동하면 된다.

## 2026-07-05 추가 96

- Home > Other Tools의 다운로드 버튼을 텍스트 버튼에서 다운로드 아이콘 버튼으로 변경했다.
- 다운로드 버튼이 More Tools 창을 여는 대신, 앱에 포함된 `save-tool` Codex 스킬을 현재 사용자 `~/.codex/skills/save-tool` 폴더에 설치하도록 Electron IPC를 추가했다.
- `packages/desktop/assets/skills/save-tool`에 `SKILL.md`, `agents/openai.yaml`, `scripts/create_tool_draft.py`를 포함해 다른 컴퓨터에서도 프로그램만 있으면 같은 스킬을 설치할 수 있게 했다.
- Windows 패키징 스크립트가 `packages/desktop/assets` 폴더를 release 앱 리소스에 함께 복사하도록 수정했다.
- 현재 컴퓨터의 `C:\Users\DONG KIM\.codex\skills\save-tool`에도 새 bundled 스킬 구성을 복사해 갱신했다.

## 2026-07-05 추가 97

- Custom Tools / More Tools에서 MD 파일을 등록하거나 폴더 동기화로 읽을 때, 파일 내용을 분석해 모델 객체/요소 삭제 또는 생성 후보 동작을 감지하도록 했다.
- 감지 기준은 삭제/생성 계열 동작어와 객체/요소/블록/패밀리/레이어/모델 같은 대상어가 함께 나타나는 경우다.
- 감지된 툴은 리스트의 툴 이름 옆에 네모 형태 `주의` 배지를 표시한다.
- `주의` 배지를 클릭하면 해당 행이 펼쳐지고, 경고 이유가 설명 위쪽에 표시된다.
- 메인페이지의 Custom Tools 카드에서도 위험 후보 툴에는 `주의` 배지를 표시하고, 실행 전 확인창을 한 번 띄우도록 했다.

## 2026-07-05 추가 98

- Other Tools의 `/SAVE` 스킬 다운로드는 고정 절대경로가 아니라 Electron `app.getPath("home")` 기준으로 현재 사용자 홈의 `.codex/skills/save-tool`에 설치된다. 따라서 다른 컴퓨터에서도 해당 컴퓨터 사용자 계정 경로에 맞춰 설치된다.
- Share Tools 카드도 Custom Tools처럼 툴 이름 아래에 `v1.0.0 - MCP Registry` 형태의 버전/제작자 메타를 표시하도록 했다.
- Share Tools 데이터 타입에 선택적 `version`, `author` 필드를 추가하고, 기존 공유 툴은 기본값으로 표시한다.
- 좌측 메뉴에서 메뉴를 즐겨찾기하면 원래 메뉴 목록에서는 숨기고, 즐겨찾기 해제하면 다시 메뉴 목록에 나타나도록 했다.

## 2026-07-05 추가 99

- Home의 Other Tools 설명을 한 줄로 들어가기 쉽도록 `채팅을 TOOL로 저장하고 내려받습니다.`로 줄였다.
- Share Tools의 버전/제작자 메타를 Custom Tools처럼 툴 이름 옆 한 줄에 붙여 표시하도록 바꿨다.
- Share Tools가 아래 MCP 툴 목록에 반복 표시되는 영역도 같은 인라인 메타 기준으로 맞췄다.
- More Tools / Custom Tools 창 상단의 GitHub, 툴 등록 버튼 텍스트가 낮게 보이던 문제를 줄이기 위해 버튼 내부 라벨 높이와 패딩을 조정했다.

## 2026-07-05 추가 100

- More Tools / Custom Tools 창 상단에서 GitHub 버튼을 제거했다.
- GitHub 저장소 URL을 입력/저장하던 renderer 상태와 localStorage 연동도 더 이상 사용하지 않도록 제거했다.
- Home의 신규 커스텀 툴 설명을 GitHub 기준 문구에서 최근 등록된 커스텀 툴 기준 문구로 변경했다.
- 현재 More Tools / Custom Tools 목록은 실제 GitHub 저장소에서 읽은 항목이 아니라 앱 localStorage와 기존 로컬 MD 동기화 흐름에서 들어온 항목이라는 점을 README_HANDOFF에 기록했다.

## 2026-07-05 추가 101

- More Tools / Custom Tools 창을 열 때 GitHub 저장소 `kdg200121-hash/AI_PROGRAM`의 `tools/` 폴더에서 `.md/.markdown` 툴 목록을 자동으로 읽도록 Electron IPC를 추가했다.
- GitHub에서 가져온 툴은 `github:owner/repo/path` 형식의 source/installed path로 구분하며, 기존 등록/상단고정/사용횟수 상태를 유지한 채 목록만 갱신한다.
- GitHub MD frontmatter에 제작자가 없으면 기본 제작자를 저장소 owner인 `kdg200121-hash`로 표시하도록 했다.
- `툴 등록` 신규 폼의 기본 제작자도 현재 GitHub 기준 이름인 `kdg200121-hash`로 변경했다.
- GitHub tools 폴더 자리 표시용 `tools/.gitkeep`을 추가했다.

## 2026-07-05 추가 102

- 좌측 상단 브랜드 영역의 접기 버튼 왼쪽에 로그인 버튼을 추가했다.
- 로그인 버튼은 로그인 전 `로그인`, 로그인 후 `닉네임님`으로 표시되며 클릭 시 회원 정보 창을 연다.
- 로그인 창에는 `GitHub로 로그인`, `회원가입` 버튼을 배치했고, 회원가입/회원정보 화면에서는 GitHub ID와 닉네임을 입력/수정할 수 있게 했다.
- 현재는 GitHub OAuth App `client_id`가 없으므로 실제 OAuth 토큰 발급 대신 GitHub ID와 닉네임을 앱 localStorage에 저장하는 UI/상태 기반으로 구현했다.
- 툴 등록 신규 폼의 제작자 기본값은 로그인 닉네임을 우선 사용하고, 로그인 전에는 GitHub owner를 사용한다.
- 기존 localStorage에 남아 있던 로컬 MD 동기화 툴은 시작 시 제외하고, GitHub `tools/` 폴더에서 읽은 `github:` source 툴만 표시되도록 변경했다.

## 2026-07-05 추가 103

- 로그인 흐름을 가짜 localStorage 입력 방식에서 실제 GitHub OAuth Device Flow 기반 IPC로 교체했다.
- Electron main process에서 `github-auth:begin-login`, `github-auth:poll-login`, `github-auth:get-profile`, `github-auth:update-nickname`, `github-auth:logout` IPC를 제공한다.
- GitHub access token은 renderer localStorage에 저장하지 않고 `%APPDATA%\ai-program\github-auth.json`에 저장하며, renderer에는 GitHub ID/닉네임/아바타 URL만 전달한다.
- GitHub 로그인 시작 시 브라우저로 verification URL을 열고, 앱에는 user code와 `인증 완료 확인` 버튼을 표시한다.
- 실제 인증을 시작하려면 `AI_PROGRAM_GITHUB_CLIENT_ID` 또는 `VITE_GITHUB_CLIENT_ID` 환경 변수에 GitHub OAuth App client_id가 필요하다.
- 좌측 상단 로그인/닉네임 버튼 위치를 제목 줄에서 버전 `v0.1.0` 줄로 내렸다.

## 2026-07-05 추가 104

- Settings 왼쪽 메뉴에 `계정 정보`와 `관리` 섹션을 추가했다.
- `계정 정보`에서는 현재 GitHub 로그인 계정, 관리자 여부, 라이선스 사용 가능 여부를 확인하고 회원 정보 수정/로그아웃을 실행할 수 있다.
- `관리` 섹션은 GitHub ID가 `kdg200121-hash`인 관리자 계정에서만 활성화된다.
- 관리자 화면에서 회원가입 방식을 `자유 가입` 또는 `승인제`로 선택할 수 있게 했다.
- 관리자 화면에서 라이선스 정책을 `모두 사용` 또는 `라이선스만`으로 선택할 수 있게 했다.
- 회원 목록에서 승인 대기 계정을 승인하고, 계정별 라이선스를 부여/해제하고, 일반 계정을 삭제할 수 있게 했다. 관리자 계정은 삭제/라이선스 해제할 수 없다.
- 라이선스 정책이 `라이선스만`이거나 계정이 승인 대기 상태이면 앱 위에 사용 제한 안내 오버레이를 표시한다.
- 현재 계정/정책/회원 목록은 로컬 앱 상태(localStorage) 기반이며, 여러 컴퓨터에서 중앙 강제하려면 GitHub 업로드 또는 서버 동기화가 다음 단계로 필요하다.

## 2026-07-05 추가 105

- Settings > 계정 정보 안의 로그인 버튼을 누르면 로그인 모달이 Settings 모달 뒤에 깔려 보이지 않던 문제를 수정했다.
- 원인은 인증 모달과 Settings 모달이 같은 z-index의 backdrop을 사용하고, Settings 모달이 나중에 렌더링되어 위에 덮이는 구조였다.
- 인증 모달 backdrop에 `authDialogBackdrop` 클래스를 추가하고 z-index를 더 높여 Settings 안에서도 로그인 창이 정상적으로 보이게 했다.
- GitHub OAuth client_id를 환경 변수뿐 아니라 앱에 포함되는 `data/github-oauth.json`에서도 읽도록 추가했다.
- `data/github-oauth.json` 템플릿을 추가했다. 실제 GitHub OAuth App의 Client ID를 `clientId`에 넣으면 다른 사용자는 환경 변수 설정 없이 로그인할 수 있다.

## 2026-07-05 추가 106

- GitHub Developer Settings에서 `AI_PROGRAM MCP Registry` OAuth App을 생성했다.
- OAuth App의 Device Flow를 활성화하고 Client ID를 `data/github-oauth.json`에 반영했다.
- Client Secret은 생성하거나 저장하지 않았다. 현재 로그인은 공개 Client ID만 사용하는 GitHub OAuth Device Flow 기준이다.
- 새 Client ID가 포함되도록 Windows 포터블 exe를 다시 패키징하고 실행했다.

## 2026-07-05 추가 107

- GitHub 재로그인 시 회원가입/회원정보에서 저장한 닉네임이 GitHub 이름으로 덮이는 문제를 수정했다.
- 원인은 Device Flow 인증 완료 후 새 프로필을 만들 때 입력 닉네임이 비어 있으면 항상 GitHub `name/login`을 닉네임으로 저장하던 로직이었다.
- 같은 GitHub 계정의 기존 저장 프로필이 있으면 기존 닉네임을 우선 유지하고, 사용자가 새 닉네임을 입력한 경우에만 새 값으로 갱신하도록 분리했다.
- `githubAuthProfile.test.ts`를 추가해 기존 닉네임 유지, 새 입력 우선, 다른 계정 닉네임 미재사용 케이스를 검증했다.
- 로그인 토큰/프로필은 Electron 사용자 데이터 폴더의 `github-auth.json`에 저장되므로 로그아웃하거나 사용자 데이터가 삭제되지 않는 한 앱 재시작/PC 재부팅 후에도 유지된다.

## 2026-07-05 추가 108

- Settings 상단 설명을 `MCP 서버, 화면 모드, 계정과 권한 정책을 설정합니다.`로 정리했다.
- 승인제 회원가입에서 승인 대기 계정이 `Settings > 관리 > 회원 목록`에 표시된다는 안내 문구를 회원 목록 상단에 추가했다.
- 회원 목록의 라이선스 칸을 텍스트와 별도 부여/해제 버튼 구조에서 `활성화`/`비활성화` 상태 버튼 구조로 변경했다.
- 라이선스 활성화 버튼은 초록색, 비활성화 버튼은 회색으로 표시되도록 스타일을 추가했다.
- 관리 정책에 `툴 등록 방식`을 추가해 `자유 등록`과 `승인제`를 선택할 수 있게 했다.
- 툴 등록 방식이 `승인제`이고 일반 사용자가 커스텀 툴을 추가하면 `승인 대기` 상태로 저장되며, 관리자만 목록에서 승인할 수 있게 했다.
- 인코딩이 깨져 있던 일부 UI 문자열이 실제 JSX 문법 오류로 변한 부분을 복구하고, 타입체크/테스트/패키징을 다시 통과시켰다.

## 2026-07-05 추가 109

- Settings > 관리 > 회원 목록에 `가입일` 컬럼을 추가했다.
- 회원 목록의 `계정`, `닉네임`, `상태`, `라이선스`, `가입일` 타이틀을 클릭하면 내림차순/오름차순/기본 순서로 정렬되도록 했다.
- 회원 목록 정렬 UI는 Custom Tools / More Tools 표의 정렬 버튼 패턴을 재사용한다.
- 이전 작업 중 일부 남아 있던 깨진 한글 UI 문자열을 다시 점검하고, 사이드바/설정/커스텀툴/홈/소메뉴/모니터/서버 상세 화면의 주요 문구를 UTF-8 한글 문장으로 복구했다.
- `pnpm test`와 `pnpm typecheck`를 다시 통과시켰다.

## 2026-07-05 추가 110

- 전역 알림 토스트의 검정 배경 투명도를 낮추고 blur를 추가해 뒤 화면이 약하게 비치도록 조정했다.
- 알림창과 탭 닫기 버튼에 남아 있던 깨진 닫기 문자 `횞`를 `×`로 교체했다.
- 실행이 느려진 원인을 점검한 결과, GitHub tools 목록 fetch는 앱 시작이 아니라 More Tools / Custom Tools 창을 열 때만 실행된다.
- 현재 시작 지연 후보는 커진 renderer 초기 렌더, 여러 localStorage 상태 복원/저장 effect, 단일 `App.tsx`에 누적된 UI 로직이다.

## 2026-07-05 추가 111

- 툴 등록 저장 흐름을 GitHub PR 생성 방식으로 연결했다.
- GitHub Device Flow 로그인 scope를 `read:user public_repo`로 확장했다. 기존 로그인 토큰은 권한이 부족할 수 있으므로 PR 생성 실패 시 재로그인이 필요하다.
- 툴 등록 시 앱이 MD frontmatter를 정리한 뒤 GitHub `tools/` 폴더 대상 브랜치에 파일을 올리고 Pull Request를 만든다.
- 저장소 owner가 아닌 사용자는 fork를 만들고 fork 브랜치에서 원 저장소로 PR을 생성한다. fork 생성 직후 API 준비 시간을 고려해 repository 준비 대기 로직을 추가했다.
- Custom Tools / More Tools 목록에 `PR 생성됨` 배지와 `GitHub PR 보기` 링크를 표시한다.
- 등록 방식이 `자유 등록`이면 앱 내부 승인 상태는 즉시 approved가 되고, `승인제`이면 기존처럼 관리자 승인 대기로 저장된다. GitHub 공유 저장소 반영은 두 경우 모두 PR 머지가 필요하다.
- Settings > 관리의 회원가입 방식/라이선스 사용/툴 등록 방식 카드가 한 줄에 보이도록 3열 배치와 카드 크기를 조정했다.
- 앱 시작 직후 복원된 localStorage 상태를 다시 쓰는 effect를 첫 렌더에서는 건너뛰게 해 초기 로딩 부담을 줄였다.

## 2026-07-05 추가 112

- 툴 공유 정책을 `자유 등록 + 안전한 툴 = 직접 GitHub commit`, `위험 툴 또는 승인제 = Pull Request 승인` 기준으로 변경했다.
- 자유 등록에서 안전한 툴은 원 저장소 `tools/` 폴더에 직접 commit을 시도하고, push 권한이 없으면 자동으로 fork/PR 방식으로 전환한다.
- 위험 감지 툴은 자유 등록이어도 앱 내부 승인 대기와 GitHub PR 승인 대기 상태로 저장된다.
- PR 상태 자동 추적을 추가했다. Custom Tools / More Tools 열기 또는 새로고침 시 PR 번호가 있는 툴은 GitHub에서 `승인 대기`, `머지됨`, `닫힘` 상태를 갱신한다.
- Settings > 관리에 `승인 대기 툴` 목록을 추가해 앱 승인 상태, GitHub PR 상태, PR 보기 링크, 상태 새로고침을 제공한다.
- GitHub Releases latest를 확인해 현재 앱 버전보다 새 릴리스가 있으면 하단 알림을 띄우도록 했다.
- 툴 등록 폼에 MD 미리보기 패널을 추가했다. 파일 선택 후 문서 앞부분, 위험 감지 결과, 안전 안내를 등록 전에 확인할 수 있다.
- `toolSharingPolicy.ts`를 추가해 공유 승인 정책과 버전 비교 로직을 `App.tsx`에서 분리했고, `toolSharingPolicy.test.ts`로 정책을 검증한다.

## 2026-07-05 추가 113

- 탭 옆 새 탭 `+` 컨트롤이 탭 높이보다 위로 떠 보이던 문제를 수정했다.
- `+`/`...` 컨트롤 슬롯을 탭 하단 기준으로 정렬해 탭 높이와 시각 중심이 맞도록 했다.
- 다크모드에서 More Tools, Monitor, Settings, Home 하단 버튼에 hover/active 효과가 보이지 않던 문제를 수정했다.
- Settings 창의 메뉴 버튼, 화면 모드 선택 버튼, 세그먼트 버튼, 일반/삭제 액션 버튼에 기본모드와 다크모드 hover/focus/press 효과를 추가했다.
- 전역 `button` 효과 변경은 기존 버튼 느낌을 바꾸므로 되돌리고, 앞으로 새 버튼은 기존 개별 버튼 클래스/패턴을 따르는 기준으로 정리했다.
- Settings > 관리의 `상태 새로고침` 버튼을 텍스트 버튼에서 아이콘 버튼으로 변경했다.

## 2026-07-05 추가 114

- 하단 알림 토스트 폭을 줄이고 화면 중심에 고정되도록 조정했다.
- 시작 시 표시되던 기본 업데이트 안내 알림을 제거하고, 실제 새 GitHub Release가 있을 때만 업데이트 알림이 뜨도록 유지했다.
- 툴 등록 PR 상태가 `merged` 또는 `closed`로 바뀌면 승인/거절 알림이 뜨도록 했다.
- 프로그램이 켜져 있는 동안에도 PR 상태를 주기적으로 확인해 승인/거절 상태 변화가 알림으로 표시되도록 했다.
- 관리자가 앱 내부 승인 대기 툴을 승인하면 즉시 `툴 등록이 승인되었습니다` 알림이 뜨도록 했다.
- 향후 기간제 라이선스를 위해 계정에 `licenseExpiresAt` 필드를 추가하고, 만료 7일 전 1회 및 3일 이하부터 일별 알림이 뜨는 구조를 준비했다.
- 프로그램 첫 렌더링 시 짧은 로딩 오버레이가 표시되도록 추가했다.

## 2026-07-05 추가 115

- 하단 알림 토스트 중심 계산을 전체 창 기준에서 사이드바를 제외한 메인 콘텐츠 영역 기준으로 변경했다.
- 툴 소메뉴 화면의 `작동 원리`/`설정` 2분할 비율을 조정해 설정 칸이 더 넓게 보이도록 했다.
- 다른 컴퓨터에서도 MCP MD 툴을 쉽게 만들 수 있도록 Codex 스킬 `mcp-tool-builder`를 사용자 스킬 폴더에 추가했다.
- `mcp-tool-builder` 스킬은 프로그램 종류, 툴 목적, 작동 원리, 설정 필드, 위험 작업 여부를 질문으로 정리하고 AI Program용 Markdown 툴 파일 구조를 생성하도록 안내한다.
- 스킬 참고 문서 `references/ai-program-md-tool.md`에 frontmatter, 작동 원리, 설정 필드 타입, 프로그램별 질문 기준을 정리했다.

## 2026-07-05 추가 116

- 프로그램 시작 시 React가 렌더된 뒤 보이던 로딩 오버레이를 제거하고, `index.html`에 정적 시작 로딩 화면을 추가했다.
- Electron 창이 비어 있는 초기 구간에도 로딩 화면이 먼저 표시되고, React 앱이 렌더되면 자동으로 실제 화면으로 교체된다.

## 2026-07-05 추가 117

- `Revit ↔ CAD`로 보이던 워크플로우 메뉴/탭 표시명을 `커스텀 플로우`로 변경했다.
- 내부 id는 기존 `workflow`를 유지해 저장된 탭, 즐겨찾기, 최근 사용 데이터와의 호환성을 유지했다.
- 커스텀 플로우 페이지의 설명과 보드 문구를 기존 CAD/Revit 전달 흐름에서 MCP 툴을 노드처럼 연결하는 자동화 흐름으로 정리했다.
- `navigationModel`, `workspaceTabs`, `mcpToolCatalog`에 남아 있던 깨진 한글 문자열을 정상 문구로 정리하고 관련 테스트 기대값을 갱신했다.

## 2026-07-05 추가 118

- `mcp-tool-builder` 스킬을 데스크톱 앱 번들 자산(`packages/desktop/assets/skills`)에 추가했다.
- Home > Other Tools에 `MCP TOOL 만들기` 항목을 추가해 다운로드 버튼으로 스킬을 설치할 수 있게 했다.
- Electron preload와 main IPC에 `installMcpToolBuilder` 경로를 추가해 다른 컴퓨터에서도 앱에서 바로 `~/.codex/skills/mcp-tool-builder`로 설치되도록 했다.
- 번들되는 `mcp-tool-builder` 문서는 한글 깨짐이 없도록 정상 한글 질문/템플릿 기준으로 재작성했다.

## 2026-07-05 추가 119

- 워크플로우 표시명을 한국어 `커스텀 플로우`에서 영어 `Custom Flow`로 변경했다.
- 프로그램 시작 로딩이 보이지 않는 문제를 줄이기 위해 Electron 창 생성 시 로딩 전용 HTML을 먼저 띄운 뒤 실제 앱 화면으로 전환하도록 보강했다.
- `index.html`에 남아 있던 깨진 로딩 문구와 잘못된 태그를 정상 한글 문구로 복구했다.
- Home > 신규 커스텀 툴 영역의 `등록된 커스텀 툴이 없습니다` 빈 상태 메시지를 카드 중앙에 맞추도록 조정했다.
- Custom Flow 화면에 1차 노드 캔버스를 추가했다. 페이지 내부 툴 팔레트에서 드래그하거나 `+` 버튼으로 노드를 추가할 수 있고, 노드를 펼치면 입력/출력 포트를 확인할 수 있다.

## 2026-07-05 추가 120

- Custom Flow 캔버스를 단순 세로 목록에서 자유 배치형 보드로 변경했다.
- 노드 위치를 상태로 관리하고, 노드 헤더를 잡아 캔버스 안에서 드래그 이동할 수 있게 했다.
- 팔레트에서 캔버스로 툴을 드롭하면 드롭 위치 기준으로 새 노드가 생성되도록 조정했다.

## 2026-07-05 추가 121

- Custom Flow에서 캔버스가 화면 아래로 밀려 보이지 않던 문제를 수정했다.
- 3단계 설명 카드를 캔버스 아래로 내리고, Custom Flow 화면의 첫 영역을 실제 Flow Canvas로 변경했다.
- 캔버스 테두리, 배경 그리드, `Flow Canvas` 라벨을 강화해 작업 영역이 명확히 보이도록 했다.
- 기본 노드 위치를 캔버스 라벨 아래에서 시작하도록 조정했다.

## 2026-07-05 추가 122

- Custom Flow 메뉴에서 전용 `WorkflowView`가 렌더되지 않고 기존 `ToolWorkspaceView`가 표시되던 분기 오류를 수정했다.
- `workflow` 섹션은 Share Tools/Custom Tools 메인 레이아웃을 건너뛰고 Flow Canvas 전용 화면을 바로 표시한다.
- Custom Flow 캔버스에 노드 연결선 SVG 레이어와 입력/출력 포트 점을 추가해 Dynamo, Grasshopper, Blueprint류의 노드 편집기 방향이 보이도록 했다.
- 노드 폭을 줄이고 가로 배치 기본값을 적용해 캔버스 위에서 배치/연결 구조가 더 명확하게 보이도록 했다.

## 2026-07-05 추가 123

- Custom Flow 화면의 내부 팔레트와 하단 설명 카드를 제거하고, 본문 영역 전체를 노드 캔버스로 사용하도록 변경했다.
- 캔버스 위 마우스 휠로 55%~180% 범위에서 줌 인/줌 아웃되도록 추가했다.
- 줌 배율이 바뀌어도 노드 드래그와 드롭 좌표가 어긋나지 않도록 캔버스 좌표 계산을 배율 기준으로 수정했다.
- 출력 포트 클릭 후 입력 포트를 클릭하면 연결되고, 이미 연결된 조합은 다시 클릭하면 끊어지도록 노드 연결 상태를 명시적으로 관리한다.
- 입력 포트만 클릭하면 해당 노드로 들어오는 연결을 제거할 수 있게 했다.
- 노드 접힘 상태에서도 입력/출력 타입 아이콘이 보이도록 포트 미리보기를 추가하고, Excel 포트는 Excel 아이콘으로 표시되도록 정리했다.

## 2026-07-05 추가 124

- Custom Flow에서 출력 포트를 잡아 끌면 마우스를 따라 임시 연결선이 표시되고, 입력 포트 위에서 놓으면 연결되도록 변경했다.
- 마우스 휠 버튼을 누른 채 움직이면 캔버스 전체를 패닝할 수 있게 했다.
- 마우스 휠 줌은 캔버스 원점이 아니라 현재 마우스 커서 위치를 기준으로 확대/축소되도록 변경했다.
- 노드 헤더 클릭으로 펼쳐지던 동작을 제거하고, 오른쪽 화살표 버튼을 클릭할 때만 노드가 펼쳐지거나 접히게 했다.
- 노드 제목 앞에는 해당 툴의 프로그램 아이콘(CAD/Revit/Excel)을 표시하고, Share/Custom 구분 아이콘은 노드 제목에서 사용하지 않도록 정리했다.
- 연결 포트 모양을 원형에서 사각형 커넥터로 변경했다.
- 선택 노드 기준으로 `Ctrl+C`, `Ctrl+V`, `Delete/Backspace`, `Ctrl+Z`를 지원하고, 노드 우클릭 메뉴에 복사/복제/삭제를 추가했다.
- 왼쪽 메뉴의 소메뉴 드래그 데이터에 Flow 툴 id를 함께 실어 Custom Flow 캔버스에 드롭하면 노드가 생성되도록 수정했다.

## 2026-07-05 추가 125

- Custom Flow 연결 모델을 노드 단위 `from/to`에서 포트 단위 `fromNodeId/fromPortId/toNodeId/toPortId`로 확장했다.
- 노드마다 여러 입력/출력 포트를 표시하고, 포트 타입(CAD/Revit/Excel/Object 등)이 맞지 않는 연결은 점선 경고선으로 표시하도록 했다.
- 왼쪽 메뉴에서 드래그한 실제 소메뉴 정보를 Flow 노드 데이터로 전달해 Custom Flow 캔버스에 바로 추가할 수 있게 했다.
- Custom Flow 노드, 연결선, 줌 배율, 패닝 위치를 localStorage에 저장해 앱을 다시 열어도 작업 중인 플로우가 유지되도록 했다.
- 줌아웃 시 격자가 일부 구간에서 끊기던 문제를 해결하기 위해 Flow Canvas 월드와 SVG 연결 레이어 크기를 크게 확장했다.
- GitHub access token 저장 시 Electron `safeStorage`를 사용할 수 있으면 암호화해 저장하도록 보강했다.
- MCP 툴 카탈로그와 남아 있던 UI 문구의 깨진 한글 문자열을 정상 문구로 정리했다.
- `App.tsx`에 있던 Custom Flow 모델/직렬화/저장 헬퍼를 `packages/desktop/src/customFlowModel.ts`로 분리했다.

## 2026-07-06 추가 126

- Custom Flow 격자를 월드 요소 배경이 아니라 캔버스 viewport 배경으로 옮겨, 줌아웃/패닝 시 왼쪽과 상단에 빈 영역이 생기지 않도록 수정했다.
- 마우스 휠 버튼으로 캔버스를 패닝하는 동안 커서가 `grabbing`으로 바뀌도록 했다.
- 노드 외부 포트 점과 포트 미리보기 아이콘을 제거하고, 노드 내부 좌우 포트 행에 아이콘과 텍스트를 표시하도록 재설계했다.
- 연결선은 내부 포트 행의 중앙 높이에 맞춰 연결되도록 포트 좌표 모델을 조정했다.
- 노드 헤더는 프로그램 아이콘이 툴 이름 바로 앞에 오도록 정렬하고, 번호는 보조 배지로 뒤쪽에 배치했다.
- 노드 우클릭 메뉴에서 `복사` 항목을 제거하고 `복제`/`삭제`만 남겼다.
- 노드 복제 시 새 노드가 자동으로 펼쳐지지 않고 닫힌 상태로 생성되도록 변경했다.
- `Custom Flow` 메뉴/탭 아이콘을 Revit/CAD 조합 아이콘에서 노드 연결형 아이콘으로 교체했다.
- 탭의 `Custom Flow` 텍스트가 잘리지 않도록 탭 폭과 메뉴 메타 영역 폭을 조정했다.

## 2026-07-06 추가 127

- Custom Flow 입력/출력 포트 행을 버튼처럼 보이던 요소에서 일반 포트 라벨형 행으로 바꿔, 실제 연결 지점만 인터랙션 요소처럼 느껴지도록 정리했다.
- 내부 포트 행 위치에 맞춰 연결선 시작/끝 좌표를 다시 맞췄다.
- 빈 캔버스를 드래그하면 여러 노드를 박스 선택할 수 있게 했고, 선택된 노드 하나를 잡아 움직이면 선택된 노드들이 함께 이동하도록 했다.
- 노드 위치 이동 시 왼쪽/상단 좌표 제한을 제거해, 음수 좌표 영역으로도 노드를 자유롭게 옮길 수 있게 했다.
- 선택 노드 우클릭 또는 `Ctrl+G`로 그룹 박스를 만들 수 있게 했고, 그룹 정보도 Custom Flow localStorage 저장 데이터에 포함했다.
- 마우스 휠 버튼을 빠르게 두 번 누르면 현재 배치된 모든 노드가 보이도록 캔버스 줌/위치를 자동 조정하게 했다.

## 2026-07-06 추가 128

- Custom Flow 노드 순서 번호 배지를 제거했다.
- 프로그램/포트 타입별 색상표를 추가해 노드 배경, 입력/출력 커넥터, 연결선을 CAD/Revit/Excel/Tekla/Object 색상으로 구분했다.
- 입력 포트 커넥터에 마우스를 올리면 X 표시가 나타나, 클릭 시 연결을 끊을 수 있음을 알 수 있게 했다.
- 그룹 박스를 드래그하면 그룹 안 노드들이 함께 이동하도록 했다.
- 그룹 헤더에 이름 입력과 배경색 선택 컨트롤을 추가했고, 그룹 색상도 Custom Flow localStorage 저장 데이터에 포함했다.

## 2026-07-06 추가 129

- Custom Flow 연결선 시작/끝 좌표를 포트 네모 중심에 맞추기 위해 커넥터 위치와 연결 좌표 보정값을 정렬했다.
- 입력 포트에 마우스를 올리면 커서도 X 모양으로 바뀌도록 했다.
- Object/Number/Text 포트 전용 아이콘을 추가하고, 포트 행의 아이콘을 타입 색상 배지처럼 보이도록 수정했다.

## 2026-07-06 추가 130

- 전체 검토 중 남아 있던 Home/Custom Flow 안내 문구와 기본 Flow 노드 문구의 깨진 한글을 정상 문구로 복구했다.
- 기존 localStorage에 저장된 기본 Flow 노드도 `cad-read`, `excel-export`, `revit-place` id 기준으로 최신 기본 문구/포트 라벨을 사용하도록 보정했다.
- `customFlowModel.test.ts`를 추가해 기본 Flow 노드 문구와 포트 중심 연결 좌표가 다시 깨지지 않도록 회귀 테스트를 추가했다.

## 2026-07-06 추가 131

- `MonitorView`를 `packages/desktop/src/MonitorView.tsx`로 분리하고 Process Monitor 공통 타입을 `processMonitor.ts`로 분리했다.
- Electron main에 MCP 서버 프로세스 실행/중지 IPC를 추가했다. 등록 서버의 `launchCommand`, `workingDirectory`, `environment`를 사용해 child process를 실행하고 stdout/stderr/오류/종료 이벤트를 최근 로그로 유지한다.
- 설정 창의 서버 상세에 실행/중지 버튼을 연결했고, 실행 결과는 Process Monitor 로그와 서버 상태에 반영되도록 했다.
- Custom Flow 실행 전 검증 로직을 `customFlowValidation.ts`로 분리하고, 포트 타입 불일치/없는 노드 또는 포트/미연결 입력을 검출하는 패널을 캔버스에 추가했다.
- `customFlowValidation.test.ts`를 추가해 Flow 검증 로직을 회귀 테스트로 고정했다.
- Home 대시보드 디자인 개편은 사용자 요청에 따라 제외했다.

## 2026-07-06 추가 132

- Settings의 MCP 서버 목록은 전체 서버를 보여주므로, 설정 창이 열려 있을 때는 선택 상태도 전체 서버 목록 기준으로 유지하도록 수정했다.
- 이 변경으로 CAD/Revit/Tekla/Excel 등 현재 작업공간 필터 밖에 있는 서버를 Settings 목록에서 선택해도 다시 첫 서버로 되돌아가지 않는다.
- Custom Flow 입력/출력 포트 아이콘을 작은 인라인 타입 아이콘으로 정리하고, 출력 포트는 텍스트 오른쪽에 아이콘이 붙도록 바꿔 텍스트가 한쪽으로 밀려 보이는 문제를 줄였다.

## 2026-07-06 추가 133

- 사용자 피드백에 따라 Custom Flow 입력/출력 포트 안쪽의 타입 아이콘을 제거했다.
- 포트 타입 구분은 포트 네모와 연결선 색상으로만 보여주도록 정리해 노드 내부 시각 잡음을 줄였다.
- 연결선 세로 좌표를 실제 첫 포트 행 중심에 맞도록 보정하고, 회귀 테스트 기준도 함께 수정했다.

## 2026-07-06 추가 134

- Custom Flow 연결선이 포트 네모 중심이 아니라 모서리 쪽에서 나가 보이는 문제를 다시 분석했다.
- 원인은 CSS 레이아웃으로 실제 포트 네모 위치가 결정되는데, 연결선은 TypeScript의 고정 숫자 좌표(`flowConnectionEndpoint`)로 계산하던 구조였다.
- 각 포트 네모에 측정용 data attribute를 붙이고, 렌더링된 DOM의 실제 중심 좌표를 읽어 SVG 연결선 시작/끝점으로 사용하도록 변경했다.
- `flowConnectionEndpoint`는 측정값이 아직 없는 첫 렌더 시점의 fallback으로만 사용한다.

## 2026-07-06 추가 135

- Custom Flow 노드를 `Shift`를 누른 상태로 드래그하면 Photoshop/Figma Smart Guides처럼 다른 노드의 left/center/right, top/middle/bottom 기준에 가까울 때 자동으로 붙도록 했다.
- 여러 노드를 선택해 함께 움직일 때는 선택 묶음 전체의 바운딩 박스를 기준으로 정렬한다.
- 세 노드가 나란히 있을 때 가운데 노드가 양쪽 노드 사이의 같은 간격 위치에 가까워지면 자동으로 붙는 간격 스냅도 추가했다.
- 정렬선은 붉은 실선, 간격선은 보라색 점선으로 표시한다.
- Smart Guide 계산 로직을 `customFlowSmartGuides.ts`로 분리하고, 정렬/가로 간격/세로 간격 테스트를 추가했다.

## 2026-07-06 추가 136

- Custom Flow 그룹 이름 입력 글자 크기를 키우고, 그룹 색상 변경을 자유 색상 선택기에서 정해진 색상 팔레트 버튼 방식으로 바꿨다.
- 노드 우클릭 메뉴에 `그룹 만들기 Ctrl+G`, `복제 Ctrl+C / V`, `삭제 Del`처럼 단축키를 오른쪽에 표시했다.
- 빈 캔버스 우클릭 메뉴와 더블클릭으로 메모를 만들 수 있게 했고, 메모는 Custom Flow 저장 데이터에 포함된다.
- 실행 전 검증 패널을 오른쪽 아래 고정 아이콘 위젯으로 바꾸고, 정상/경고/오류 상태별 아이콘 색을 분리했다. hover 시 상세를 표시하고 클릭하면 상세가 고정된다.
- ESC를 누르면 노드 선택, 선택 박스, 연결 대기, 우클릭 메뉴, 드래그/패닝 임시 상태를 취소하도록 했다.
- 출력 포트를 클릭하면 연결 대기 상태가 유지되고, 입력 포트를 클릭하면 연결된다. 빈 캔버스를 클릭하거나 ESC를 누르면 연결 대기가 해제된다.
- 박스 선택 판정 범위를 넓히고 입력 포트 해제 커서를 더 크게 조정했다.

## 2026-07-06 추가 137

- Custom Flow Smart Guides의 같은 간격 스냅에 `60px` 같은 치수 라벨을 추가해, `Shift` 드래그 중 어떤 간격 기준으로 맞춰지는지 보이게 했다.
- 노드 위에서 마우스 휠 버튼을 눌러도 노드 선택/드래그가 아니라 캔버스 패닝이 시작되도록 이벤트 차단 순서를 수정했다.
- 캔버스 오른쪽 위에 반투명 도구막대를 추가했다. 순서는 뒤로가기, 되돌릴 위치 선택, 앞으로가기, 실행, 실행 설정이며 hover/focus 시 선명해진다.
- 실행 설정은 `일괄 실행`/`단계별 실행`을 고를 수 있고, 실행 중인 노드는 청록색 두꺼운 테두리로 강조된다.
- 기존 실행 전 검증 위젯을 `흐름 점검`으로 변경하고 오른쪽 위로 이동했다. 경고/오류 개수 배지를 아이콘 왼쪽 위에 표시한다.
- 흐름 점검 항목을 클릭하면 문제가 있는 연결선은 붉은 점선, 문제가 있는 노드는 붉은 테두리/배경으로 하이라이트한다.
- 그룹 색상 변경은 현재 그룹 색상 점 하나만 보이다가 클릭하면 팔레트가 펼쳐지는 방식으로 변경했다.
- 노드를 그룹 박스 안으로 드래그하면 그룹이 drop target 상태로 강조되고, 그 상태에서 놓으면 그룹에 포함되도록 했다.

## 2026-07-06 추가 138

- Custom Flow 메모에 상단 드래그 손잡이를 추가해 텍스트 입력 영역을 건드리지 않고 이동할 수 있게 했다.
- 메모 박스는 가로/세로 resize를 모두 허용하고, 폭/높이/색상을 localStorage 저장 데이터에 포함했다.
- 메모 색상도 그룹처럼 현재 색상 점을 클릭하면 팔레트가 펼쳐지는 방식으로 바꿨다.
- Custom Flow 상단 도구막대의 뒤로가기 왼쪽에 `기본도구` 버튼을 추가했다.
- 기본도구 창은 `툴`과 `인풋/아웃풋 도구` 탭으로 나뉜다. `툴`에는 결과 미리보기, 경로 지정, 활성 파일, 커스텀 창 기본 노드가 들어간다.
- `인풋/아웃풋 도구` 항목은 노드 위에 드롭하면 커스텀 입력/출력 포트를 추가한다.
- 커스텀 포트는 점선 스타일로 기본 포트와 구분하고, 삭제 버튼으로 연결선과 함께 제거할 수 있게 했다.
- 노드 제목과 설명 색상을 기존보다 진하게 조정했다.

## 2026-07-06 추가 139

- 그룹에 포함된 노드를 우클릭하면 `그룹에서 제거` 메뉴가 나타나고, 선택된 노드들을 그룹에서만 분리할 수 있게 했다.
- Custom Flow 오른쪽 위 도구막대 버튼 크기를 줄이고, 실행 설정은 별도 톱니바퀴가 아니라 실행 버튼 옆 작은 화살표로 변경했다.
- 도구막대는 hover 또는 메뉴가 열린 상태에서만 선명해지도록 바꿔, 실행 설정을 닫은 뒤에도 계속 불투명하게 남는 문제를 수정했다.
- 흐름 점검 창은 ESC 또는 바깥 클릭으로 닫히도록 했다.
- 앞으로가기를 `Ctrl+Y` 단축키로도 실행할 수 있게 했다.
- 그룹/메모 색상 팔레트 후보를 늘리고, 색상 선택 점이 타원으로 찌그러지지 않도록 원형 크기를 고정했다.

## 2026-07-06 추가 140

- Custom Flow 메모에서 상단 `메모` 제목과 구분선을 제거했다. 메모 배경은 드래그 이동 영역으로 쓰고, 텍스트 영역을 클릭했을 때만 문장을 수정한다.
- 기본도구의 `결과 미리보기` 노드는 출력 포트를 제거하고, 펼쳤을 때 실행 결과가 표시될 전용 미리보기 영역을 보여주도록 변경했다.
- `커스텀 창` 기본 노드를 `프롬프트`로 이름을 바꾸고 출력 포트를 제거했다. 프롬프트는 선 연결 대신 다른 노드 아래에 드래그해 붙이는 보조 노드로 동작하며, 내부에서 프롬프트 문장을 직접 입력한다.
- 기본도구의 `인풋/아웃풋 도구` 탭 이름을 `연결값 도구`로 변경했다. `텍스트`는 공용 포트 도구로 입력/출력 칸 어디에나 넣을 수 있고, `경로`/`활성파일`/`결과`처럼 방향이 정해진 도구는 오른쪽에 `input`/`output`/`공용` 배지를 표시한다.
- 연결값 도구를 드롭할 때 노드 전체가 아니라 해당 Input 또는 Output 영역에만 드롭 강조가 뜨도록 수정했다.
- 드래그한 기본 도구가 명시적으로 빈 입력/출력 포트 배열을 가질 때 기본 포트를 자동으로 붙이지 않도록 모델 파싱 로직과 회귀 테스트를 추가했다.

## 2026-07-06 추가 141

- Custom Flow 노드 설명 펼침 상태를 단일 노드가 아니라 여러 노드 배열로 바꿔, 한 노드를 열어도 다른 노드가 닫히지 않게 했다.
- 흐름 점검 팝오버는 최대 크기를 제한하고 내부 목록만 마우스 휠로 스크롤되도록 변경했다. 점검 항목은 5개 제한 없이 모두 표시한다.
- 기본도구 버튼을 도구막대 오른쪽, 흐름 점검 아이콘 가까운 위치로 옮기고 기본도구 메뉴가 화면 밖으로 잘리지 않도록 오른쪽 정렬과 최대 높이/스크롤을 적용했다.
- 마우스 휠 버튼 패닝은 캔버스 캡처 단계에서 우선 처리해 노드, 그룹, 메모, 입력칸 위를 눌러도 화면 이동이 시작되도록 했다.
- 그룹과 메모도 선택 상태를 가지도록 추가했다. 박스 선택으로 메모/그룹을 선택할 수 있고 Delete/Backspace로 메모 삭제 또는 그룹 박스 해제가 가능하다.
- 노드를 `그룹에서 제거`하면 그룹 소속만 빼는 데서 끝나지 않고 노드가 그룹 바깥쪽으로 이동해 제거 결과가 눈에 보이도록 했다.
- 마우스 휠 줌아웃 최소 배율을 55%에서 28%로 낮췄다.

## 2026-07-06 추가 142

- Custom Flow를 Dynamo, Grasshopper, Blueprint 같은 비주얼 코딩 도구 관점에서 검토했다. 즉시 보강할 부분은 `포트 타입 부족`과 `연결 전 호환성 피드백 부족`으로 판단했다.
- 연결값 도구에 `숫자`, `좌표`, `객체`, `테이블`, `파일`, `폴더`, `참/거짓`, `선택 요소`, `로그`, `오류`, `리포트`를 추가했다. 가능한 도구는 공용으로 두고, 입력/출력 전용이 자연스러운 항목만 방향을 제한했다.
- Flow 포트 타입에 `coordinate`, `table`, `file`, `boolean`을 추가하고 타입별 색상 팔레트를 확장했다.
- 출력 포트를 선택해 연결 대기 상태일 때 입력 포트가 연결 가능하면 초록색, 타입이 맞지 않아 경고 연결이 될 경우 붉은색으로 미리 표시되도록 했다.
- 후속 제안으로 미니맵, 노드 검색, reroute 노드, subflow, 실행 로그 타임라인, 포트 자동 변환 노드, 정렬/배치 명령 팔레트가 필요하다고 정리했다.

## 2026-07-06 추가 143

- Custom Flow 도구막대에 `검색` 버튼과 `Ctrl+F` 단축키를 추가했다. 노드 이름, 설명, 입력/출력 포트 라벨을 검색하고 결과를 클릭하면 해당 노드를 선택, 펼침, 화면 중앙 이동 처리한다.
- 메모가 노드/그룹 뒤에 묻혀 선택하기 어려운 문제를 줄이기 위해 메모 레이어를 높였다.
- 메모 텍스트 영역을 클릭해 선택한 상태에서도 `Ctrl+Delete`로 메모를 삭제할 수 있게 했다. 메모 빈 영역이나 박스 선택으로 선택한 경우는 기존 Delete/Backspace 삭제를 유지한다.
- 메모 위에서 `Shift+드래그`하면 메모 이동 대신 선택 박스를 시작하도록 추가했다.

## 2026-07-06 추가 144

- Custom Flow 메모의 드래그 실패 원인을 텍스트 입력 영역이 포인터 이벤트를 먼저 먹는 구조로 보고, 메모 상단에 별도 이동 손잡이와 제목 입력칸을 추가했다.
- 메모 우클릭 메뉴에 `복제`, `삭제`를 추가했다. 메모 텍스트 편집 중에는 글자 삭제와 메모 삭제가 섞이지 않도록 우클릭 메뉴 또는 `Ctrl+Delete` 삭제를 사용한다.
- 저장된 기본 노드 데이터를 정규화해 `결과 미리보기`, `경로 지정`, `활성 파일`, `프롬프트` 노드의 불필요한 입력/출력 포트가 다시 나타나지 않게 했다.
- `결과 미리보기` 노드는 펼쳤을 때 작동 원리 대신 결과값 영역을 보여주고, `프롬프트` 노드는 내부 텍스트 입력 영역을 표시한다.
- 일괄 실행 시 모든 노드가 동시에 실행 강조되던 문제를 수정했다. 실행 버튼을 누르면 노드가 순서대로 하나씩 파란색 실행 테두리로 지나가며 표시된다.
- 흐름 점검의 오류/경고가 있는 노드는 실행 전후에도 해당 노드에 경고/오류 배지와 테두리로 표시되도록 했다.
- 흐름 점검 패널은 긴 메시지가 밖으로 튀어나오지 않도록 줄바꿈과 내부 스크롤 제한을 보강했다.
- 기본도구 버튼은 오른쪽 도구막대 끝, 흐름 점검 아이콘 바로 옆 위치로 유지되도록 정렬을 확인했다.

## 2026-07-06 추가 145

- Custom Flow 메모 우클릭 메뉴가 좁은 화면에서 오른쪽으로 잘려 삭제 버튼을 누를 수 없던 문제를 확인했다. 메뉴 위치를 화면 안쪽으로 자동 보정하도록 수정했다.
- 노드 우클릭 메뉴도 같은 방식으로 화면 안쪽에 뜨도록 보정했다.
- 메모 제목 입력칸과 본문 textarea에 직접 `onContextMenu`를 연결해, 메모 내부 어느 곳에서 우클릭해도 앱의 `복제`/`삭제` 메뉴가 뜨도록 했다.
- 메모 객체 선택과 텍스트 편집이 섞이지 않도록 메모 이동은 상단 손잡이에서만 시작하고, 메모 박스 클릭은 선택만 하도록 정리했다.
- 중간 마우스 버튼 패닝 중 브라우저 기본 보조 클릭 동작이 끼어들지 않도록 캔버스 `auxclick` 기본 동작을 차단했다.
- `활성 파일` 기본 노드는 입력 포트 없이 현재 파일 값을 출력하는 노드로 정리했고, 저장된 이전 `basic-active-file` 노드도 같은 구조로 정규화했다.

## 2026-07-06 추가 146

- 기본도구의 프롬프트 노드 이름을 최종 요청 기준인 `프롬프트`로 변경했다. 저장된 이전 `basic-custom-prompt` 노드도 `프롬프트` 이름으로 정규화한다.
- 프롬프트 노드를 다른 노드 아래로 드래그할 때, 조건에 맞는 첫 노드가 아니라 아래쪽 거리가 가장 가까운 노드에 붙도록 감지 로직을 바꿨다.
- 메모나 그룹, 노드, 입력칸 위에서 마우스 휠 버튼을 눌러도 캔버스 패닝이 더 안정적으로 시작되도록 `pointerdown`과 `mousedown` 캡처를 함께 처리한다.
- 메모 입력 중 `Esc`를 누르면 입력칸 focus를 해제하고 열린 메뉴/팔레트를 닫도록 수정했다. 다른 캔버스/노드/그룹 영역을 클릭할 때도 활성 입력칸을 blur 처리한다.
- 노드 검색 버튼 문구를 `노드 검색`으로 바꿔 기능 위치가 더 명확히 보이게 했다.
- 흐름 점검 팝오버와 항목에 `min-width`, `overflow-x: hidden`, 강제 줄바꿈을 보강해 긴 내용이 창 밖으로 튀어나오지 않게 했다.
- 기본도구 창 폭을 화면 폭 기준으로 제한해 좁은 창에서 잘릴 가능성을 줄였다.

## 2026-07-06 추가 147

- 그룹 안의 노드를 우클릭했을 때 선택 상태 갱신 타이밍 때문에 `그룹에서 제거`가 누락될 수 있어, 선택 노드 목록뿐 아니라 우클릭한 노드 자체도 그룹 포함 여부 검사와 제거 대상에 포함했다.
- `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`는 Electron 기본 실행 파일을 복사해 만드는 구조라 수정된 앱 코드가 들어가도 파일 시간이 오래된 것처럼 보일 수 있었다. 패키징 후 exe의 수정 시간을 현재 시간으로 갱신하도록 `package-win.mjs`를 보정했다.
- 최신 코드로 `pnpm typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop package:win`을 통과했고, `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`와 `release\AI_PROGRAM-win32-x64-latest.zip`을 다시 생성했다.
- 브라우저 확인용 개발 서버도 `127.0.0.1:5173`에서 다시 실행했다.

## 2026-07-06 추가 148

- Custom Flow에서 노드를 드래그할 때 화면이 튀는 원인을 노드 위치 상태 변경마다 드래그 이벤트 effect가 재등록되는 구조와 localStorage 저장이 매 이동마다 실행되는 구조로 보고 수정했다. 노드 드래그 effect는 드래그 시작 당시 상태를 기준으로 유지하고, 저장은 짧게 지연해 이동 중 끊김을 줄였다.
- 흐름 점검 팝오버의 항목이 서로 겹쳐 보이지 않도록 패널 폭과 내부 스크롤 영역을 키우고, 점검 항목을 자동 높이 카드처럼 표시하도록 CSS를 정리했다.
- 노드의 경고/오류 아이콘에 마우스를 올리면 해당 노드와 관련된 흐름 점검 사유가 표시되도록 노드별 이슈 목록을 연결했다.
- 프롬프트 노드는 붙기 전에는 끊어진 연결 아이콘, 다른 노드 아래에 붙은 뒤에는 연결된 아이콘으로 표시되게 했다. 붙일 대상 노드에는 `프롬프트 붙이기` 슬롯과 강조 효과가 나타난다.
- 노드 아이콘, 노드명, 설명 텍스트를 더 진하게 보이도록 라이트/다크 모드 색상과 아이콘 대비를 조정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm --filter @mcp-registry/desktop package:win` 통과 후 최신 exe와 zip을 다시 생성했다.

## 2026-07-06 추가 149

- 노드 드래그 중 화면이 여전히 튀는 문제를 다시 분석했다. 남아 있던 핵심 원인은 드래그 중 노드 위치가 바뀔 때마다 포트 중심 좌표를 DOM에서 다시 측정하고, 그 측정 결과가 추가 렌더링을 만드는 구조였다.
- 드래그 중인 노드/그룹의 포트 위치는 DOM 측정값 대신 노드 좌표 기반 계산값을 사용하도록 바꿨고, 드래그 중에는 포트 중심 재측정을 건너뛰도록 했다.
- 노드/그룹/메모 이동 업데이트를 `requestAnimationFrame` 단위로 묶어 마우스 이동 이벤트가 과하게 들어와도 화면 갱신은 프레임 단위로 안정적으로 일어나게 했다.
- 드래그 위치 계산을 `applyFlowNodeDrag` 함수로 분리하고, 이미 한 번 이동된 노드 배열이 들어와도 시작 위치 기준으로만 계산한다는 회귀 테스트를 추가했다.
- 패키징 중 기존 `AI_PROGRAM.exe` 프로세스 4개가 release 폴더를 잠그고 있어 `EBUSY`가 발생했다. 실행 중인 앱을 종료한 뒤 `pnpm --filter @mcp-registry/desktop package:win`을 다시 실행해 최신 exe와 zip을 생성했다.

## 2026-07-06 추가 150

- Custom Flow를 실제 Chrome 디버그 창에서 열어 노드 드래그를 12단계로 이동시키며 확인했다. 좌표가 입력한 이동량대로 일정하게 변해 테스트 화면에서는 튐 현상이 재현되지 않았다.
- 그룹 안의 노드를 우클릭해 `그룹에서 제거`를 실제로 눌렀을 때 저장된 그룹 목록에서 노드가 빠지지 않던 문제를 수정했다. 메뉴 닫힘 이벤트와 충돌하지 않도록 우클릭 메뉴의 pointer 이벤트를 보강하고, 그룹 제거 로직을 `removeNodeIdsFromFlowGroups` 함수와 테스트로 분리했다.
- 기본도구의 `연결값 도구` 목록에서 `공용`, `Input`, `Output` 배지는 방향별 색상으로 통일하고, 적용 가능한 프로그램은 별도 배지로 표시하도록 정리했다. `객체` 항목처럼 긴 프로그램명이 세로로 눌리지 않게 버튼 레이아웃을 3줄 구조로 고정했다.
- 기본도구 창 위에서 마우스 휠을 돌릴 때 캔버스 줌이 같이 움직이지 않도록 도구창 내부 wheel 이벤트 전파를 차단했다.
- 커스텀 입력/출력 포트의 삭제 X 버튼을 작은 원형 버튼으로 바꾸고 입력/출력 방향에 맞게 자연스럽게 정렬되도록 스타일을 정리했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop build`, `pnpm --filter @mcp-registry/desktop package:win` 통과 후 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 다시 생성했다.

## 2026-07-06 추가 151

- Custom Flow를 다시 직접 열어 노드 이동, 그룹 만들기/해제, 메모 생성/편집/우클릭 삭제, 노드 검색, 도구창 휠 동작, 실행 중 노드 강조, 뒤로/앞으로, 노드 위 휠버튼 화면 이동을 점검했다.
- 저장된 연결선이 빈 배열일 때 기본 연결선으로 되살아나는 문제를 발견했다. 모든 연결을 지운 상태가 사용자 저장 상태로 유지되도록 `WorkflowView` 초기화 로직을 수정했다.
- 위 문제 때문에 흐름 점검이 연결선 없는 상태를 통과로 표시하던 문제도 함께 해결했다. 연결선이 없으면 `입력 미연결`, `연결 없음` 경고와 노드 배지가 표시되는 것을 실제 브라우저에서 확인했다.
- 흐름 점검 메시지 파일의 깨진 한글 문구를 정상 한글로 교체하고, 빈 연결/빈 플로우를 검증하는 테스트를 추가했다.
- 사용성 검토 결과, 상단 도구막대가 너무 투명해 비활성 버튼처럼 보일 수 있고, 좁은 화면에서는 상단 탭 제목이 많이 잘리는 점을 후속 개선 후보로 남겼다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop build`, `pnpm --filter @mcp-registry/desktop package:win` 통과 후 최신 exe를 다시 생성했다.

## 2026-07-06 추가 152

- 실행 버튼을 눌렀을 때 흐름 문제가 있으면 실행 애니메이션을 돌리지 않고 `실행 중단` 이슈로 표시하도록 변경했다. 해당 이슈는 노드 경고/오류 아이콘과 상단 흐름 점검 패널에 함께 표시된다.
- 노드 경고/오류 아이콘의 기본 브라우저 title 툴팁을 제거하고 앱 내부 툴팁으로 바꿨다. hover 판정 영역은 실제 원형 아이콘 19px보다 큰 34px 영역으로 넓혀 마우스를 올렸을 때 설명이 안정적으로 뜨게 했다.
- 드래그 중 흰 화면 문제를 재현하기 위해 기본 상태, 그룹 상태, 실행 이슈/흐름 점검 패널이 열린 상태에서 반복 드래그를 수행했지만 개발 서버 화면에서는 재현되지 않았다. 대신 Custom Flow 전용 오류 보호 패널을 추가해 렌더링 오류가 발생해도 전체 흰 화면 대신 오류 메시지와 새로고침 버튼이 표시되도록 했다.
- 브라우저에서 연결선 없는 상태로 실행을 눌러 `실행 중단: 입력 미연결` 툴팁과 상단 흐름 점검 패널이 함께 표시되는 것을 확인했다. 25회 반복 드래그에서도 흰 화면, 콘솔 오류, 오류 보호 패널은 발생하지 않았다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop build` 통과. 기존 `AI_PROGRAM.exe` 4개가 release 폴더를 잠가 패키징이 한 번 실패했으나 종료 후 `pnpm --filter @mcp-registry/desktop package:win`을 통과해 최신 exe를 다시 생성했다.

## 2026-07-06 추가 153

- 사용자가 전달한 `Custom Flow 화면 오류 / Minified React error #185` 화면을 기준으로 실제 EXE를 디버그 포트로 실행해 확인했다. 최신 패키지에서는 동일 저장 그래프, 실행 클릭, 경고 아이콘 hover, 반복 드래그 상황에서 오류 패널이 재현되지 않았다.
- React #185가 상태 업데이트 반복 오류 계열이므로, 실행 이슈 배열이 같은 내용일 때는 `setFlowRunIssues`를 다시 호출하지 않도록 비교 함수를 추가했다. 연결/노드 변경으로 실행 이슈를 비울 때도 이미 빈 배열이면 상태 업데이트를 건너뛰도록 바꿨다.
- 오류 보호 패널이 React minified 코드만 보여주지 않도록 #185 메시지를 한국어 안내로 바꾸고, `화면 새로고침` 버튼이 과하게 커지지 않도록 스타일을 고정했다.
- 실제 EXE에서 사용자 저장 그래프 기준으로 실행 후 상단 흐름 점검 패널과 노드 경고 툴팁이 표시되는 것을 확인했고, 10회 반복 드래그에서도 오류 패널은 재현되지 않았다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop package:win` 통과 후 최신 exe를 다시 생성했다.

## 2026-07-06 추가 154

- 사용자가 `노드를 잡고 빠르게 움직이고 바로 놓으면 오류가 나고, 1초 기다렸다 놓으면 오류가 안 난다`고 알려준 조건을 기준으로 드래그 종료 로직을 다시 분석했다.
- 빠른 mouseup 시 마지막 그룹/프롬프트 부착 대상 상태를 읽기 위해 `setState` updater 안에서 다른 `setState`를 호출하던 구조를 제거했다. 마지막 drop/attach 대상은 ref에 저장하고, 드래그 종료 시 ref를 읽어 한 번만 후처리하도록 변경했다.
- `pointerup`과 `pointercancel`이 거의 동시에 들어와도 드래그 종료 후처리가 중복 실행되지 않도록 노드/그룹/메모 드래그 종료 함수에 idempotent guard를 추가했다.
- 개발 서버에서 한 번 크게 이동 후 즉시 mouseup 하는 빠른 드래그를 40회 반복했고 오류 패널/흰 화면/콘솔 오류가 없음을 확인했다. 최신 EXE에서도 같은 조건을 50회 반복해 오류가 없음을 확인했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop package:win` 통과 후 최신 exe를 다시 생성했다.

## 2026-07-06 추가 155

- `활성 파일`, `경로 지정`처럼 입력 포트가 없고 출력만 있는 기본 도구의 출력 텍스트가 왼쪽으로 붙지 않도록 output-only 포트 레이아웃을 오른쪽 정렬로 보정했다.
- `활성 파일` 노드에 감지된 활성 파일 목록을 표시하고, CAD/Revit/Excel/Tekla 파일을 복수 선택할 수 있게 했다. 선택한 프로그램이 하나면 노드 앞 아이콘이 해당 프로그램 아이콘으로 바뀌고, 없거나 여러 프로그램이면 `?` 사각형 아이콘으로 표시된다.
- `경로 지정` 노드에는 파일 선택 버튼을 추가해 파일 탐색기에서 고른 파일명을 노드 하단에 표시하도록 했다.
- 붙어 있는 `프롬프트` 노드는 상위 노드를 이동할 때 함께 움직이도록 노드/그룹 드래그 대상 계산에 포함했다.
- `Esc` 입력 시 흐름 점검에서 선택했던 노드/선 강조와 실행 이슈도 함께 해제되도록 보강했다. 입력창에 커서가 있는 상태에서 `Esc`를 눌러도 입력 종료와 메뉴/강조 해제가 같이 처리된다.
- 실제 브라우저에서 활성 파일 Excel 선택 시 아이콘 변경, output-only 오른쪽 정렬, 빠른 노드 드래그 20회 반복을 확인했다. 오류 패널/흰 화면/콘솔 오류는 재현되지 않았다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop package:win` 통과 후 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 다시 생성하고 실행했다.

## 2026-07-06 추가 156

- Custom Flow 상단 툴바가 좁은 화면에서 버튼을 다 표시하지 못할 때 가로 스크롤이 가능하도록 바꾸고, 좌우 끝에 작은 화살표 힌트를 표시했다.
- 기본도구 아이콘 배경을 회색 계열로 통일했다. `결과 미리보기`는 눈 모양, `경로 지정`은 폴더 모양, `활성 파일`은 물음표 사각형, `프롬프트`는 연결 상태 아이콘을 사용한다.
- `활성 파일`과 `경로 지정` 노드의 출력 포트 왼쪽 위치에 현재 선택/인식된 파일명을 요약 표시하도록 추가했다. 파일이 없으면 각각 `인식된 파일 없음`, `선택된 파일 없음`으로 표시된다.
- 기본도구 노드끼리 배경색이 달라 보이지 않도록 `basic-*` 노드는 공통 회색 배경 팔레트를 사용하게 했다.
- 실제 브라우저에서 활성 파일/경로 지정 노드의 왼쪽 파일명 표시, 기본도구 아이콘 클래스와 회색 배경, 툴바 overflow 설정을 확인했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop package:win` 통과 후 최신 exe를 다시 생성하고 실행했다.

## 2026-07-06 추가 157

- 상단 툴바 자체에 가로 스크롤을 넣었던 변경을 되돌렸다. 이 방식이 `노드 검색`, `도구`, 뒤로가기 메뉴 클릭을 방해할 수 있어 버튼 줄은 고정하고, 열린 팝업 내부만 스크롤되도록 수정했다.
- `노드 검색` 결과 영역과 뒤로가기 기록 메뉴에 최대 높이와 내부 스크롤을 추가해, 목록이 많아도 아래쪽이 잘리지 않게 했다.
- `경로 지정` 폴더 아이콘과 기본도구 노드 아이콘은 회색 배경으로 보이도록 노드 아이콘 스타일을 보강했다.
- `활성 파일`, `경로 지정` 노드에 추가한 파일명 요약 박스의 세로 위치를 출력 포트 행과 맞추도록 조정했다.
- `Excel 내보내기`를 Custom Flow 기본도구에 추가하고, 왼쪽 Excel 메뉴에도 `Excel 내보내기`, `표 읽기`, `셀 값 업데이트`가 나오도록 카탈로그를 분리했다. 기존에는 Excel/Tekla/Custom Flow 메뉴가 CAD 카탈로그를 재사용하는 구조였다.
- 노드 펼침 영역을 `작동 원리` / `설정` 탭으로 바꿨다. 설정 탭에서는 노드 이름, 설명, 작업 대상, 실행 옵션, 메모를 수정할 수 있고 저장 그래프에 같이 보존된다.
- 검증: 샌드박스에서 `pnpm typecheck`와 `vitest`는 프로세스 실행 권한 문제로 막혔지만, `node node_modules/typescript/bin/tsc`를 패키지별로 직접 실행한 타입 검사는 통과했다. 현재 브라우저 직접 확인과 EXE 재패키징은 권한/사용량 제한 때문에 완료하지 못했다.

## 2026-07-07 추가 158

- 전날 권한/사용량 제한 때문에 미완료였던 정식 검증과 패키징을 이어서 완료했다.
- `pnpm typecheck`가 shared/core/desktop 모두 통과했다.
- `pnpm test`가 19개 테스트 파일, 69개 테스트 기준으로 통과했다.
- `pnpm package:win`으로 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 다시 생성했고, 생성 후 앱을 실행했다.

## 2026-07-07 추가 159

- `mcp-tool-builder` 스킬을 설정 schema 중심으로 재작성했다. 커스텀 툴 MD 파일을 만들 때 `settings`, `inputs`, `outputs`, 위험도, 실패 처리, 결정적 실행 조건을 빠뜨리지 않도록 질문 흐름을 강화했다.
- AI Revit / AI CAD 애드인을 나중에 MD 툴로 변환할 때 필요한 후보 설정값을 스킬에 추가했다. Revit은 카테고리, 패밀리/타입, 레벨, 뷰, 작업세트, 파라미터, 트랜잭션, 결과 로그를 확인하게 했고, CAD는 객체 타입, 레이어, 블록명, 좌표 기준, 허용 오차, 결과 레이어, 내보내기 경로를 확인하게 했다.
- `save-tool` 스킬도 `/save 툴이름` 실행 시 단순 대화 요약으로 바로 저장하지 않고, 설정창에 들어갈 값이나 결정적 실행에 필요한 정보가 부족하면 먼저 질문하도록 바꿨다.
- 두 스킬 모두 사용자가 질문 일부만 답하면 남은 질문을 임의로 넘기지 않고, 이유와 함께 2-4개 선택지와 추천안을 제시하도록 규칙을 추가했다.
- `/save` 초안 생성 스크립트가 frontmatter에 `tool: true`, `deterministic: true`, `settings`, `inputs`, `outputs` 기본값을 넣도록 수정했다.
- 앱에 포함되는 스킬(`packages/desktop/assets/skills`)과 실제 Codex 로컬 스킬(`C:\Users\Donggeon\.codex\skills`)을 같은 내용으로 동기화했다.
- 검증: TypeScript 직접 검사 통과, 번들 Python으로 `create_tool_draft.py` 실행 확인.

## 2026-07-07 추가 160

- MCP TOOL 만들기 스킬의 호출 방식을 `/make <툴이름>` 기준으로 바꿨다. `/make`만 입력하면 대화 내용을 보고 툴 이름 후보를 먼저 제안하고, 정보가 부족하면 선택지를 제시해 물어보도록 규칙을 추가했다.
- 앱 홈 화면의 `MCP TOOL 만들기` 항목과 설치 완료 안내 문구도 `/make MCP TOOL 만들기`, `/make 툴이름` 기준으로 변경했다.
- 앱에 포함되는 `mcp-tool-builder` 스킬 메타데이터와 기본 프롬프트를 `/make` 기준으로 갱신했다.
- 실제 Codex 로컬 스킬(`C:\Users\Donggeon\.codex\skills\mcp-tool-builder`)에도 같은 내용을 동기화했고, 최신 EXE 패키지 내부 리소스에도 `/make` 문구가 포함된 것을 확인했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과.

## 2026-07-07 추가 161

- `save-tool`도 `/make`와 같은 방식으로 바꿨다. `/save <툴이름>`이면 해당 이름으로 저장하고, `/save`만 입력하면 현재 대화 내용을 분석해 2-4개 툴 이름 후보와 추천안을 먼저 제안하도록 규칙을 추가했다.
- 앱 홈 화면의 `채팅을 TOOL로 저장` 설명과 `/SAVE` 설치 완료 안내 문구를 `/save` 또는 `/save 툴이름` 기준으로 정리했다.
- 앱에 포함되는 `save-tool` 메타데이터와 실제 Codex 로컬 스킬(`C:\Users\Donggeon\.codex\skills\save-tool`)을 같은 내용으로 동기화했다.
- 최신 EXE 패키지 내부 리소스에도 `/save` 변경이 포함된 것을 확인했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과.

## 2026-07-07 추가 162

- `mcp-tool-builder` 스킬을 실행 가능한 MCP 툴 명세에 가깝게 보강했다. `/make`로 툴을 만들 때 MCP 서버, 명령 이름, 파라미터 매핑, 실행 순서, 서버 미연결 대응을 질문하도록 추가했다.
- MD 툴 참조 문서에 `executionMode`, `requiredServers`, `mcpCommands`, `preflightChecks`, `resultSchema`, `failurePolicy`, `settingsLayout`, `testCases`를 추가했다.
- 설정창 UI schema를 확장했다. 기본형, 기본/고급 분리, 단계별 설정, 반복 테이블 설정을 고를 수 있게 하고, 설정 필드마다 `section`, `advanced`, `preview`, `confirmOnChange` 같은 UI 힌트를 넣을 수 있게 했다.
- 위험도를 읽기 전용, 파일 생성, 원본 수정, 대량 수정, 삭제/덮어쓰기 단계로 세분화했다.
- `save-tool`도 같은 기준을 따르도록 수정했고, 초안 생성 스크립트가 새 frontmatter 기본 항목을 생성하도록 변경했다.
- 앱에 포함되는 스킬과 실제 Codex 로컬 스킬(`C:\Users\Donggeon\.codex\skills`)을 같은 내용으로 동기화했다.
- 최신 EXE 패키지 내부 리소스에도 보강한 스킬 항목이 포함된 것을 확인했다.
- 검증 완료: 초안 생성 스크립트 실행 확인, `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과.

## 2026-07-07 추가 163

- MCP 툴을 한 번도 만들지 않았고 AI 사용에도 익숙하지 않은 사용자를 가정해 `/make`, `/save` 시나리오를 점검했다.
- 테스트에서 발견한 첫 번째 부족점은 기술 질문이 너무 빨리 나올 수 있다는 점이었다. `mcp-tool-builder`와 `save-tool`에 초보 사용자 모드를 추가해 MCP 명령 이름, 포트, schema 같은 용어를 직접 묻지 않고, 실제 업무 기준 질문으로 받은 뒤 AI가 기술 명세 후보를 제안하도록 바꿨다.
- 두 번째 부족점은 새 MD frontmatter의 `risk: modify/delete/bulk-modify` 같은 값이 앱 위험 감지에 직접 반영되지 않을 수 있다는 점이었다. `toolMarkdown` helper를 추가하고 Electron 메인 프로세스가 이를 사용하도록 바꿔, `tool: true`, `executionMode`, `mcpCommands`, `settings`, `inputs`, `outputs`를 툴 인식 신호로 보고 새 위험도도 경고로 감지하게 했다.
- 세 번째 부족점은 참조 문서가 `name/program` 예시를 쓰고, 앱과 `/save`는 `toolName/sectionId`를 우선 사용해 기준이 섞일 수 있다는 점이었다. MD 참조 문서를 앱 기준인 `toolName`, `sectionId` 중심으로 통일하고 `name`, `program`은 호환/가독성 용도로만 설명했다.
- 새 위험 감지 단위 테스트를 추가했다. 실행 가능한 MCP metadata가 있는 MD를 툴로 인식하는지, `risk: delete`, `risk: bulk-modify`, `mcpCommands` 기반 수정 위험을 감지하는지 확인한다.
- 검증 완료: 초안 생성 스크립트가 `risk: "read"`와 실행 명세 필드를 생성하는 것 확인, `pnpm typecheck` 통과, `pnpm test` 20개 파일/73개 테스트 통과.
- `pnpm package:win`으로 최신 EXE를 다시 생성했고, 패키지 내부 스킬 리소스와 번들 코드에 초보 사용자 모드 및 새 위험 감지 로직이 포함된 것을 확인했다.

## 2026-07-07 추가 164

- MD 툴 frontmatter의 실행/설정 schema를 앱에서 읽을 수 있도록 `toolSettingsSchema` 파서를 추가했다. `settings`, `inputs`, `outputs`, `requiredServers`, `mcpCommands.params`, `preflightChecks`, `resultSchema`, `failurePolicy`, `visibleWhen`을 읽는다.
- Custom Tool 또는 More Tools에서 불러온 MD 툴의 schema가 Custom Flow 노드로 전달되도록 연결했다. 노드의 `설정` 탭에서는 schema에 따라 기본 설정, 고급 설정, 실행 전 요약, 실행 전 점검을 자동으로 표시한다.
- CAD 객체 읽기, Excel 내보내기, Revit 배치 기본 노드에 1차 설정 schema를 넣었다. 선택 범위, 객체 타입, 저장 경로, 기존 파일 처리, 패밀리/레벨, 파라미터 매핑, 실패 처리 같은 설정을 노드 안에서 편집할 수 있다.
- 설정창 UI에서 선택지가 없는 `layer`, `level`, `family-type`, `parameter` 같은 필드는 빈 드롭다운 대신 텍스트 입력으로 보이게 했다. 고급 설정 섹션은 접고 펼칠 수 있게 했다.
- `/make`와 `/save` 스킬에 설정창 목업 확인 단계를 추가했다. 설정창 구성이 어느 정도 잡히면 AI가 한글 목업을 보여주고, 그대로 진행/항목 추가/항목 제거/순서 변경 중 선택을 받은 뒤 MD 파일을 만들도록 했다.
- MD 툴 참조 문서에 `scope-picker`, `unit`, `tolerance`, `mapping-table`, `filter-builder`, `sort-rule`, `overwrite-policy`, `conflict-policy`, `backup-policy`, `transaction-policy`, `version-compatibility` 같은 설정 타입을 추가했다.
- 앱에 포함되는 스킬과 실제 Codex 로컬 스킬(`C:\Users\Donggeon\.codex\skills`)을 같은 내용으로 동기화했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-07 추가 165

- Custom Tool 등록창에 `설정창 미리보기` 패널을 추가했다. MD 파일의 schema가 앱에서 어떤 설정창으로 보일지 섹션, 필드, 입력/출력 포트, 위험도, 필요 MCP 서버 기준으로 확인할 수 있다.
- `toolRuntimeValidation.ts`를 추가해 schema 정의 오류, 필수 설정값 누락, MCP 실행 계획 생성을 분리했다. `mcpCommands.params`의 `settings.xxx` 참조를 현재 설정값 또는 기본값으로 풀어 실행 계획을 만든다.
- Custom Tool 저장 시 MCP 명령의 서버/명령 이름 같은 schema 오류가 있으면 저장 전에 막도록 했다. 경고는 미리보기 패널에서 보여주되 저장은 가능하다.
- Custom Flow 흐름 점검에 노드별 필수 설정값 누락 검사를 연결했다. 노드 설정 탭에서 저장 경로, 레벨, 패밀리 타입 같은 필수값이 비어 있으면 흐름 점검과 노드 배지에 오류로 표시된다.
- Custom Flow 노드 설정 탭에 `설정 검증`과 `테스트 실행 요약`을 추가했다. 노드가 실제 MCP 호출로 넘어가기 전에 필요한 서버, preflight, 설정 요약, 예정 MCP 명령과 파라미터를 볼 수 있다.
- CAD 객체 읽기, Excel 내보내기, Revit 배치 기본 노드에 `planned` MCP 명령 계획을 추가했다. 실제 실행 엔진 연결 시 `buildToolExecutionPlan(schema, settingsValues).commands`를 호출 큐로 넘기면 된다.
- 설정 필드 UI를 보강했다. `mapping-table`, `filter-builder`, `sort-rule`, `repeatable-list`는 textarea 대신 행 추가/삭제 방식의 작은 표 UI로 편집한다.
- 기본 Excel 내보내기 노드의 저장 경로는 앱 첫 실행부터 오류가 뜨지 않도록 `ask_on_run` 기본값으로 바꿨다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과. 테스트는 22개 파일, 82개 테스트 기준으로 통과했다.

## 2026-07-07 추가 166

- Custom Flow 실행 모델을 `customFlowRunModel.ts`로 분리했다. 실행 범위(`전체`, `선택 노드만`, `선택 노드까지`, `선택 노드부터`), 노드별 중간결과 미리보기, 실행 전 영향 범위, 그룹을 재사용 툴 schema로 바꾸는 로직을 담당한다.
- 실행 설정 메뉴를 확장해 `일괄 실행`/`단계별 실행`에 더해 실행 범위를 고를 수 있게 했다. 단계별 실행은 선택 범위 안에서 한 단계만 실행하는 방식으로 동작한다.
- 실행 후 오른쪽에 실행 로그 타임라인 패널을 표시한다. 각 단계는 대기/실행 중/완료 상태를 보여주고, 상단에는 이번 실행의 영향 범위와 필요한 MCP 서버/예정 명령을 요약한다.
- 노드 상세 탭에 `결과` 탭을 추가했다. 실행 로그 항목을 클릭하면 해당 노드로 이동하고 결과 탭이 열려 예상 중간결과와 영향 범위를 확인할 수 있다.
- 그룹 헤더에 `툴 저장` 버튼을 추가했다. 그룹 내부 노드의 입력/출력/필요 MCP/명령 계획을 읽어 Custom Flow 기반 커스텀 툴로 저장한다.
- 실행 로그/흐름 점검 패널 클릭이 캔버스 빈 곳 클릭으로 전파되어 선택이 해제되는 문제를 막았다.
- 검증 완료: `customFlowRunModel.test.ts` 3개 테스트 통과, 전체 `vitest run` 23개 파일/85개 테스트 통과, shared/core/desktop 직접 TypeScript 검사 통과, `pnpm --filter @mcp-registry/desktop build` 통과. 브라우저에서 Custom Flow 실행 로그 5/5 완료와 로그 클릭 후 결과 탭 표시를 확인했다.

## 2026-07-07 추가 167

- 전체 리뷰에서 나온 우선순위 항목을 반영했다.
- Custom Flow 실행 범위 계산을 노드 배열 순서가 아니라 연결선 그래프 기준 순서로 바꿨다. `전체`, `선택 노드까지`, `선택 노드부터`는 연결된 앞/뒤 노드를 따라 계산하고, 단계별 실행은 해당 범위의 첫 단계만 실행한다.
- 그룹을 커스텀 플로우 툴로 저장할 때도 그룹 내부 노드를 연결선 기준 순서로 정렬해 입력/출력 fallback과 MCP 명령 순서가 화면 흐름과 맞도록 했다.
- Custom Tools의 로컬 MD 파일 읽기/복사/비교/GitHub 업로드 IPC에 허용 경로 검사를 추가했다. 사용자가 선택한 툴 폴더는 Electron 사용자 데이터 폴더의 `custom-tool-roots.json`에 저장되고, 그 폴더 밖의 파일은 처리하지 않는다.
- GitHub access token은 `safeStorage`로 암호화할 수 없으면 저장하지 않도록 바꿨다. 기존 평문 토큰 파일은 암호화 가능하면 자동 재저장하고, 불가능하면 토큰을 제거한다.
- Electron `BrowserWindow` 보안 옵션을 명시했다. `contextIsolation: true`, `nodeIntegration: false`, `sandbox: true`, `webSecurity: true` 기준이다.
- `outputs/` 폴더를 `.gitignore`에 추가해 100MB 이상 산출물과 채팅 export가 실수로 커밋되지 않게 했다.
- `README_HANDOFF.md`의 현재 안내 문구를 `/save`와 현재 사용자 홈 기준 경로로 정리했다.

## 2026-07-07 추가 168

- Custom Flow의 `활성 파일` 노드가 선택한 프로그램을 출력 포트 타입으로 사용하도록 수정했다. CAD만 선택하면 출력 포트가 `cad` 타입이 되어 `CAD 객체 읽기`의 CAD 입력과 포트 타입 불일치 경고가 나오지 않는다.
- 저장된 `basic-active-file` 노드도 복원 시 선택된 프로그램 기준 출력 포트로 정규화되도록 했다.
- `활성 파일` 노드의 첫 상세 탭 표시를 `작동 원리`에서 `프로그램`으로 바꿨다.
- `활성 파일` 노드에 `새로고침` 버튼을 추가했다. Electron main process가 등록된 CAD/Revit MCP 서버의 `/active-file`, `/active_file`, `/current-file`, `/current_file`, `/status` endpoint를 시도하고, 응답에서 현재 파일명을 추출해 노드 선택 목록에 반영한다.
- 아직 저장소 안에는 실제 CAD/Revit 브리지 endpoint 구현이 없으므로, 실제 파일명 표시가 되려면 브리지 쪽에서 위 endpoint 중 하나로 현재 파일 정보를 반환해야 한다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 25개 파일/93개 테스트 통과, `pnpm --filter @mcp-registry/desktop build` 통과. 브라우저에서 `도구`/`노드 검색` 상호 닫힘과 노드 `설정` 탭의 이름/설명 입력칸 제거를 확인했다.

## 2026-07-07 추가 169

- Custom Flow 오른쪽 위 도구막대에서 `도구` 버튼을 열 때 `노드 검색`, 실행 설정, 뒤로가기 목록이 같이 닫히도록 수정했다. 반대로 `노드 검색`을 열 때도 기존처럼 다른 메뉴가 닫힌다.
- Custom Flow 노드 상세의 `설정` 탭에서 `노드 이름`, `설명` 직접 수정 칸을 제거했다. 설정 탭은 MCP 툴 실행에 필요한 설정값만 보여주는 방향으로 정리했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 25개 파일/93개 테스트 통과, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 170

- Custom Flow를 제외한 `Share Tools`, `Custom Tools`, 개별 커스텀 툴 페이지의 상단 제목/설명은 더 이상 클릭해서 수정할 수 없도록 했다.
- 편집 가능 여부 규칙을 `submenuEditPolicy.ts`로 분리하고 테스트를 추가했다. 일반 사용자 생성 소메뉴는 계속 제목/설명 편집이 가능하다.
- 검증 완료: `submenuEditPolicy.test.ts` 3개 테스트 통과, `pnpm typecheck`, `pnpm test` 26개 파일/96개 테스트 통과, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 171

- 우측 상단 MCP 상태 배지를 누르면 등록된 HTTP/SSE MCP 서버 URL, `/status`, `/active-file` endpoint를 실제로 요청해 화면 상태를 갱신하도록 했다. stdio 서버는 앱이 실행한 프로세스 상태를 기준으로 표시한다.
- Custom Flow 노드의 `설정` 탭에 `저장한 설정` 영역을 추가했다. 현재 설정값을 프리셋으로 저장하고, 같은 툴 ID를 쓰는 다른 노드에서도 불러오거나 삭제할 수 있다.
- Custom Flow 기본 진입 화면을 캔버스에서 메인 페이지로 바꿨다. 왼쪽은 샘플 공유 플로우, 오른쪽은 이 컴퓨터에 저장한 내 플로우 목록이며, `새 Custom Flow 만들기`를 누르면 기존 캔버스 편집 화면으로 들어간다.
- 편집 화면 도구막대에 `메인`, `저장` 버튼을 추가했다. 저장한 플로우는 localStorage `mcp-registry:saved-custom-flows`에 저장된다. 공유 플로우의 실제 GitHub/서버 연결은 후속 단계로 남겼다.
- `settingPresetModel.ts`, `customFlowLibrary.ts`를 추가해 프리셋과 저장 플로우 로직을 UI와 분리했고 테스트를 추가했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 28개 파일/102개 테스트 통과, `pnpm --filter @mcp-registry/desktop build` 통과. 브라우저에서 Custom Flow 메인 페이지, 새 플로우 진입, 설정 프리셋 영역, MCP 상태 점검 중 표시를 확인했다.
- 사용자가 실제 화면이 그대로라고 알려준 뒤 확인한 결과, 실행 중인 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`가 오전 10:29 이전 빌드라 최신 코드가 반영되지 않은 상태였다. 실행 중인 기존 EXE 프로세스를 종료하고 `pnpm package:win`을 다시 실행해 오후 1:58 기준 최신 EXE를 생성한 뒤 실행했다.

## 2026-07-07 추가 172

- Custom Flow 캔버스에서 `Ctrl+S` 또는 `Meta+S`를 누르면 브라우저/앱 기본 저장창을 열지 않고 현재 캔버스 그래프를 즉시 저장하도록 했다.
- 저장이 끝나면 기존 앱 하단 알림 시스템에 `저장되었습니다` 알림이 뜨도록 연결했다.
- 단축키 판정을 `customFlowShortcut.ts`로 분리하고 테스트를 추가했다.
- 검증 완료: `customFlowShortcut.test.ts` 2개 테스트 통과, `pnpm typecheck`, `pnpm test` 29개 파일/104개 테스트 통과.

## 2026-07-07 추가 173

- Custom Flow 노드 펼침/접힘 화살표 크기를 키워 클릭 위치를 더 명확하게 보이도록 했다.
- Custom Flow 메인페이지에서는 `Ctrl+S` 저장과 자동저장이 작동하지 않도록 제한하고, 캔버스 편집 화면에서만 저장되게 했다.
- `Ctrl+S` 저장 완료 알림은 일반 업데이트/설치 알림과 분리해 작은 `저장되었습니다` 토스트로 표시하고 약 2.2초 후 자동으로 사라지게 했다.
- 왼쪽 메뉴의 `+` 소메뉴는 Custom Flow에만 남겼다. Custom Flow의 `+`는 커스텀툴 추가가 아니라 새 캔버스를 바로 여는 동작으로 바꿨다.
- Custom Flow 소메뉴는 `공유 플로우`, `내 플로우`, `+` 구성으로 정리했다. CAD/Revit/Excel/Tekla 메뉴의 기본 `+`는 제거했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 30개 파일/110개 테스트 통과, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 174

- Custom Flow 왼쪽 메뉴를 `공유 플로우`, `내 플로우` 분류 항목이 아니라 실제 플로우 항목 목록으로 바꿨다.
- 공유 플로우 샘플과 이 컴퓨터에 저장된 내 플로우를 왼쪽 메뉴에 함께 표시하고, 내 플로우는 최신 수정 순서로 정렬한다.
- 왼쪽 메뉴에서 플로우 이름을 누르면 해당 플로우 그래프가 바로 Custom Flow 캔버스 편집 화면으로 열린다.
- 저장/가져오기/복제/삭제/이름 변경으로 `savedFlows`가 바뀌면 왼쪽 메뉴 목록도 같이 갱신되도록 상위 App 상태와 동기화했다.
- `customFlowLibrary`에 workflow 메뉴용 flow id 생성/파싱/목록화 함수를 추가하고 테스트를 보강했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 30개 파일/112개 테스트 통과, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 175

- Custom Flow 메인페이지 상단의 `새 Custom Flow 만들기` 버튼을 제거하고, `내 플로우` 패널 헤더 오른쪽의 작은 `+` 버튼으로 옮겼다.
- 메인페이지의 `내 플로우` 카드에서 플로우 이름과 설명을 직접 수정할 수 있게 했다. 수정 내용은 저장 목록과 왼쪽 Custom Flow 메뉴에 함께 반영된다.
- 왼쪽 메뉴에서 개별 플로우를 연 뒤 Custom Flow 메인 메뉴를 선택해도 내부 편집 모드가 메인페이지로 전환되도록 `homeRequestId` 흐름을 추가했다.
- 개별 플로우 메뉴 항목이 일반 소메뉴 페이지로 렌더링되어 Custom Flow 캔버스가 숨겨질 수 있던 원인을 수정했다. workflow flow submenu는 `WorkflowView`가 계속 렌더링되게 처리한다.
- 저장된 개인 플로우를 편집 중일 때 그래프가 저장본과 달라지면 메인페이지 이동 시 저장되지 않은 변경 경고를 띄운다. `Ctrl+S` 또는 편집 화면의 `저장` 버튼을 누르면 현재 개인 플로우 그래프에 덮어 저장한다.
- `customFlowLibrary`에 저장 플로우 이름/설명 수정, 그래프 덮어쓰기, dirty 감지 함수를 추가하고 테스트를 보강했다.
- 검증 완료: `customFlowLibrary.test.ts` 7개 테스트 통과, `pnpm typecheck`, `pnpm test` 30개 파일/114개 테스트 통과, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 176

- Custom Flow 왼쪽 메뉴의 `+`를 눌렀을 때 임시 캔버스만 열리고 저장 목록/메뉴에는 바로 추가되지 않던 흐름을 수정했다.
- 이제 `+`를 누르면 즉시 `새 Custom Flow` 저장 항목을 만들고, 왼쪽 메뉴에 바로 추가한 뒤 해당 플로우 캔버스로 이동한다.
- 다른 메뉴 화면에서 Custom Flow `+`를 눌러도 먼저 Custom Flow 화면으로 전환한 다음 새 플로우를 생성하도록 해서 화면 상태가 꼬이지 않게 했다.
- Custom Flow 메인페이지의 `내 플로우 +` 버튼도 클릭 이벤트가 생성 source 값으로 잘못 들어가지 않도록 명시적으로 `menu` source를 넘기게 수정했다.
- 검증 완료: `pnpm test` 30개 파일/114개 테스트 통과, `pnpm typecheck` 통과, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 177

- Home 메인페이지의 Other Tools 카드에서 `MCP 툴 만들기` 제목 앞에 붙어 있던 `/make` 표기를 제거했다. 사용 방법 설명에는 `/make 툴이름` 명령 안내를 유지했다.
- CAD/Revit/Excel/Tekla 같은 프로그램 메뉴의 왼쪽 소메뉴에서 `Share Tools`, `Custom Tools` 분류 항목이 기본으로 뜨지 않도록 했다. 이제 프로그램 메뉴에는 실제 공유 툴과 등록된 커스텀 툴 항목만 표시된다.
- Custom Flow 메뉴의 `+` 새 플로우 항목은 그대로 유지했다.
- 검증 완료: `pnpm typecheck`, `pnpm test -- navigationModel`, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 178

- Home 메인페이지 Other Tools의 `MCP 툴 만들기` 설명에서 `툴이름` 문구를 제거했다. 설명은 `/make 명령하면 질문을 따라 설정, 입출력 포트가 있는 TOOL md 파일을 만듭니다.`로 정리했다.
- Custom Flow 메뉴의 `+` 동작을 다시 점검했다. 이전 수정에서 `+` 클릭 시 즉시 저장 플로우를 만들고 저장 플로우 메뉴로 이동하던 것이 화면 꼬임의 원인이었다.
- `+`는 이제 저장 목록을 만들지 않고 빈 캔버스만 연다. 사용자가 `저장` 또는 `Ctrl+S`로 저장할 때 내 플로우 목록과 왼쪽 메뉴에 추가된다.
- 한 번 소비한 새 캔버스 생성 요청은 상위 상태에서 비워, 화면 전환이나 재렌더링 때 같은 요청이 반복 실행되지 않게 했다.
- 검증 완료: `pnpm typecheck`, `pnpm test -- customFlowLibrary navigationModel` 통과.

## 2026-07-07 추가 179

- 툴 상세 페이지 레이아웃을 사진 기준으로 재배치했다. `작동 원리`는 상단 전체 폭의 가로 패널로 두고, `설정`은 그 아래 전체 폭의 넓은 패널로 배치했다.
- 커스텀 툴 MD에서 읽은 `settings` schema가 있으면 하단 `설정` 패널에 실제 설정 입력 UI가 표시되도록 연결했다. schema가 없는 기본 툴은 기존 작업 대상/실행 옵션/메모 기본 폼을 계속 보여준다.
- 설정 schema가 많은 커스텀 툴도 하단 패널 안에서 스크롤되도록 하고, 일반 화면에서는 설정 필드를 2열로, 간소화/좁은 화면에서는 1열로 표시되게 했다.
- `/make`, `/save` 스킬 문서에 새 툴 페이지 기준을 반영했다. 앞으로 설정창 목업은 상단 `작동 원리`, 하단 `설정` 패널 구조를 기준으로 만들도록 했다.
- 앱에 포함된 스킬과 실제 Codex 로컬 스킬(`C:\Users\Donggeon\.codex\skills`)을 같은 내용으로 동기화했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 30개 파일/114개 테스트 통과, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 180

- Custom Flow 소메뉴 클릭 시 메인페이지가 뜨거나 더블클릭 후 `새 페이지` fallback이 보이던 원인을 수정했다.
- 원인은 저장 플로우 소메뉴 ID가 `saved-flow:<flow-id>`처럼 내부에 `:`를 포함하는데, 탭/소메뉴 키 파서가 `workflow:saved-flow:<flow-id>`를 단순 `split(":")`로 잘라 실제 flow id를 버리던 문제였다.
- 소메뉴 키 파서를 첫 번째 `:`만 기준으로 나누도록 바꿔 `workflow`와 `saved-flow:<flow-id>`가 정상 분리되게 했다.
- Custom Flow 메뉴의 `+`는 사용자가 의도한 대로 `내 플로우`에 `새 페이지` 저장 항목을 즉시 만들고, 해당 캔버스를 바로 열도록 다시 맞췄다.
- 검증 완료: `pnpm typecheck`, `pnpm test -- customFlowLibrary navigationModel`, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 178

- `CAD 도면번호 일괄 순번 변경` MCP 툴 설계를 시작했다.
- CAD DWG 파일을 여러 개 직접 선택하고, 불러온 목록에서 파일 추가/선택 삭제/위로 이동/아래로 이동으로 처리 순서를 조정하는 방향으로 정했다.
- 첫 DWG에서 도곽 후보 블록을 표시해 사용자가 도곽을 선택하고, 기준 검색 문자(`P-` 등)를 포함한 Text/MText 위치를 도곽 기준 상대 위치로 인식하는 흐름으로 정했다.
- 새 도면번호는 접두어, 시작번호, 직접 지정 자리수, 증가값을 기준으로 좌상단에서 우하단 방향으로 순번을 부여하는 방식으로 정했다.
- 설정창 검토용 HTML 목업을 `outputs/cad-drawing-number-batch-renumber-mockup.html`에 추가했다.

## 2026-07-07 추가 179

- `/make`용 `mcp-tool-builder` 스킬과 `/save`용 `save-tool` 스킬의 절차를 업데이트했다.
- 두 스킬 모두 MD 파일을 작성하기 전에 전체 도구 사양을 먼저 추출해 사용자 확인을 받도록 했다.
- 전체 사양 승인 후 실제 HTML 설정창 목업을 `outputs/<safe-tool-name>-settings-mockup.html` 형태로 만들고, 그 HTML 화면을 확인받은 뒤에만 최종 MD 작성/저장을 진행하도록 했다.
- HTML 목업에는 실제 AI Program 설정창 형태, 필드, 버튼, 경고, 미리보기/결과 영역, 목록 추가/삭제/정렬 컨트롤, 위험 작업 최종 확인 영역을 포함하도록 명시했다.
- 앱 내장 스킬(`packages/desktop/assets/skills/...`)과 개인 설치 스킬(`C:\Users\Donggeon\.codex\skills\...`)을 같은 내용으로 동기화했다.

## 2026-07-07 추가 180

- 다른 채팅에서 수정된 `mcp-tool-builder` 스킬 지침을 다시 확인했다.
- 새 지침의 HTML 목업 요구사항에 맞춰 `작동 원리`를 상단 전체 폭 패널로, `설정`을 하단의 큰 전체 폭 패널로 배치하는 구조를 반영했다.
- 기존 오른쪽 설정 사이드바형 목업 대신 AI Program 툴 페이지 구조에 가까운 새 목업을 `outputs/cad-drawing-number-batch-renumber-settings-mockup.html`에 추가했다.
- 새 목업에는 DWG 직접 선택/추가/삭제/순서 조정, 도곽 후보 선택, 기준 문자 기반 Text/MText 위치 인식, 새 도면번호 규칙, 변경 전/후 미리보기, 원본 DWG 직접 수정 확인 영역을 포함했다.

## 2026-07-07 추가 181

- 사용자가 HTML 설정창 목업을 승인해 `CAD 도면번호 일괄 순번 변경` 툴 MD 초안을 작성했다.
- 초안 파일은 `tool-drafts/cad-drawing-number-batch-renumber.md`에 저장했다.
- frontmatter에는 `risk: bulk-modify`, `requiredServers: cad`, planned CAD MCP 명령 계획, preflight checks, result schema, failure policy, settings layout, settings schema, inputs/outputs, test cases를 포함했다.
- 본문에는 목적, 작동 원리, 설정 설명, MCP 명령 계획, 실행 조건, 주의사항, 예상 결과, 실패 처리, 테스트 예시를 정리했다.
- planned MCP 명령은 아직 실제 CAD 브리지 구현/확인이 필요하다는 주석을 남겼다.

## 2026-07-07 추가 182

- `/make`와 `/save` 스킬에 승인된 한글 툴 이름을 frontmatter `toolName`과 첫 Markdown 제목에 그대로 넣도록 명시했다.
- 영어 파일명, slug, command id는 저장 파일명/id/MCP 명령명에만 쓰고 AI Program 표시명에는 쓰지 않도록 규칙을 추가했다.
- 사용자가 설명을 따로 주지 않아도 대상 프로그램, 입력/범위, 핵심 동작, 결과를 바탕으로 한글 한 문장 `description`을 자동 생성하도록 스킬 지침을 보강했다.
- `mcp-tool-builder` reference 문서에도 `toolName` 한글 표시명 유지와 `description` 필수 생성 규칙을 추가했다.
- `save-tool/scripts/create_tool_draft.py`가 `--description`이 비어 있을 때 본문 첫 설명 문장에서 자동 설명을 만들도록 수정했고, UTF-8 BOM이 있는 본문에서도 제목이 설명으로 들어가지 않게 처리했다.
- AI Program 단일 MD 파일 선택 핸들러가 파일명 대신 frontmatter `toolName`을 우선 표시명으로 읽고, `description`도 등록 폼에 자동 입력되도록 `main.ts`, `types.ts`, `App.tsx`를 수정했다.
- 기존 `tool-drafts/cad-drawing-number-batch-renumber.md`에는 호환용 `name: "CAD 도면번호 일괄 순번 변경"`을 추가했다.
- 검증 완료: `pnpm test -- toolMarkdown` 30개 파일/114개 테스트 통과, `pnpm typecheck` 통과, `pnpm --filter @mcp-registry/desktop build` 통과, `create_tool_draft.py` 임시 실행에서 한글 `toolName`과 자동 생성 `description` 저장 확인, `git diff --check` 공백 오류 없음.

## 2026-07-07 추가 182

- Custom Flow의 `내 플로우` 페이지에서 상단 제목과 설명을 직접 수정할 수 있게 했다.
- 원인은 저장 플로우 캔버스 페이지가 일반 소메뉴 렌더링을 막기 위해 `displaySubmenuItem`을 숨기면서, 기존 상단 `EditableText` 편집 UI도 같이 숨겨지던 구조였다.
- 저장 플로우(`saved-flow`)일 때만 별도의 상단 편집 분기를 열고, 제목/설명 변경 요청을 `WorkflowView` 내부 저장 플로우 상태와 App의 왼쪽 메뉴 표시 상태에 함께 반영하도록 연결했다.
- 공유 플로우는 기존처럼 읽기 전용으로 유지하고, 내 플로우만 편집 가능하게 했다.
- 검증 완료: `pnpm typecheck`, `pnpm test -- customFlowLibrary submenuEditPolicy`, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 183

- 로그인하지 않은 상태에서는 프로그램 전체를 사용할 수 없도록 계정 접근 조건을 강화했다. 이제 GitHub 로그인 전에는 라이선스/승인 여부와 관계없이 `로그인이 필요합니다` 화면이 먼저 표시된다.
- 로그인 후에는 기존처럼 관리자 승인, 라이선스 정책을 이어서 확인한다.
- 툴 등록 폼의 `제작자`는 로그인한 계정의 닉네임으로 자동 입력되고 읽기 전용으로 표시되게 했다. 신규 등록과 버전 업데이트 저장 시에도 사용자가 입력한 값이 아니라 현재 로그인 닉네임을 강제로 사용한다.
- 툴 등록 폼이 열린 상태에서 닉네임이 변경되면 제작자 표시도 자동으로 갱신되게 했다.
- 검증 완료: `pnpm typecheck`, `pnpm test -- githubAuthProfile toolSharingPolicy`, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 184

- 커스텀 툴 등록 정책에서 `자유 등록`이면 위험 경고가 있어도 앱 승인/PR 검토를 강제로 요구하지 않도록 수정했다. 위험 경고는 카드의 `주의` 표시로만 남긴다.
- Custom Tools 카드 UI를 정리했다. `주의` 배지는 툴 이름 왼쪽에 표시하고, 버전과 제작자는 오른쪽에 같은 줄로 정렬되게 했다. 의심 툴 카드 전체가 과하게 노란 배경으로 보이던 스타일도 완화했다.
- 승인 대기 툴 목록에서 항목을 우클릭하면 관리자가 `거절`할 수 있게 했다. PR로 올라온 툴은 거절 시 GitHub PR도 닫도록 IPC를 추가했다.
- 관리자 계정에서는 다른 사용자가 올린 새 승인 대기/PR 대기 툴이 감지되면 앱 알림이 뜨도록 했다.
- 검증 완료: `pnpm test -- toolSharingPolicy`, `pnpm typecheck`, `pnpm --filter @mcp-registry/desktop build`, `pnpm package:win` 통과. 최신 exe를 다시 실행했다.

## 2026-07-07 추가 185

- 커스텀 툴 설정 schema의 `repeatable-list`에 파일 목록 메타데이터를 추가했다. `itemType: file`, `valueKey: file_path`, `accept: .dwg,.dxf`를 읽어 `항목 추가`가 파일 선택으로 동작하고 선택한 경로를 목록 값으로 저장하게 했다.
- 설정창의 실행 전 요약, 점검, 설정 검증, 테스트 실행 요약은 기본 화면에 길게 펼치지 않고 하단 `검토` 아이콘 영역에서 필요할 때만 열어보도록 바꿨다.
- `CAD 도면번호 일괄 순번 변경` MD와 HTML 목업을 단순화했다. 섹션은 `DWG 파일`, `인식 기준`, `번호 규칙`, 접힌 `실행`으로 줄이고, DWG 파일 목록은 실제 파일 선택 기반 repeatable-list로 명시했다.
- `/make`와 `/save` 스킬 및 `ai-program-md-tool` reference에 파일 목록 설정 규칙과 검토 아이콘 접힘 패널 규칙을 추가했고, 개인 Codex 스킬 디렉터리에도 동기화했다.
- 검증 완료: `pnpm test -- toolSettingsSchema` 30개 파일/115개 테스트 통과, `pnpm typecheck` 통과, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-07 추가 185

- 프로그램 MCP 연결값을 만들고 검증하는 새 `/등록` 스킬(`program-mcp-registrar`)을 추가했다.
- `/등록` 스킬은 AutoCAD, Revit, Excel, Tekla, Dynamo, 기타 프로그램 선택지를 먼저 제시하고, AI Program MCP 서버 등록값과 연결/실행 검증 순서를 정리하도록 했다.
- 앱 홈의 Other Tools에 `프로그램 MCP 등록` 다운로드 항목을 추가하고, Electron IPC/preload/types에 설치 함수를 연결했다.
- `/save 툴이름` 방식은 제거하고 `/save`만 지원하도록 `save-tool` 스킬 본문과 Other Tools 설명을 수정했다. `/save` 뒤에 적힌 말은 확정 이름이 아니라 참고 힌트로만 쓰게 했다.
- 앱 내장 스킬과 현재 컴퓨터의 로컬 Codex 스킬 폴더를 동기화했다.
- 설정 schema 렌더링의 기존 타입 오류(`repeatable-list` 안에서 불가능한 `field.type === "file"` 비교)를 정리했다.
- 검증 완료: 스킬 frontmatter 간이 검증, `pnpm typecheck`, `pnpm test -- rendererBuild appNotificationModel`, `pnpm --filter @mcp-registry/desktop build`, `pnpm package:win` 통과. 최신 exe를 다시 실행했다.

## 2026-07-07 추가 186

- 관리자 계정에서 More Tools/Custom Tools 툴 항목을 우클릭하면 `삭제` 메뉴가 보이도록 했다.
- 삭제 전 앱 디자인에 맞춘 확인창을 띄우고, 확인 시 앱 목록에서 제거하면서 연결된 원본 MD 파일도 삭제하도록 했다. 로컬 파일은 승인된 툴 경로에서 삭제하고, `github:` 경로는 GitHub contents API로 원본 파일을 삭제한다.
- Custom Tools 목록 상단에 `전체`, `주의`, `승인 대기`, `등록됨`, `미등록` 필터를 추가했다.
- 커스텀 툴 항목에 `createdAt` 등록일을 저장하고, 승인 대기 툴 목록에 `등록일` 열을 추가했다. 기존 날짜가 없는 툴은 `-`로 표시한다.
- 검증 완료: `pnpm typecheck`, `pnpm test -- rendererBuild toolSharingPolicy`, `pnpm --filter @mcp-registry/desktop build`, `pnpm package:win` 통과. 최신 exe를 다시 실행했다.

## 2026-07-07 추가 187

- More Tools에서 앱 승인 상태와 GitHub PR 상태가 모두 `승인 대기`로 보여 사용자가 승인 후에도 승인 대기처럼 보이던 문제를 수정했다.
- `승인 대기` 필터는 앱 승인 대기(`approvalStatus: pending`)만 걸러내도록 변경했다.
- GitHub PR이 아직 열려 있는 경우 툴 이름 옆 배지는 `PR 대기`로 표시해 앱 승인 상태와 구분되게 했다.
- 관리자가 앱 승인 버튼을 누르면 해당 툴은 즉시 `approved`와 `registered: true` 상태가 되도록 했다.
- 검증 완료: `pnpm typecheck`, `pnpm test -- rendererBuild toolSharingPolicy`, `pnpm --filter @mcp-registry/desktop build`, `pnpm package:win` 통과. 최신 exe를 다시 실행했다.

## 2026-07-07 추가 186

- `CAD 도면번호 일괄 순번 변경` 설정창 목업의 DWG 파일 목록에 파일별 분석 결과 표시를 추가했다.
- 미리보기 전에는 `미분석`으로 보이고, 도곽/기준 문자 분석 후에는 각 파일 행에 `도곽 N개`와 `P-101~P-103` 같은 배정 번호 범위가 표시되는 방식으로 정했다.
- MD 명세에도 `file_summaries` 출력 포트와 결과 표의 `파일별 도곽 수`, `파일별 번호 범위` 필드를 추가했다.

## 2026-07-07 추가 187

- 실제 설정 schema 렌더러도 파일별 분석 요약 배지를 표시할 수 있게 확장했다.
- `repeatable-list` 파일 필드에 `showItemSummary`, `summaryCountKey`, `summaryRangeKey`, `pendingSummaryLabel`, `pendingRangeLabel`을 추가했다.
- DWG 파일 목록 설정은 `title_block_count`와 `number_range`를 읽어 각 파일 행에 `도곽 N개`, `P-101~P-103` 형태로 표시하도록 명세를 갱신했다.
- `/make`, `/save` 스킬과 MD reference에도 파일별 preview summary를 파일 행 옆 배지로 표시하는 규칙을 추가했고, 개인 Codex 스킬 디렉터리에 동기화했다.
- 검증 완료: `pnpm test -- toolSettingsSchema` 30개 파일/115개 테스트 통과, `pnpm typecheck` 통과, `pnpm --filter @mcp-registry/desktop build` 통과.

## 2026-07-08 추가 188

- 업데이트된 `mcp-tool-builder` 스킬 기준에 맞춰 `CAD 도면번호 일괄 순번 변경` MD와 HTML 목업을 다시 정리했다.
- 기존 `작동 원리` 패널형 목업을 `실행 단계` 상단 패널 구조로 바꾸고, 하단에는 선택 단계의 설정/미리보기/고정 실행 규칙이 보이게 했다.
- `preview_only` 체크박스와 단일 옵션 select, 고정값 설정을 제거했다. 미리보기와 원본 저장은 `분석 미리보기`, `원본에 적용` action으로 표현한다.
- 파일 처리 순서, Text/MText 대상, 도곽 정렬, 허용 오차 `10`, 증가값 `1`, 백업 없음, 최종 확인은 사용자 설정이 아니라 고정 실행 규칙으로 이동했다.
- frontmatter에 `executionSteps`, `actions`, `learningLog`를 추가했고, MCP 명령 매핑은 `runtime.action == "preview"` / `runtime.action == "apply"` 기준으로 수정했다.

## 2026-07-07 추가 188

- `data/registry.json` 기준 AutoCAD MCP(`localhost:5100`)와 Revit MCP(`localhost:5001`)는 이미 등록되어 있음을 확인했다.
- Excel MCP를 `Excel MCP Bridge`, `http://localhost:5200/mcp`, 포트 `5200` 값으로 추가했다.
- 현재 registry target 타입은 `cad | revit | other`만 지원하므로 Excel MCP는 임시로 `other` target에 등록했다.
- 실제 실행 앱이 쓰는 `C:\Users\Donggeon\AppData\Roaming\ai-program\registry.json`에도 AutoCAD/Revit/Excel MCP 3개를 병합 등록했다. 기존 자동 감지 서버는 유지했고, 수정 전 백업은 같은 폴더의 `registry.backup-before-program-mcp-20260707.json`에 저장했다.
- `Test-NetConnection` 확인 결과 AutoCAD/Revit/Excel MCP 포트는 모두 현재 응답하지 않았다. 실제 실행 확인은 각 프로그램과 MCP 브리지 실행 후 다시 해야 한다.

## 2026-07-07 추가 189

- 앱에 포함되는 `/등록` 스킬(`program-mcp-registrar`)을 등록값 안내 중심에서 MCP 브리지 생성/등록/연결/검증 중심으로 개편했다.
- 새 스킬은 AutoCAD/Revit/Excel/Tekla/Dynamo MCP를 대상으로, 브리지가 없을 때 스캐폴드 생성까지 진행하고 AI Program 등록, URL/포트 확인, 명령 목록 확인, 안전한 읽기 테스트까지 단계별로 확인하도록 정리했다.
- 등록 완료, 브리지 생성 완료, 프로그램 로드 확인 전, 연결 확인 완료, 명령 확인 완료, 실행 확인 완료 상태를 분리해 실제 검증 없이 연결됐다고 표시하지 않도록 했다.
- `agents/openai.yaml` 표시명과 기본 프롬프트도 새 흐름에 맞게 갱신했다.
- Home의 Other Tools 카드와 설치 완료 알림 문구도 새 `/등록` 스킬 흐름에 맞춰 `브리지 생성, 등록, 연결, 검증`으로 수정했다.
- 앱 시작 시 사용자 registry에 AutoCAD/Revit/Excel 로컬 MCP 브리지 항목이 없거나 예전 placeholder 실행 명령이면 자동으로 병합/갱신하도록 했다.
- Windows 기본 PowerShell로 실행되는 `tools/mcp-bridges/program-bridge/program-mcp-bridge.ps1` 로컬 브리지 스캐폴드를 추가했다. `/mcp`, `/status`, `/active-file`, `/commands`에 응답해 AI Program 연결 상태 점검에서 감지될 수 있다.
- 이 브리지는 현재 AI Program 연결 확인용 스캐폴드이며 실제 AutoCAD/Revit/Excel SDK 또는 애드인 제어 명령은 다음 통합 단계로 남겨뒀다.
- Settings의 `MCP 서버` 영역은 기본 접힘 상태로 바꾸고, 헤더 클릭으로 서버 목록/상세 설정을 펼치거나 다시 접을 수 있게 했다.
- Custom Flow 메인 화면의 공유 플로우/내 플로우 카드, 헤더, 편집 입력칸, 버튼에 다크모드 스타일을 추가했다.
- 패키징 후 AI Program을 숨김 창으로 실행하면 사용자에게 안 열린 것처럼 보일 수 있으므로, 확인용 실행은 보이는 창으로 띄우도록 정리했다.
- Custom Flow 메인 화면 상단의 중복 설명 박스를 제거했다.
- Player view에서 Custom Flow 메인 화면은 공유 플로우 아래에 내 플로우가 오는 1열 레이아웃으로 바꿨다.
- Excel MCP가 `other` target으로 임시 등록되어 Excel 페이지의 연결 상태 필터에서 명확히 잡히지 않던 문제를 수정했다. `McpTarget`에 `excel`/`tekla`를 추가하고, Excel 페이지/Custom Flow 연결 범위/설정 서버 필터/기본 registry/자동 병합 값을 Excel 전용 target으로 갱신했다.

## 2026-07-07 추가 190

- Custom Flow 메인 화면의 `내 플로우` 카드에서 별도 `이름 변경` 버튼을 제거했다. 이름과 설명은 카드 입력칸에서 바로 수정하는 흐름만 유지한다.
- Custom Flow의 새 플로우 생성은 더 이상 기본 CAD/Excel 노드를 만들거나 저장 목록에 즉시 추가하지 않는다. 빈 캔버스로 열고, 사용자가 `저장`을 누르기 전에는 내 플로우 목록과 localStorage 자동 보관에 남기지 않는다.
- Custom Flow 캔버스 툴바에서 `메인` 버튼을 제거하고, `저장`은 디스크 아이콘, `노드 검색`은 돋보기 아이콘 버튼으로 교체했다.
- Custom Flow 노드 상세 탭(`작동 원리`/`설정`/`결과`)은 항상 한 줄 3열로 표시되도록 정리했다.
- More Tools/Custom Tools 삭제 실패 원인을 확인했다. 앱의 `tools` 폴더 파일이 승인된 커스텀 툴 루트에 포함되지 않아 목록 제거 후 원본 파일 삭제가 실패했다. 앱/프로젝트 `tools` 폴더를 안전 삭제 허용 루트에 포함해 재발을 막았다.
- 실패했던 `tools/cad-drawing-number-batch-renumber_1.0.0.md` 원본 파일을 삭제했다.
- GitHub 툴 등록 PR 생성 시 `git/refs` 404가 나던 경로를 보강했다. fork 저장소 준비와 fork 기본 브랜치 ref를 확인한 뒤 PR 브랜치를 만들고, 저장소 접근 권한 문제가 있으면 더 명확한 오류를 표시한다.
- 툴 버전 업데이트는 현재 로그인한 닉네임 또는 GitHub ID가 기존 툴 제작자와 일치할 때만 허용하도록 했다.
- Settings의 MCP 서버 화면은 전체 접힘 UI를 제거하고, 서버 목록은 항상 보이되 선택한 서버 상세만 아래에 읽기 전용으로 표시하도록 바꿨다. MCP 서버 등록/수정은 `/등록` 스킬과 registry 자동 병합 흐름이 담당한다.
- `%APPDATA%\ai-program\registry.json` 같은 registry 파일이 UTF-8 BOM으로 저장되어도 `mcp-processes:get-snapshot` JSON 파싱이 실패하지 않도록 `loadRegistry`에서 BOM을 제거한다.
- BOM registry 로딩 회귀 테스트를 추가했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm --filter @mcp-registry/desktop package:win` 통과.
- 패키징 중 기존 `AI_PROGRAM.exe`가 release 폴더를 잡고 있으면 `EBUSY: resource busy or locked`가 발생한다. 이 경우 실행 중인 AI_PROGRAM 프로세스를 종료한 뒤 다시 패키징해야 한다.

## 2026-07-07 추가 191

- Monitor의 연결 상태 카드에서 개별 CAD/Revit bridge 항목을 제거하고 전체 연결 상태/등록 서버 중심으로 단순화했다.
- More Tools/Custom Tools 필터를 큰 필터 줄 대신 검색줄 옆 작은 아이콘 필터로 바꿨다.
- Custom Flow 메인 화면의 공유 플로우/내 플로우 헤더 구조, 텍스트 크기, 카드 hover, 다크모드 스타일을 다른 메인 페이지 기준에 맞췄다.
- 공유 플로우와 내 플로우 카드를 클릭하면 바로 해당 캔버스로 들어가고, 내 플로우의 복제/삭제는 우클릭 메뉴로 이동했다.
- 저장하지 않은 Custom Flow에서 나갈 때 브라우저 기본 확인창 대신 앱 디자인에 맞춘 확인 모달을 띄우도록 했다.
- 툴 삭제 시 원본 MD 파일이 이미 사라진 경우를 성공 처리해 `목록에서 제거됐지만 원본 파일 삭제 실패`가 반복되지 않도록 했다.
- Custom Flow 기본 도구 창의 툴/연결값 도구 탭은 스크롤 중 상단에 고정되고, 연결값 도구는 프로그램/공용/입출력 필터로 좁혀 볼 수 있다.
- 기본 전체 창 크기를 1440x900으로 키웠다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm --filter @mcp-registry/desktop package:win` 통과.

## 2026-07-07 추가 192

- Player view에서 Share/Custom Tool을 선택하면 같은 화면 아래에 선택 툴 실행 패널이 펼쳐지도록 했다.
- Player view 실행 패널은 툴 이름/버전/제작자/설명, 한 줄 버튼(`실행`, `값 저장`, `초기화`), 설정값 입력 영역 순서로 표시한다.
- Custom Tool에 MD schema가 있으면 Player view에서도 동일한 schema 설정창을 렌더링하고, schema가 없으면 기본 작업 대상/실행 옵션/메모 입력칸을 표시한다.
- 툴 삭제 시 이미 삭제된 로컬 원본 파일은 허용 루트 검사보다 먼저 존재 여부를 확인해 삭제 성공으로 처리한다. 오래된 source/installedPath 때문에 `원본 파일 삭제 실패`가 반복되는 문제를 줄였다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-07 추가 193

- Player view의 툴 실행 패널 위치를 다시 조정했다. 이제 선택한 툴 목록 섹션 아래에 따로 뜨지 않고, 클릭한 툴 카드 바로 다음 위치에서 그 자리 확장 방식으로 펼쳐진다.
- Share Tools와 Custom Tools 모두 같은 확장 기준을 사용한다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-07 추가 194

- `cad-drawing-number-batch-renumber` 삭제 실패를 재확인했다. 로컬 `tools/cad-drawing-number-batch-renumber_1.0.0.md`는 이미 삭제되어 있었고, 남은 실패 원인은 GitHub 원본 경로가 404일 때도 실패로 집계되는 흐름이었다.
- GitHub 툴 삭제에서 contents API 404는 `이미 삭제된 GitHub 원본 파일`로 보고 삭제 성공 처리하도록 수정했다.
- 삭제 일부 실패가 다시 생기면 알림에 실패 경로를 함께 표시하도록 바꿔 다음 원인을 바로 추적할 수 있게 했다.
- More Tools/Custom Tools 필터를 검색줄 옆 아이콘 버튼에서 `툴 이름` 테이블 헤더 옆 필터 버튼으로 옮겼다. 버튼을 누르면 필터 목록 드롭다운이 열리고 선택한 필터가 적용된다.
- `툴 등록` 옆에 `내 툴 관리` 버튼을 추가했다. 이 모드에서는 현재 로그인한 닉네임/GitHub ID가 제작자인 툴만 보이고, 마지막 열에서 바로 삭제할 수 있다.
- 툴 삭제 권한은 관리자뿐 아니라 제작자 본인에게도 허용했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-07 추가 195

- GitHub 원본 삭제가 권한/원격 상태 문제로 실패해도 사용자가 삭제한 툴이 새로고침 후 다시 목록에 나타나지 않도록, 삭제한 `github:` 툴 경로를 localStorage tombstone(`mcp-registry:deleted-github-tool-paths`)에 저장한다.
- GitHub 툴 동기화 시 tombstone에 있는 경로는 목록에서 제외한다.
- GitHub 동기화 성공 메시지(`GitHub에서 N개의 MD 툴을 가져왔습니다.`)는 더 이상 툴 리스트에 표시하지 않는다. 오류 메시지만 상태 영역에 남긴다.
- `내 툴 관리`는 More Tools 내부 모드가 아니라 `툴 등록`처럼 별도 창으로 열리도록 변경했다. 내 닉네임으로 등록한 툴만 표시하고, 승인/관리자 거절/GitHub 승인 대기 상태와 삭제 버튼을 보여준다.
- 툴 이름 헤더 옆 필터는 `전체`, `주의`, `앱 승인 대기`만 남기고 `등록됨`, `미등록` 필터를 제거했다.
- 필터 아이콘이 테이블 밖으로 튀어나오지 않도록 헤더 안쪽 그리드 배치와 드롭다운 오른쪽 정렬로 수정했다.
- 계정 정보에서 저장한 닉네임이 GitHub 이름으로 덮이지 않도록, 계정 목록에 저장된 닉네임을 우선 표시하고 툴 제작자 기본값에도 같은 닉네임을 사용한다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-07 추가 196

- 전체 점검 중 App/Electron main의 깨진 한글 문자열을 다시 검색했다. GitHub 로그인/토큰/브랜치 생성/툴 삭제/프로세스 로그/로딩 화면/IPC 경로 라벨의 깨진 메시지를 정상 한글로 복구했다.
- `filePathSecurity.ts`의 깨진 경로 검증 오류 메시지를 복구하고, 라벨 뒤 조사(`은`/`는`)를 자동 선택하도록 정리했다.
- More Tools/Custom Tools에서 제거한 `등록됨`/`미등록` 필터 값이 타입과 필터 로직에 남아 있던 것을 제거했다. 현재 필터는 `전체`, `주의`, `앱 승인 대기`만 유지한다.
- 툴 이름 헤더 필터 레이아웃을 `inline-flex`로 바꿔 좁은 표 컬럼에서도 필터 아이콘이 셀 밖으로 튀어나오지 않게 보완했다.
- 커스텀 툴 설명 fallback의 깨진 구분자(`쨌`)를 `·`로 교체했다.
- 툴 등록/업데이트 제작자 기본값과 제작자 확인 기준을 GitHub 표시 이름이 아니라 앱 계정의 저장 닉네임 우선으로 맞췄다.
- 검증 완료: `pnpm test` 30개 파일/118개 테스트 통과, `pnpm typecheck`, `pnpm build`, `pnpm --filter @mcp-registry/desktop package:win` 통과.

## 2026-07-07 추가 197

- More Tools/Custom Tools의 툴 이름 필터 드롭다운이 버튼 기준 왼쪽으로 펼쳐져 테이블 밖으로 튀어나오던 원인을 수정했다. `.tableFilterMenu`를 `left: 0` 기준으로 열고 폭을 줄여 버튼 아래쪽에서 시작하도록 변경했다.
- 툴 등록 저장 버튼이 일반 `primary` 클래스라 다른 주요 버튼보다 hover/active 피드백이 약했던 문제를 수정했다. `primaryAction`으로 통일하고, 저장 중에는 `저장 중...` 문구와 disabled 상태를 표시한다.
- GitHub OAuth Device Flow 요청 scope를 `read:user repo`로 변경했다. 기존 `public_repo` 토큰으로는 private repo 또는 브랜치 생성 권한이 부족해 `git/refs` 404가 발생할 수 있으므로, 기존 로그인 사용자는 로그아웃 후 다시 로그인해야 한다.
- GitHub 툴 등록 중 브랜치 생성 404/권한 오류가 나면 긴 API 오류 대신 `GitHub 권한이 부족합니다. 로그아웃 후 다시 로그인해서 저장소 권한을 승인해주세요.`로 안내하도록 정리했다.
- GitHub 원격 툴 삭제는 직접 삭제가 막히면 작성자 fork에서 삭제 브랜치와 PR을 만들 수 있도록 보강했다. 앱 목록에서는 tombstone으로 즉시 숨기고, 원격 반영은 PR 승인 후 완료된다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-07 추가 198

- 툴 이름 필터가 계속 튀어나오던 실제 원인을 재확인했다. 문제는 좌우 정렬이 아니라 `th` 안의 absolute 커스텀 드롭다운이 테이블 첫 행 위에 떠서 내용을 덮는 구조였다.
- 커스텀 드롭다운(`.tableFilterMenu`)을 제거하고, 테이블 헤더 안에서만 공간을 차지하는 작은 네이티브 select(`.tableFilterSelect`)로 바꿨다. 필터 값은 `전체`, `주의`, `대기`로 줄여 헤더 안에서 끝나게 했다.
- 사용자가 제공한 `cad-drawing-number-batch-renumber.md`를 앱의 `parseToolRuntimeSchema`로 직접 검사했다. 현재 코드 기준 `settings 15개`, `sections 4개`, `inputs 1개`, `outputs 3개`, `mcpCommands 7개`가 정상 파싱된다.
- 따라서 해당 MD의 설정창이 비어 보이는 경우는 MD schema 부재가 아니라, GitHub 권한 문제로 툴 등록이 실패해 실제 등록 목록/툴 페이지에 반영되지 않았거나 예전 등록본을 보고 있는 상황으로 판단했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-07 추가 199

- Settings 관리 탭의 `승인 대기 툴` 목록이 앱 승인 상태와 GitHub PR 상태를 함께 조건으로 사용하던 문제를 수정했다.
- 기존에는 `approvalStatus`가 `approved`여도 `reviewState`가 `open`이면 계속 승인 대기 목록에 남았다. 이제 이 목록은 `approvalStatus === "pending"`인 앱 승인 대기 툴만 표시한다.
- GitHub PR이 아직 열려 있는 상태는 툴 리스트/내 툴 관리의 PR 상태로만 확인하도록 분리했다.

## 2026-07-07 추가 200

- More Tools/Custom Tools에서 같은 프로그램/툴 이름의 여러 버전이 내려와도 리스트에는 최신 버전 하나만 표시하도록 GitHub/로컬 동기화 그룹핑을 정리했다.
- 툴 업데이트 직후 앱 상태도 최신 버전만 유지해 이전 버전 선택 UI가 다시 나타나지 않게 했다.
- GitHub 툴 등록/업데이트 시 같은 툴의 오래된 MD는 최신 버전과 직전 백업 1개만 남기고 정리하도록 했다. 직접 등록은 바로 삭제하고, PR 등록은 삭제 변경을 PR 브랜치에 함께 포함한다.
- 툴 이름 헤더 필터 select 폭이 작아 `전체` 텍스트와 화살표가 잘리던 문제를 수정했다.
- 내 툴 관리 상태 배지를 승인/승인 대기/GitHub 승인 대기/거절 상태별 배경색으로 구분했다.
- Settings의 MCP 서버 목록에서 선택된 서버를 다시 클릭하면 상세가 닫히도록 자동 재선택 effect를 수정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-07 추가 200

- OneDrive Desktop MCP의 `cad-drawing-number-batch-renumber.md` 툴 초안을 수정했다.
- 파일 처리 순서 선택, 검색 대상 문자 타입 선택, 상대 위치 허용 오차 입력, 증가값 입력을 사용자 설정에서 제거했다.
- 파일 처리 순서는 목록 순서 그대로, 검색 대상은 Text/MText, 상대 위치 허용 오차는 5 DWG 단위, 증가값은 1로 고정했다.
- `미분석`/`미리보기 필요`는 DWG 파일 목록 행의 분석 전 배지로 표시되고, 미리보기 생성 후 도곽 수/번호 범위로 대체되도록 문서 설명을 보강했다.

## 2026-07-07 추가 201

- `cad-drawing-number-batch-renumber.md`에서 `preview_only` 설정 필드를 제거하고, `분석 미리보기`/`원본에 적용` 실행 버튼 흐름으로 바꿨다.
- 미리보기는 `runtime.action == "preview"` 상태로 저장 없이 변경 예정 목록을 계산하고, `cad.save_dwg`는 `runtime.action == "apply"`일 때만 실행하도록 명시했다.
- `mcp-tool-builder`와 `save-tool` 스킬에 재발 방지 규칙을 추가했다. 사용자가 선택하지 않는 고정값, 분석 후 표시되는 파생 배지, 클릭한 버튼 상태는 `settings`에 넣지 않고 고정값/결과/런타임 액션으로 분리하도록 했다.

## 2026-07-07 추가 202

- 커스텀 툴 메뉴 항목을 ID prefix가 아니라 `customTools` 목록의 실제 ID 매칭으로 판별하도록 바꿨다.
- GitHub에서 가져온 `github-tool-*` 커스텀 툴도 메뉴/소메뉴에서 툴 이름, 설명, 설정 schema가 정상 표시되며 `새 페이지` fallback으로 빠지지 않는다.
- 툴 실행 시 위험 동작 또는 툴 문서 형태 경고가 Windows 기본 확인창으로 뜨던 문제를 앱 내부 확인 모달로 변경했다.
- Custom Flow 설정값 변경의 `confirmOnChange` 경고도 앱 내부 확인 모달을 사용하도록 바꿔 기본 `window.confirm` 사용을 제거했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-07 추가 203

- GitHub에서 가져온 `github-tool-*` 커스텀 툴이 메뉴에는 표시되지만 클릭하면 본문이 메인페이지로 돌아가던 문제를 수정했다.
- 원인은 submenuKey 유효성 검사(`isSubmenuId`)가 `github-tool-*` ID를 거부해 탭 적용 시 submenuKey가 버려지는 것이었다.
- `github-tool-*`도 유효한 소메뉴 ID로 인정해 커스텀툴 페이지 본문과 설정 schema가 정상 렌더링되도록 했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm build` 통과.

## 2026-07-07 추가 204

- 툴 페이지와 Player view의 설정값 저장 버튼이 실제 저장 로직과 연결되지 않아 동작하지 않던 문제를 수정했다.
- 기존 설정 프리셋 모델을 재사용하는 `useStandaloneToolPresets`를 추가해 툴별 설정값 저장/불러오기/삭제가 localStorage에 유지되도록 했다.
- Player view의 `값 저장` 버튼과 schema 설정 패널의 `현재 설정 저장` 버튼이 같은 프리셋 저장 흐름을 사용한다.
- 설정 schema가 없는 툴은 `값 저장` 버튼을 비활성화해 빈 버튼처럼 보이지 않게 했다.
- `settingPresetModel.ts`의 깨진 기본 한글 문구를 `저장한 설정`으로 정리했다.
- `ToolRuntimeSchema.actions` 타입 확장에 맞춰 누락된 테스트 fixture를 보강했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.
- 같은 스킬 보강 내용을 앱 번들 자산(`packages/desktop/assets/skills/mcp-tool-builder`, `packages/desktop/assets/skills/save-tool`)에도 반영해 재설치나 다른 컴퓨터 설치 시에도 유지되도록 했다.

## 2026-07-07 추가 204

- `save-tool/scripts/create_tool_draft.py`에 `--frontmatter-file` 옵션을 추가했다. 승인된 YAML frontmatter를 넘기면 `settings`, `mcpCommands`, `risk`, `inputs`, `outputs`가 기본 빈 값으로 덮이지 않고 보존된다.
- 같은 스크립트 보강을 앱 번들 사본(`packages/desktop/assets/skills/save-tool/scripts/create_tool_draft.py`)에도 반영했다.
- `save-tool` 지침은 실행 가능한/설정이 있는 툴을 저장할 때 승인 frontmatter를 만들어 `--frontmatter-file`로 넘기도록 수정했다.
- `mcp-tool-builder`와 `save-tool`의 skill description을 `Use when...` 트리거 중심으로 정리했다.
- `ai-program-md-tool.md` 참조 문서의 risk 안내를 `create/modify/bulk-modify/delete` 중심으로 바꾸고, `settings`에는 사용자 편집값만 넣도록 분류 규칙을 추가했다.
- `dry-run`은 예외적 체크박스로 낮추고, 분석/미리보기 후 적용하는 흐름은 `runtime.action` 기반 버튼 동작으로 모델링하도록 참조 문서에 예시를 추가했다.
- 검증 완료: 기존 스크립트가 `--frontmatter-file`을 인식하지 못하는 실패를 먼저 확인했고, 수정 후 로컬/번들 스크립트 모두 승인 frontmatter의 `bulk-modify`, `mcpCommands`, `settings` 보존을 확인했다. `--help`에서도 새 옵션이 표시된다.

## 2026-07-07 추가 205

- 단일 선택지만 있는 select/select-like 값은 설정창에 표시하지 않도록 `mcp-tool-builder`, `save-tool`, `ai-program-md-tool.md` 로컬/번들 스킬 지침을 보강했다.
- 앱 schema 파서에 `actions`를 추가해 MD 툴이 `분석 미리보기`, `원본에 적용` 같은 실행 버튼을 설정창에 표시할 수 있게 했다.
- 설정창은 `actions`를 실행 버튼 섹션으로 렌더링하며, `분석 미리보기`를 누른 뒤 `원본에 적용` 버튼이 활성화되는 흐름을 표시한다.
- 기존 MD 호환을 위해 단일 선택지 설정은 파서에서 완전히 삭제하지 않고 `hidden` 처리해 기본값/명령 매핑은 유지하되 UI 공간은 차지하지 않게 했다.
- `cad-drawing-number-batch-renumber.md`에서 단일 선택지/고정값 설정(`도곽 순서`, `백업 정책`, `최종 확인`, `도곽 직접 선택 허용`)을 제거하고 고정 MCP 파라미터로 옮겼다.
- 같은 툴 frontmatter에 `actions`를 추가해 `분석 미리보기`와 `원본에 적용` 버튼이 앱에서 표시되도록 했다.
- 검증 완료: `pnpm test -- toolSettingsSchema.test.ts` 30개 파일/120개 테스트 통과, `pnpm typecheck` 통과.

## 2026-07-07 추가 206

- 최신 exe 실행 시 프로세스는 뜨지만 화면이 보이지 않던 문제를 조사했다.
- 원인 1: 로딩 화면 도입 후 `ready-to-show` 이벤트가 오지 않으면 실제 앱 화면 로드가 시작되지 않는 구조였다. 로딩 HTML 로드 완료/실패 시에도 창을 보여주고 본 화면으로 넘어가도록 fallback을 추가했다.
- 원인 2: 이전에 저장된 MD 툴 schema/localStorage 데이터에는 최신 `actions`, `inputs`, `outputs`, `testCases` 같은 배열 필드가 없을 수 있는데, 렌더러가 `schema.actions.length`를 직접 읽어 화면이 죽었다.
- `normalizeToolRuntimeSchemaForRender`를 추가해 예전 schema도 렌더링 전에 기본 schema와 병합되도록 했다. 기존에 같이 진행했던 파일 처리 옵션 고정 MD처럼 최신 schema 필드가 일부 없어도 앱이 죽지 않는다.
- `settingPresetModel.ts`에 남아 있던 깨진 기본 한글 문자열을 `저장한 설정`으로 정리했다.
- 검증 완료: `pnpm --filter @mcp-registry/desktop typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop package:win` 통과.

## 2026-07-07 추가 207

- 저장한 설정 프리셋 이름을 바꿀 수 있도록 `renameSettingPreset` 모델 함수를 추가했다.
- 일반 툴 페이지, Player view, Custom Flow 노드 설정창의 `저장한 설정` 목록에서 `이름` 버튼을 누르면 인라인 입력으로 이름을 수정할 수 있다.
- Player view에서 툴 설정창이 별도 카드처럼 생기고 좁은 폭에서 잘려 보이던 문제를 수정했다. 선택된 툴 카드와 설정 패널을 시각적으로 붙이고, 설정창 내부 요소는 Player view 폭에 맞춰 1열/줄바꿈 기준으로 정리했다.
- Player view에서는 커스텀 툴을 펼칠 때 위험 경고 확인창을 띄우지 않도록 했다. 경고는 펼치기 흐름을 막지 않고 실행 단계에서 다루는 기준으로 정리한다.
- 앞으로 기능 추가 시 Full view뿐 아니라 Player view에서 좁은 폭, 확장 느낌, 실행 흐름 방해 여부를 함께 검토하는 것을 작업 기준으로 삼는다.
- 검증 완료: `pnpm --filter @mcp-registry/desktop typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop package:win` 통과.

## 2026-07-08 추가 208

- `actions` 실행 영역을 단순 버튼 토글에서 `입력` → `분석` → `적용` 3단계 워크플로 패널로 개선했다.
- `분석 미리보기`를 누르면 파일 목록 설정값을 기반으로 파일명, 도곽 수, 배정 번호 범위를 결과 영역에 표시하도록 `toolActionPreview.ts`를 추가했다.
- `cad-drawing-number-batch-renumber.md` 문서를 다시 작성해 기준 도곽 학습, 상대 위치 매칭, 고정 규칙, 미리보기/원본 적용 조건을 실제 작동 흐름 중심으로 정리했다.
- `mcp-tool-builder`, `save-tool`, `ai-program-md-tool.md` 로컬/번들 지침에 AI CAD/Revit 애드인 변환 UI 기준을 추가했다. 읽기/내보내기형은 간단한 폼, 원본 변경형은 분석 미리보기 후 적용 액션 흐름을 사용한다.
- 검증 완료: `pnpm test -- toolActionPreview.test.ts toolSettingsSchema.test.ts` 31개 파일/122개 테스트 통과, `pnpm typecheck` 통과.

## 2026-07-08 추가 209

- 저장한 설정 프리셋 목록에서 현재 설정값과 동일한 프리셋은 선택된 상태처럼 보이도록 강조했다.
- Custom Flow 노드 설정 프리셋은 `menu-cad-*` 같은 노드용 ID와 실제 툴 ID를 함께 조회하고, 새 프리셋 저장은 실제 툴 ID 기준으로 저장하도록 바꿔 일반 툴 페이지와 Custom Flow 노드가 같은 저장값을 공유한다.
- 저장한 설정 이름 변경 버튼을 텍스트 대신 연필 아이콘으로 바꿨다.
- Custom Flow 노드 경고 배지의 오류 `X`가 한쪽으로 치우쳐 보이던 문제를 CSS pseudo element 기준으로 중앙 정렬했다.
- `/save` 생성 스크립트와 `save-tool`, `mcp-tool-builder`, `ai-program-md-tool.md` 번들 지침에 `learningLog` 메타데이터 표준을 추가했다. 이 값은 실행에는 영향을 주지 않고, 사용자가 막힌 지점/선택지/선택 결과/추후 개선점을 익명 요약으로 모아 스킬 개선 통계에 쓰기 위한 영역이다.
- 검증 완료: `pnpm --filter @mcp-registry/desktop typecheck`, `pnpm test`, `pnpm build`, `pnpm package:win` 통과. 실행 중인 `AI_PROGRAM.exe`가 release 폴더를 잠그고 있어 프로세스 종료 후 패키징을 다시 실행했다.

## 2026-07-08 추가 210

- 설정 프리셋 저장을 깊은 복사 스냅샷으로 바꿔 DWG 파일 목록, 매핑표, 정렬 순서 같은 중첩 설정이 저장 뒤 수정되어도 저장본이 같이 변하지 않도록 했다.
- 툴 페이지, Player view, Custom Flow 노드 설정창에 공통 프리셋 UI를 적용했다. 설정 헤더에는 `저장` split 버튼과 `불러오기` 버튼이 있고, 불러오기 창에서 저장본 선택/이름 변경/삭제를 처리한다.
- MD action 버튼은 앱 기준 라벨인 `미리보기`, `실행`으로 렌더링하도록 표준화했다. MD의 기존 `분석 미리보기`, `원본에 적용` 문구는 설명/툴팁으로 남긴다.
- 설정 패널 상단의 `대량 수정`, `필요 MCP` 위험 요약을 작동 원리 영역으로 옮겼다. 도곽 후보가 필요한 툴은 `도면 입력 → 도곽 후보 선택 → 번호 규칙 적용` 흐름으로 보이도록 했다.
- 미리보기 후 도곽 후보 선택 패널을 추가해 기준 도곽 후보를 고른 뒤 실행할 수 있게 했다. 선택값은 설정값에 같이 저장되어 프리셋/노드에서도 유지된다.
- Player view 기본 창 폭을 560x780으로 키우고 최소 폭도 520으로 올려 설정 패널이 잘리는 기준을 줄였다.
- Custom Flow 메인 페이지의 내 플로우 이름/설명은 평소 텍스트로 보이고, 연필 아이콘을 누른 카드만 편집 모드가 되도록 바꿨다.
- 검증 완료: `pnpm test` 31개 파일/125개 테스트 통과, `pnpm typecheck`, `pnpm build`, `pnpm package:win` 통과. 처음 패키징은 실행 중인 `AI_PROGRAM.exe`가 release 폴더를 잠가 실패했고, 프로세스 종료 후 다시 실행해 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 생성했다.

## 2026-07-08 추가 211

- Custom Flow 캔버스의 `연결값 도구` 필터가 도구 목록 공간을 너무 많이 차지하지 않도록 기본 접힘 상태로 바꿨다.
- 필터 요약 버튼에는 현재 선택값만 작게 표시하고, 펼쳤을 때만 CAD/Revit/Excel/Tekla 필터 칩이 보이도록 정리했다.
- 기존 필터 버튼이 작아지지 않던 원인은 뒤쪽 공통 `.flowToolbarMenu button` 규칙이 `min-height: 44px`과 padding을 덮어쓴 것이었다. `.flowToolbarMenu .basicPortFilterBar button`처럼 더 구체적인 선택자로 크기를 고정했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-08 추가 212

- Custom Flow 메인 페이지의 Share Flow 카드에서 `내 플로우로 가져오기` 버튼을 제거했다.
- Share Flow 카드는 My Flow 카드처럼 클릭하면 바로 캔버스 편집 화면으로 열린다. 이때 내 Flow 목록에 자동 복사하지 않고, 사용자가 저장할 때 새 Flow로 저장하는 기준이다.
- Share Flow를 단순히 열어보기만 한 상태에서는 나갈 때 저장 경고가 뜨지 않고, 실제 그래프를 수정했을 때만 저장 경고가 뜨도록 baseline 비교를 적용했다.
- Market 다이얼로그의 Tools/Flows 분기 JSX가 내 툴 관리 창 안쪽으로 잘못 들어가 있던 구조를 바로잡았다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm --filter @mcp-registry/desktop package:win` 통과. 최신 실행 파일은 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`로 생성했다.

## 2026-07-08 추가 213

- Settings에 `AI 연결` 탭을 추가했다. 사용자는 OpenAI API 키와 모델을 입력할 수 있고, 키 값은 렌더러/localStorage/Git에 저장하지 않는다.
- Electron main process는 `%APPDATA%` 계열 userData의 `openai-settings.json`에 API 키를 Electron `safeStorage`로 암호화해 저장한다. 실행 시 우선순위는 앱 저장 키, `AI_PROGRAM_OPENAI_API_KEY`/`OPENAI_API_KEY` 환경 변수 순서다.
- 툴 실행의 OpenAI 직접 호출 경로가 설정창 저장 키를 먼저 사용하도록 바뀌었다. 키가 없으면 `Settings > AI 연결`에서 저장하라는 안내를 반환한다.
- 앱 preload와 Window 타입에 `openAiSettings.get/save/clear` IPC를 추가했다. 설정창에는 저장 여부, 사용 모델, 암호화 가능 상태만 표시하고 API 키 원문은 다시 보여주지 않는다.
- 번들 `/등록` 스킬에 `Settings > AI 연결` API 키 설정 절차와 비밀키 취급 금지 규칙을 추가했고, 현재 사용자 로컬 `.codex\skills\program-mcp-registrar`에도 같은 내용을 설치했다.
- Home 페이지 `Other Tools`의 `/등록` 설명을 최신화해 MCP 브리지 등록뿐 아니라 AI 연결 API 키 설정 확인까지 포함하도록 바꿨다.
- 검증 완료: `pnpm test packages/desktop/src/openAiSettings.test.ts packages/desktop/src/settingsDialog.test.ts packages/desktop/src/programMcpRegistrarSkill.test.ts packages/desktop/src/openAiToolRunner.test.ts`, `pnpm typecheck`, `pnpm test`, `pnpm build` 통과.

## 2026-07-08 추가 214

- GitHub에서 받은 `codex/mcp-registry-desktop` 최신 Market 기준 파일을 기준으로 다시 정리했다. Documents 폴더의 오래된 More Tools/AI 연결 변경을 기준으로 덮지 않는다.
- 사용자가 AI 연결은 필요 없다고 정리했으므로 Settings의 `AI 연결` 탭, OpenAI API 키 저장 IPC, OpenAI 직접 실행 fallback, 관련 테스트 파일을 제거했다. 툴 실행은 등록된 MCP 서버/명령 기준으로만 안내한다.
- 기본 공유 툴을 소메뉴와 Custom Flow 노드로 넘길 때 `settingsSchema`가 빠져 툴 페이지 설정창이 비어 보이던 문제를 수정했다.
- `MY Flow` 공유 등록을 GitHub `flows/` JSON 업로드/PR 흐름으로 연결했고, Market > Flows에서 GitHub 공유 Flow를 새로고침해 볼 수 있게 했다. 공유 Flow 카드에는 버전/제작자를 표시한다.
- Custom Flow 편집 화면에서 다른 Flow를 열면 현재 캔버스에 그룹으로 삽입되도록 유지했고, 그룹의 `Flow 저장` 버튼은 커스텀툴이 아니라 `MY Flow`로 저장한다.
- Settings > 관리 화면을 `회원 관리`, `툴 관리`, `플로우 관리`, `스킬 데이터` 탭으로 나눴다. `스킬 데이터`는 MD 툴의 `learningLog`를 모아 미반영/반영됨으로 보여주고, 자동 덮어쓰기 대신 검토용 스킬 업데이트 초안을 만든다.
- 자유등록 모드에서는 기존 승인대기 커스텀툴도 승인 상태로 보정되도록 했다.
- MCP 프로그램 브리지의 TCP 상태 확인이 HTTP 요청 없이 연결만 하고 끊는 경우에도 멈추지 않도록 PowerShell bridge 읽기 타임아웃을 추가했다.
- Custom Flow 그룹 헤더 여백과 폭을 정리하고, 프롬프트 붙이기 안내 CSS 깨진 문자열을 수정했다. 소메뉴 드래그는 마지막 빈 공간으로 끌어도 맨 뒤에 놓을 수 있도록 end drop zone을 추가했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 통과. 패키징은 이어서 `pnpm package:win`으로 확인한다.

## 2026-07-08 추가 215

- `Register program MCP` 스레드에서 처리했던 것은 AI Program registry(`%APPDATA%\ai-program\registry.json`) 등록과 Codex Bridge 설정이었고, Codex CLI가 읽는 `C:\Users\Donggeon\.codex\config.toml`의 MCP 서버 등록은 별도였음을 확인했다.
- 실제 Codex CLI 설정에 `ai-program-cad`, `ai-program-revit`, `ai-program-excel` 3개 streamable HTTP MCP 서버를 추가했다. 추가 전 `C:\Users\Donggeon\.codex\config.toml.backup-ai-program-mcp-20260708-1505` 백업을 만들었다.
- `codex mcp get ai-program-cad`, `codex mcp get ai-program-revit`, `codex mcp get ai-program-excel`로 각각 `enabled: true`, URL `127.0.0.1:5100/5001/5200` 등록을 확인했다.
- 앱 번들 `/등록` 스킬과 실제 사용자 `C:\Users\Donggeon\.codex\skills\program-mcp-registrar` 스킬을 동기화하고, 앞으로 누락되지 않도록 `codex mcp add` 기준과 `ai-program-*` 서버명을 스킬 지침에 추가했다.
- 주의: 현재 브리지는 PowerShell placeholder bridge이므로 Codex CLI 설정 등록은 완료됐지만, 실제 AutoCAD/Revit/Excel SDK/애드인 명령 실행은 여전히 후속 TODO다.

## 2026-07-08 추가 216

- Custom Tool 페이지 설정창이 비어 보일 수 있던 원인은 실제 툴 `settingsSchema` 뒤에 localStorage의 `submenuMeta`를 통째로 덮어쓰던 구조였다. 이제 메뉴 메타는 제목/설명만 덮어쓰고, 스키마는 실제 툴/공유툴의 값을 유지한다.
- Market > Flows를 Tools와 같은 검색/새로고침/표 구조로 정리했다. Shared Flows와 My Flow를 같은 기준으로 보여주고, My Flow 등록 여부는 id만이 아니라 이름/버전/제작자 조합까지 비교한다.
- GitHub 새로고침 뒤 Flow id가 달라져도 같은 Flow를 이미 등록된 항목으로 인식하도록 `flowRegistrationKey`, `isFlowRegistered`, `upsertSharedFlowByIdentity`를 추가하고 회귀 테스트를 남겼다.
- Custom Flow 메뉴에서 공유 Flow를 열 때도 열린 그래프 baseline을 저장해, 수정하지 않은 상태라면 Custom Flow 메인으로 돌아갈 때 저장 경고/캔버스 잔류가 생기지 않도록 했다.
- 탭을 맨 뒤 빈 공간으로 드래그할 때 인식되는 영역을 마지막 탭 바로 뒤로 옮기고 폭을 키웠다. 소메뉴 맨 뒤 드롭 영역도 높이를 키워 빈 공간 드롭 판정이 더 잘 보이게 했다.
- Custom Flow 그룹 박스는 노드와 헤더 사이 여백을 키우고, 헤더의 이름/색상 영역을 더 넓게 보이도록 조정했다.
- Canvas 기본도구의 `Excel 내보내기` 아이콘을 Excel 문자 아이콘에서 다운로드 아이콘으로 바꿔 기능 의미가 먼저 보이게 했다.
- Custom Tool 목록의 `주의` 배지는 툴 이름 왼쪽으로 이동하고, 이름/버전/제작자 줄과 어긋나지 않도록 정렬했다.
- 앱 시작 시 등록된 MCP 서버가 있으면 한 번 자동 상태 점검을 실행하도록 연결했다. 사용자가 누르는 `상태 새로고침` 버튼은 기존처럼 별도 동작한다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm build` 통과.

## 2026-07-08 추가 217

- 툴 페이지 구조를 기존 `작동 원리`/`설정` 분리 방식에서 `실행 단계` 중심 구조로 바꿨다. 각 단계는 설정 섹션 또는 실행 액션과 연결될 수 있고, 설명은 단계 안에 섞어 표시한다.
- Custom Flow 노드 상세도 `작동 원리`, `설정`, `결과` 탭 대신 `1. 설정 입력`, `2. 실행 흐름`, `3. 결과 확인` 같은 실행 단계 버튼으로 표시되게 했다.
- `/make`, `/save` 스킬과 번들 참조 문서, 실제 사용자 로컬 `.codex\skills` 사본을 실행 단계 구조 기준으로 동기화했다. 앞으로 새 MD 툴 초안에는 `executionSteps`가 포함된다.
- Custom Flow 저장 경고 로직을 수정했다. 메인 페이지에서 다른 Flow로 이동할 때는 저장 경고가 뜨지 않고, 편집 중인 캔버스에 저장되지 않은 변경이 있을 때 다른 Flow를 열면 경고가 뜬다.
- 그룹의 `Flow 저장` 버튼은 즉시 저장하지 않고 `MY Flow에 저장할까요?` 확인창에서 `네`를 눌렀을 때만 MY Flow에 저장하도록 변경했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 36개 파일/148개 테스트, `pnpm build`, `pnpm package:win` 통과. 패키징된 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe` 실행과 기본 화면 로드를 확인했다.

## 2026-07-08 추가 218

- Custom Flow 저장 경고창에서 `저장`을 눌렀을 때 새 플로우도 prompt 없이 바로 MY Flow로 저장하고, 대기 중이던 이동/닫기 동작을 이어서 실행하도록 보강했다.
- Market > Flows 새로고침 성공 안내 문구는 표시하지 않도록 정리했다. 비어 있거나 오류가 있을 때만 상태 메시지를 보여준다.
- Shared Flow는 현재 사용자 닉네임/GitHub ID 또는 관리자 권한 기준으로 `등록 해제`할 수 있게 했다. GitHub 원본 Flow는 삭제 요청 IPC를 통해 제거 또는 PR 생성 흐름으로 연결한다.
- Market > My Flow 헤더 설명이 제목 오른쪽이 아니라 아래쪽에 보이도록 마크업을 정리했다.
- Settings > 관리 > 툴 관리에 `모든 툴`, `승인 대기 툴` 하위 탭을 추가했다. 툴 목록은 회원관리처럼 헤더 클릭 정렬을 지원하고, 우클릭 또는 삭제 버튼으로 툴 삭제 확인창을 열 수 있다.
- Settings > 관리 > 플로우 관리는 GitHub에서 불러온 공유 Flow만 관리 대상으로 보여주며, 이름/버전/제작자/노드/출처 정렬과 우클릭 삭제를 지원한다.
- 검증 완료: `pnpm test` 36개 파일/154개 테스트 통과, `pnpm typecheck`, `pnpm build`, `pnpm package:win` 통과. 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 실행했고 렌더러 제목 `MCP Registry` 로드를 확인했다.

## 2026-07-08 추가 219

- Market > Flows의 Shared Flow `등록 해제` 권한을 제작자 본인 기준으로 제한했다. 관리자는 Settings > 관리 > 플로우 관리에서는 삭제할 수 있지만, Market에서는 제작자가 다른 공유 Flow의 등록 해제 버튼이 비활성화된다.
- 모니터, Settings, Market 등 모달 창의 창 제어 버튼을 왼쪽 상단에 고정되도록 정리했다. 긴 창을 스크롤해도 닫기 버튼이 상단에 붙어 보이도록 `sticky` 기준을 적용했다.
- MCP 오류 원인은 앱 첫 렌더링 시 fallback registry(Revit/CAD 2개)로 자동 점검이 먼저 실행되고, 실제 registry(CAD/Revit/Excel 포함)를 불러온 뒤에는 자동 점검이 다시 실행되지 않는 것이었다.
- 자동 MCP 점검은 실제 registry 로딩이 완료된 뒤 한 번만 실행되게 바꿨고, 실행 명령이 있는 HTTP MCP 서버는 상태 확인 실패 시 자동 시작 후 재시도한다. 동시에 여러 서버를 시작하면서 상태 저장이 꼬이지 않도록 순차 점검으로 바꿨다.
- 실행 명령이 없는 오래된 수동 HTTP 서버는 연결 실패 시 `error`가 아니라 `stopped`로 내려가게 했다.
- 검증 완료: `pnpm typecheck`, `pnpm test` 36개 파일/157개 테스트, `pnpm build`, `pnpm package:win` 통과. 최종 exe 실행 후 `5001/Revit`, `5100/AutoCAD`, `5200/Excel` `/status` 응답이 모두 200임을 확인했다.

## 2026-07-08 추가 219

- `CAD 도면번호 일괄 순번 변경` 도구 초안과 HTML 설정창 목업을 프로그램 수정 작업과 분리해 MD/목업 기준으로만 정리했다.
- 설정 입력 단계는 DWG 파일 목록, 검색 기준 문자, 번호 prefix/시작번호/자릿수만 받도록 줄이고, 후보 확인 단계는 분석 결과에서 도곽 후보와 기준 문자 위치를 고르는 별도 화면으로 분리했다.
- `분석 미리보기`와 `원본에 적용`은 사용자 설정값이 아니라 실행 단계/action으로만 남겼고, 설정 입력/후보 확인 패널 안에 중복 실행 버튼이 생기지 않도록 HTML 목업을 수정했다.
- DWG 파일 목록 행에는 분석 전/후에 도곽 인식 개수와 해당 파일의 번호 범위를 배지로 표시하는 구조를 반영했다.
- 검증 완료: `git diff --check tool-drafts/cad-drawing-number-batch-renumber.md outputs/cad-drawing-number-batch-renumber-settings-mockup.html WORK_LOG.md` 통과. 기존 줄바꿈 경고 외 공백 오류는 없었다.

## 2026-07-08 추가 220

- `mcp-tool-builder` 스킬에 실행 버튼 중복 방지 규칙을 추가했다. `executionSteps[].actionId`가 있는 `분석 미리보기`, `원본에 적용` 같은 action은 단계/action 영역에만 표시하고 설정 섹션이나 후보 확인 패널 안에 다시 넣지 않도록 명시했다.
- HTML 설정창 목업 규칙에 각 단계 패널의 목적이 화면상 다르게 보여야 한다는 기준을 추가했다. `설정 입력`은 입력 폼, `분석 미리보기`는 분석 상태/요약/action, `후보 확인`은 감지된 후보와 선택 컨트롤, `원본에 적용`은 최종 확인 화면으로 구분한다.
- CAD 도곽 후보나 Revit 요소 후보처럼 감지 결과를 고르는 흐름에서는 후보 확인 단계가 원래 설정 입력 폼처럼 보이면 안 되고, 후보명/개수/신뢰도/핸들 또는 id/선택 확정 컨트롤을 보여주도록 보강했다.
- 저장소 번들 스킬 `packages/desktop/assets/skills/mcp-tool-builder/SKILL.md`와 현재 사용자 로컬 스킬 `C:\Users\Donggeon\.codex\skills\mcp-tool-builder\SKILL.md`를 같은 내용으로 동기화했다.

## 2026-07-08 추가 221

- `mcp-tool-builder` 스킬에 선택된 action 패널 내부의 `입력 / 미리보기 / 실행` 요약 줄을 만들지 말라는 규칙을 추가했다.
- 상단의 전체 폭 `실행 단계`가 이미 workflow 순서를 보여주므로, 하단 선택 단계 패널은 현재 단계의 컨트롤과 결과만 보여주도록 명시했다.
- 실제 앱 렌더러의 중복 workflow 카드 제거는 프로그램 코드 수정 사항이므로 이 작업에서는 건드리지 않았다.

## 2026-07-08 추가 222

- 설정 preset/profile용 `저장`, `불러오기` 버튼은 선택 단계 패널 헤더의 오른쪽 끝에 배치하도록 `mcp-tool-builder` 스킬에 규칙을 추가했다.
- CAD 도면번호 일괄 순번 변경 HTML 목업에서도 선택 단계 헤더를 제목/설명 영역과 오른쪽 버튼 영역으로 나누고, `저장`, `불러오기` 버튼을 오른쪽 정렬로 추가했다.
- 실제 앱 렌더러의 버튼 위치 수정은 프로그램 코드 변경 사항이므로 이 작업에서는 건드리지 않았다.

## 2026-07-08 추가 223

- Custom Tool 설정창 렌더러에서 `저장`, `불러오기` preset 버튼을 별도 `저장한 설정` 섹션으로 만들지 않고 첫 설정 섹션 헤더 오른쪽 끝에 붙도록 수정했다.
- `ToolExecutionActions` 내부의 중복 workflow 카드(`입력 / 미리보기 / 실행`)를 제거했다. 상단 `실행 단계`가 workflow 순서를 담당하고, 하단 실행 패널은 action 버튼과 실행 결과만 표시한다.
- 관련 CSS에서 preset 버튼 오른쪽 정렬을 보강하고, 제거된 `schemaWorkflowStep` 스타일과 다크 모드 잔여 스타일을 정리했다.
- 검증 완료: 번들 Node/pnpm 경로로 `pnpm typecheck`, `pnpm test` 36개 파일/161개 테스트, `pnpm build` 통과. 기본 `pnpm typecheck`는 sandbox에서 `spawn EPERM`, 승인 실행에서는 PATH의 `node` 누락으로 실패해 번들 경로를 명시해 재실행했다.

## 2026-07-08 추가 222

- 모달 창 닫기/탭열기 버튼이 왼쪽에 고정되던 원인은 이전 CSS와 회귀 테스트가 `order: -1`로 왼쪽 고정을 요구하고 있었기 때문이었다. 요구사항에 맞춰 모든 dialog header action을 우측 상단 sticky로 되돌리고 테스트도 우측 상단 기준으로 바꿨다.
- Custom Tool 등록/업데이트 창에서 GitHub 오류 문장이 길면 내용이 잘려 보이던 문제를 줄바꿈 가능한 `.formError`와 넓어진 `customToolFormDialog` 폭으로 보완했다.
- 툴 페이지의 위험도/필요 MCP 표시는 실행 단계 위 별도 한 줄이 아니라 실행 단계 헤더 오른쪽의 작은 배지 묶음으로 이동했다. 선택된 실행 단계는 숫자 원 배경/테두리까지 바뀌게 해 일반 hover/선택 느낌과 구분했다.
- Shared Flow 등록 해제 후 새로고침하면 다시 나타나던 문제는 GitHub 원본 삭제가 즉시 반영되지 않아 목록을 다시 덮어썼기 때문이었다. 삭제 요청한 GitHub Flow 경로를 로컬 차단 목록에 저장하고, GitHub 새로고침/카탈로그/관리 화면에서 모두 필터링하도록 했다.
- 툴 등록 중 발생한 GitHub 409 오류는 GitHub Contents API 삭제 시 파일 SHA가 바뀐 상태에서 오래된 SHA로 삭제 요청해서 생긴 충돌이었다. 삭제 직전 최신 SHA를 다시 조회하고, 409가 오면 최신 SHA로 한 번 재시도하는 `deleteGitHubContentFile` 공통 함수를 추가했다.
- 검증 완료: `pnpm test packages/desktop/src/rendererBuild.test.ts`, `pnpm typecheck`, `pnpm test` 36개 파일/161개 테스트, `pnpm build`, `pnpm package:win` 통과. 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe` 실행 후 `5001/Revit`, `5100/AutoCAD`, `5200/Excel` `/status` 응답이 모두 200임을 확인했다.

## 2026-07-08 추가 224

- Market을 열 때 GitHub tools 목록 동기화가 `customTools` 상태를 GitHub 항목만으로 다시 만들면서, 로컬/기존 등록 툴 상세 페이지가 사라질 수 있던 문제를 수정했다.
- `customToolSync.ts`를 추가해 GitHub 툴만 갱신하고, GitHub 경로가 아닌 기존 등록 툴은 보존하도록 병합 로직을 분리했다.
- GitHub 툴은 기존 id/등록/고정/사용횟수 상태를 유지한 채 최신 버전 정보만 갱신하도록 테스트를 추가했다.
- 검증 완료: `pnpm test packages/desktop/src/customToolSync.test.ts`, `pnpm typecheck`, `pnpm test`, `pnpm package:win` 통과. 패키징 중 release 폴더를 잠근 MCP bridge 프로세스가 있어 종료 후 재실행했고, 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 생성/실행했다.

## 2026-07-08 추가 225

- 실행 단계 설정 패널 헤더의 `저장`, `불러오기` preset 버튼이 제목 바로 옆에 붙어 보이던 문제를 수정했다.
- `.submenuConfigPanel > .panelHeader .schemaPresetControls`에 오른쪽 정렬 규칙을 추가해 버튼이 패널 오른쪽 끝에 고정되도록 했다.
- 같은 배치가 깨지지 않도록 `rendererBuild.test.ts`에 회귀 테스트를 추가했다.
- 검증 완료: `pnpm test packages/desktop/src/rendererBuild.test.ts`, `pnpm typecheck`, `pnpm test` 통과.

## 2026-07-08 추가 226

- 툴 설정 패널에서 `Ctrl+S`를 누르면 현재 설정 preset을 저장하도록 추가했다.
- 일반 툴 페이지, Player view, Custom Flow 노드 설정 저장 시 기존 하단 save-toast 알림으로 저장 완료를 표시하도록 연결했다.
- CAD 도면번호 일괄 순번 변경처럼 `미리보기` 후 `실행`으로 넘어가는 툴에서 미리보기 완료 상태가 상단 실행 단계 카드까지 반영되도록 수정했다. 완료 단계는 카드 전체에 완료 색상과 다크 모드 스타일을 적용했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm build` 통과.

## 2026-07-08 추가 227

- `tools/CAD_도면번호_일괄_순번_변경_1.0.8.md`에 명시적 `settingsLayout`, `settings`, `actions`, `executionSteps`, `mcpCommands`, `preflightChecks`, `resultSchema`, `inputs`, `outputs`, `testCases`를 추가했다.
- 기준 흐름은 `입력 -> 미리보기 -> 실행`이며, `apply` action은 `requiresPreview: true`로 preview 완료 후에만 활성화된다.
- `toolSettingsSchema.test.ts`에 최신 CAD 도면번호 툴 MD가 명시적 preview/apply workflow로 파싱되는지 확인하는 회귀 테스트를 추가했다.
- 검증 완료: `pnpm test packages/desktop/src/toolSettingsSchema.test.ts`, `pnpm typecheck`, `pnpm test`, `pnpm --filter @mcp-registry/desktop package:win` 통과.
- 기존 release 폴더 잠금으로 첫 패키징이 실패해 실행 중인 release/bridge 프로세스를 종료한 뒤 재실행했다. 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 생성하고 실행 확인했다.

## 2026-07-08 추가 228

- 설정 preset 저장 동작을 분리했다. `저장`은 현재 선택된 저장본만 덮어쓰고, 선택된 저장본이 없으면 새 항목을 만들지 않는다.
- 새 저장본 생성은 `저장` 옆 화살표의 `다른 이름으로 저장`에서만 수행하도록 수정했다. `다른 이름으로 저장` 후 생성된 preset은 현재 선택 preset으로 잡힌다.
- 설정 패널 `Ctrl+S`도 현재 선택된 preset이 있을 때만 덮어쓰기 저장하고, 선택 preset이 없으면 브라우저 기본 저장 동작만 막고 새 항목은 만들지 않도록 수정했다.
- 현재 실행 단계 표시 색상을 파랑 계열에서 주황/amber 계열로 변경하고 다크 모드 색상도 맞췄다.
- 검증 완료: `pnpm typecheck`, `pnpm test packages/desktop/src/rendererBuild.test.ts packages/desktop/src/toolSettingsSchema.test.ts`, `pnpm test`, `pnpm build` 통과.

## 2026-07-08 추가 229

- 실행 단계 카드의 `선택됨` 효과와 `현재 진행 단계` 효과를 분리했다. 선택 효과는 기존 파랑 계열을 유지하고, 실제 현재 단계(`state: active`)만 주황/amber 계열로 표시한다.
- CAD 도면번호 일괄 순번 변경처럼 도곽 후보가 필요한 툴에서 미리보기 결과에 `titleBlockCandidates`가 비어 있으면 하단 알림으로 도곽 후보 미검출을 알려주도록 했다.
- 설정 패널 `Ctrl+S`가 패널 내부 입력창에 포커스가 있을 때만 동작하던 제한을 풀었다. 툴 설정 패널이 열려 있으면 현재 선택된 preset을 덮어쓰고 저장 알림을 띄운다.
- 회귀 방지를 위해 현재 단계 스타일이 `active` 선택 스타일과 다른 `currentStep` 클래스로 유지되는지 렌더러 테스트에 추가했다.
- 검증 완료: `pnpm typecheck`, `pnpm test packages/desktop/src/rendererBuild.test.ts packages/desktop/src/toolExecutionModel.test.ts packages/desktop/src/toolSettingsSchema.test.ts packages/desktop/src/settingPresetModel.test.ts`, `pnpm test`, `pnpm build`, `pnpm --filter @mcp-registry/desktop package:win` 통과. 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 실행했다.

## 2026-07-08 추가 230

- 도곽 후보가 필요한 preview action에서 `status: preview`만으로 미리보기 완료 처리되던 문제를 수정했다.
- 이제 CAD 도면번호 일괄 순번 변경처럼 `titleBlockCandidates`가 필요한 툴은 후보가 1개 이상 반환되어야만 `previewGenerated`와 실행 단계 완료 처리가 된다.
- 후보가 0개면 실행 단계가 4번/실행으로 넘어가지 않고, `도곽 후보를 찾지 못했습니다` 알림을 표시한다.
- `window.toolExecution`이 연결되지 않은 fallback 상태에서도 도곽 후보 필요 툴은 preview 완료로 처리하지 않도록 막았다.
- 회귀 방지를 위해 도곽 후보 없는 preview가 완료 처리되지 않는 조건을 `rendererBuild.test.ts`에 추가했다.
- 검증 완료: `pnpm typecheck`, `pnpm test packages/desktop/src/rendererBuild.test.ts packages/desktop/src/toolExecutionModel.test.ts packages/desktop/src/toolSettingsSchema.test.ts`, `pnpm test`, `pnpm build`, `pnpm --filter @mcp-registry/desktop package:win` 통과. 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 실행했다.

## 2026-07-08 추가 231

- preview/apply 결과 공통 오류 판정 함수를 추가했다. `status: error`뿐 아니라 `ok: false`, `success: false`, `errors`, `failures`, `issues severity=error` 같은 raw payload 실패 신호도 감지한다.
- 실패 신호가 감지되면 실행 단계 완료 콜백을 호출하지 않고 하단 알림과 패널 메시지만 표시하도록 했다.
- `needs-ai`나 완료 상태가 아닌 결과도 preview/apply 완료로 처리하지 않고 사용자에게 추가 확인 필요 알림을 띄운다.
- 관련 회귀 테스트를 `toolExecutionModel.test.ts`에 추가했다.
- 검증 완료: `pnpm typecheck`, `pnpm test`, `pnpm build`, `pnpm --filter @mcp-registry/desktop package:win` 통과. 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 실행했다.

## 2026-07-08 추가 232

- OpenAI API 직접 호출 오케스트레이터는 이번 범위에서 제외하고, MD 툴 제작/등록/실행 전 안전망을 강화했다.
- `toolRuntimeValidation.ts`를 정리해 중복 설정/액션/단계 ID, 끊어진 `executionSteps[].actionId`, 없는 설정 섹션, 없는 `settings.*` MCP 파라미터 참조, `requiredServers` 누락, 하나뿐인 select 설정, 위험 action의 preview/confirm 부족, 출력/testCases 누락을 잡도록 했다.
- `toolExecutionModel.ts`의 사용자 안내 문장과 도곽 후보 추출 문구를 깨지지 않은 UTF-8 한글로 정리하고, `raw.mcp`로 감싸진 실패 응답도 `toolExecutionFailureMessage`가 감지하도록 했다.
- Electron main의 MCP 실행 결과 처리에서 raw MCP 응답이 `ok:false`, `success:false`, `errors`, `failures`, `issues severity=error` 등을 반환하면 바로 `status: error`로 돌려보내도록 연결했다.
- 번들 `mcp-tool-builder`, `save-tool`, `ai-program-md-tool` 참고 문서를 UTF-8 기준으로 재작성했다. 새 기준은 스무고개식 질문, 사양 승인, HTML 설정창 목업 승인, 실행 단계/action 기반 MD, `learningLog` 비실행 메타데이터, CAD/Revit/Excel/Tekla/Custom Flow 체크리스트를 포함한다.
- 스킬 설치 전에 번들 스킬 본문이 깨진 인코딩 패턴을 포함하는지 검사해 깨진 스킬을 사용자 `.codex/skills`로 복사하지 않도록 했다.
- 현재 사용자 로컬 `C:\Users\DONG KIM\.codex\skills\mcp-tool-builder`, `C:\Users\DONG KIM\.codex\skills\save-tool`도 번들 스킬과 같은 내용으로 다시 동기화했다.
- 회귀 테스트를 갱신해 스킬 문서 깨짐 방지, MD 런타임 교차검증, `raw.mcp` 실패 감지를 검증한다.
- 검증 완료: `pnpm test` 37개 파일/169개 테스트, `pnpm typecheck`, `pnpm build`, `pnpm --filter @mcp-registry/desktop package:win` 통과. release 폴더를 잠근 기존 AI_PROGRAM/bridge 프로세스를 종료한 뒤 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 생성하고 실행했다.

## 2026-07-08 추가 233

- 출시 전 최종 점검 관점으로 Custom Flow를 별도 시뮬레이션했다.
- `customFlowPreRelease.test.ts`를 추가해 기본 CAD -> Excel 플로우가 검증, 연결 순서 계산, 실행 미리보기, 그룹 삽입, 재사용 가능한 custom-flow schema 생성까지 통과하는지 한 번에 확인한다.
- Custom Flow 기본 노드/검증/실행 모델 테스트가 깨진 문자열을 기준으로 삼지 않도록 기대값을 사람이 읽을 수 있는 UTF-8 한글로 정리했다.
- `buildFlowGroupToolSchema`가 그룹 내부 노드의 `settings.*` MCP 파라미터를 참조하면서 정작 그룹 툴 schema에는 설정 필드를 넣지 않아 runtime validation error가 나던 구조를 수정했다. 이제 그룹 저장 툴은 각 노드 설정을 `nodeId__settingId`로 namespacing하고 MCP command params도 같은 ID로 재매핑한다.
- Custom Flow validation/run preview 문구를 정상 한글로 정리하고, 깨진 인코딩 패턴이 다시 들어오면 pre-release test에서 잡히도록 했다.
- 확인한 제품 리스크: 현재 Custom Flow의 `실행` 버튼은 실제 MCP 명령 큐가 아니라 UI 타임라인/예상 결과 시뮬레이션을 성공 처리한다. 실제 출시 전에는 노드별 MCP 실행 엔진, 중간 결과 전달, 실패/부분성공/롤백 적용이 다음 핵심 단계다.
- 검증 완료: `pnpm test -- packages/desktop/src/customFlowPreRelease.test.ts packages/desktop/src/customFlowModel.test.ts packages/desktop/src/customFlowRunModel.test.ts packages/desktop/src/customFlowValidation.test.ts` 통과, `pnpm typecheck` 통과.
- 전체 검증: `pnpm test` 38개 파일/171개 테스트, `pnpm typecheck`, `pnpm build` 통과. `pnpm --filter @mcp-registry/desktop package:win`은 실행 중인 release `AI_PROGRAM.exe`가 `release\AI_PROGRAM-win32-x64\icudtl.dat`를 잠가 실패했다. 실행 파일 갱신은 앱을 닫은 뒤 재시도해야 한다.

## 2026-07-08 추가 234

- Custom Flow `실행` 버튼을 UI 타이머 시뮬레이션에서 실제 `window.toolExecution.run` 실행 흐름으로 전환했다.
- 노드별 `settingsValues`를 `buildToolExecutionRequest` 기반 MCP 실행 요청으로 만들고, 앞 노드 결과를 `previous.result` 또는 `previous.<portId>` 파라미터 참조에 주입하도록 `customFlowExecutionModel.ts`를 추가했다.
- MCP 실행 결과는 `raw.mcp`, `raw.result`, `raw.data` 우선순위로 정규화해 다음 노드 입력으로 넘긴다. 보조 노드(`결과 미리보기`, `경로 지정`, `활성 파일`, `프롬프트`)는 MCP 호출 없이 pass-through 결과를 만든다.
- 실행 중 오류, `needs-ai`, raw payload 실패 신호가 나오면 해당 노드를 error로 표시하고 하단 알림/흐름 점검에 원인을 남긴다. 기본은 이후 노드를 건너뛰며, 노드 `failurePolicy.partialSuccess`가 `keep-success`일 때만 다음 노드로 계속 진행한다.
- 회귀 테스트를 추가해 Custom Flow 실행이 더 이상 `setTimeout` 가짜 완료에 의존하지 않고 tool execution bridge를 호출하는지 확인한다.
- 검증 완료: `pnpm test`, `pnpm typecheck`, `pnpm build` 통과.

## 2026-07-08 추가 235

- 다음 단계 1번으로 AutoCAD MCP 브리지의 안전한 읽기 명령을 구현했다.
- `tools/mcp-bridges/program-bridge/program-mcp-bridge.ps1`가 실제 AutoCAD COM 세션을 읽어 `cad.get_active_document`, `cad.list_layers`, `cad.detect_title_block_candidates`를 처리한다.
- AutoCAD가 실행 중이 아니거나 COM 연결이 없으면 가짜 성공 대신 `ok:false`, `connectedToProgram:false`를 반환하게 했다.
- `/commands`, `/active-file`, JSON-RPC `tools/call`, `/tools/{command}` 호출을 모두 처리하도록 브리지 HTTP 라우팅을 정리했다.
- Custom Flow 기본 CAD 객체 읽기 노드의 `cad.read_objects` 명령은 큰 DWG 전체 스캔 안전 전략이 정리될 때까지 `planned`로 유지한다.
- MD `mcpCommands[].runtimeAction`을 파싱하고 실행 요청에서 현재 action과 맞는 명령만 보내도록 했다. 이로써 CAD 도면번호 툴의 `preview`가 `open_dwg` 같은 apply 명령을 먼저 호출하지 않는다.
- 최신 `CAD 도면번호 일괄 순번 변경` MD는 `detect_title_block_candidates`만 preview available로 두고, 실제 열기/수정/저장 명령은 apply/planned로 분리했다.
- PowerShell parser 검사와 로컬 브리지 smoke test를 실행했다. AutoCAD가 붙어 있지 않은 환경에서는 `cad.get_active_document`가 기대대로 실패 응답을 반환했다.
- 검증 완료: `pnpm test` 40개 파일/181개 테스트, `pnpm typecheck`, `pnpm build`, `git diff --check`, `pnpm --filter @mcp-registry/desktop package:win` 통과.
- 패키징 중 기존 release 앱과 AutoCAD/Revit/Excel 브리지 PowerShell 프로세스가 release 폴더를 잠가 실패했으나, 해당 프로세스만 종료한 뒤 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 생성했다.

## 2026-07-08 추가 236

- 실제 AutoCAD 2022와 현재 DWG를 대상으로 CAD bridge를 검증했다.
- `GET /mcp/status`, JSON-RPC `tools/call`의 `cad.get_active_document`, `cad.list_layers`가 실제 AutoCAD COM 세션에서 성공했다.
- JSON-RPC POST body가 비거나 command가 빈 값이 되는 문제가 있어, `Read-HttpRequest`가 `Content-Length` 기준으로 request body를 끝까지 읽고 `Expect: 100-continue`도 처리하도록 수정했다.
- 처음 확인한 DWG는 도곽 주소 변경으로 제대로 로드되지 않은 도면이라 도곽 후보 0개가 나왔다. 이후 다른 DWG에서 활성 문서/레이어 읽기는 성공했다.
- 큰 DWG에서 `foreach ModelSpace` 또는 AutoCAD SelectionSet 전체 스캔은 20~30초 이상 bridge를 점유할 수 있음을 확인했다. 따라서 `cad.read_objects`는 다시 `planned`로 내리고, 호출 시 전체 모델 공간 스캔을 하지 않고 `ok:false` 안내 메시지를 반환하도록 했다.
- `cad.detect_title_block_candidates`는 빠른 종이공간/layout 검사만 유지한다. 모델 공간 도곽 자동 탐색은 ActiveSelection, 사용자가 고른 블록명, 또는 범위 제한 전략이 정리된 뒤 다시 구현한다.
- 검증 완료: PowerShell parser `OK`, `pnpm test` 40개 파일/181개 테스트, `pnpm --filter @mcp-registry/desktop package:win` 통과. 최신 exe를 실행한 뒤 `cad.get_active_document`, `cad.list_layers`, `cad.read_objects` 빠른 실패 응답을 확인했다.

## 2026-07-08 추가 237

- AutoCAD bridge의 `cad.read_objects`를 현재 AutoCAD 선택 객체만 읽는 안전 명령으로 전환했다. 전체 ModelSpace 순회와 AutoCAD SelectionSet 전체 스캔 경로는 큰 DWG에서 멈출 수 있어 제거/비활성화했다.
- `cad.read_objects`는 `scope: selection/current_selection/selected`만 허용하며, 선택이 없거나 다른 scope가 들어오면 `ok:false`, `code: selectionRequired`를 반환한다.
- `cad.detect_title_block_candidates`에 `scope`, `blockName`, `titleBlockName` 파라미터를 추가했다. 기본은 기존처럼 Paper Space/Layout 블록만 빠르게 검사하고, `scope=selection`이면 사용자가 AutoCAD에서 선택한 블록 참조를 도곽 후보로 반환한다.
- CAD 도면번호 일괄 순번 변경 MD 설정에 `도곽 감지 방식`과 `도곽 블록명`을 추가해 자동 후보가 없을 때 사용자가 선택 블록 또는 명시 블록명으로 재시도할 수 있게 했다.
- Custom Flow 기본 CAD 객체 읽기 노드는 `cad.read_objects`를 `available`로 표시하되 선택 기반 읽기라는 제한을 유지한다.
- 앱 실행 기준은 `AI Program -> MCP bridge -> AutoCAD COM` 직접 호출이다. Codex CLI 연결은 `/등록` 자동화나 Codex 채팅에서 MCP를 직접 호출할 때 쓰는 보조 경로로 분리한다.
- 검증 완료: PowerShell parser `OK`, `pnpm test` 40개 파일/183개 테스트, `pnpm typecheck`, `pnpm --filter @mcp-registry/desktop package:win` 통과. 브리지를 직접 띄워 JSON-RPC `cad.read_objects`가 선택 객체 없을 때 `ok:false`, `code: selectionRequired`, `connectedToProgram:true`를 즉시 반환하는 것을 확인했고, 최신 `release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`를 실행했다.

## 2026-07-09 추가 238

- 파일 꼬임 방지를 위해 이번 작업 기준을 `C:\CodexProjects\AI_PROGRAM`, `codex/mcp-registry-desktop`, 커밋 `8c5fa43`로 고정하고 시작했다. 기존 로컬 백업 stash는 건드리지 않았다.
- AutoCAD bridge의 `cad.read_objects`에 `scope=window/range`를 추가했다. 전체 ModelSpace 스캔은 계속 금지하고, 사용자가 지정한 두 좌표 `point1`, `point2`로 AutoCAD `SelectionSet.Select(0, point1, point2)`만 실행한다.
- 좌표 입력은 객체 `{x,y,z}`, 배열 `[x,y,z]`, 문자열 `0,0,0` / `0 0 0` 형식을 모두 받을 수 있게 했다.
- Custom Flow 기본 CAD 객체 읽기 노드에 `범위 시작 좌표`, `범위 끝 좌표` 설정을 추가하고, MCP 파라미터 `point1`, `point2`로 전달되도록 연결했다.
- 관련 회귀 테스트를 추가했다. `cad.read_objects`가 window 읽기를 지원해도 `foreach ($entity in $doc.ModelSpace)`와 `$selection.Select(5)` 같은 전체 도면 스캔 경로가 다시 들어오지 않도록 막는다.
- 검증 완료: PowerShell parser `OK`, `pnpm test` 40개 파일/184개 테스트, `pnpm typecheck`, `git diff --check`, `pnpm --filter @mcp-registry/desktop package:win` 통과. 최신 exe는 `C:\CodexProjects\AI_PROGRAM\release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`에 생성됐다.

## 2026-07-09 추가 239

- 공통 `program-mcp-bridge.ps1`의 `/status`, `/active-file`, `active-file` MCP 명령이 더 이상 AutoCAD COM만 기준으로 판단하지 않도록 target별 분기를 추가했다.
- Excel target은 `Excel.Application` COM의 `ActiveWorkbook`을 읽어 `activeFile`, `workbook.name`, `workbook.fullName`, `workbook.path`를 반환한다.
- Revit target은 일반 PowerShell 브리지에서 활성 문서명까지 직접 읽을 수 없으므로, Revit 프로세스 감지는 `connectedToProgram`으로 표시하고 `/active-file`은 `Revit add-in bridge`가 필요하다는 명확한 실패 응답을 반환한다.
- `revit.get_active_document`, `excel.get_active_workbook` 명령명을 catalog와 command normalize 경로에 추가했다.
- 임시 포트 smoke test 완료: Excel bridge `/status`, `/active-file`은 `target=excel`로 응답했고, Excel 미실행 상태에서 `Excel is not running or no active workbook is available.` 메시지를 반환했다. Revit bridge는 현재 Revit 프로세스를 감지해 `connectedToProgram=true`를 반환하되 활성 문서명은 전용 애드인 필요 메시지로 구분했다.
- 검증 완료: PowerShell parser `OK`, `pnpm test` 40개 파일/185개 테스트, `pnpm typecheck`, `git diff --check`, `pnpm --filter @mcp-registry/desktop package:win` 통과. 최신 exe는 `C:\CodexProjects\AI_PROGRAM\release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`에 다시 생성됐다.

## 2026-07-09 추가 240

- 앱의 활성 파일 감지 경로에서 Excel MCP 서버가 누락되어 있던 문제를 수정했다. 기존 Electron main의 `activeFileProgramForTarget`은 `cad`, `revit`만 허용해 Excel 서버를 순회하지 않았다.
- `activeFileProgramForTarget`을 `activeFileDetection.ts` 공통 함수로 옮기고 `excel` target을 지원하게 했다. Electron main은 이 공통 함수를 사용한다.
- Excel bridge가 `workbook.fullName`/`workbook.name` 형태로 응답해도 활성 파일 후보로 인식되도록 `workbook` 후보와 `fullName` 경로 키를 추가했다.
- 활성 파일 새로고침 실패 안내 문구를 `CAD/Revit`에서 `CAD/Revit/Excel` 기준으로 수정했다.
- 검증 완료: `pnpm test packages/desktop/src/activeFileDetection.test.ts`, 관련 bridge 테스트, `pnpm typecheck`, `pnpm test` 40개 파일/187개 테스트, `git diff --check`, `pnpm --filter @mcp-registry/desktop package:win` 통과. 최신 exe는 `C:\CodexProjects\AI_PROGRAM\release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`에 다시 생성됐다.

## 2026-07-09 추가 241

- Revit 전용 애드인 브리지 1차 소스를 `tools/mcp-bridges/revit-addin-bridge`에 추가했다.
- Revit API는 Revit 메인 스레드에서만 안전하게 접근해야 하므로, HTTP listener 스레드가 직접 `ActiveUIDocument`를 읽지 않고 `ExternalEvent` + `IExternalEventHandler`로 Revit API 스레드에 요청을 넘기도록 구성했다.
- `/active-file`, `/mcp/active-file`, `/current-file`, `/status`, `/mcp/status` 요청을 처리하며, 활성 문서가 있으면 `activeFile`과 `document.name/fullName/path`에 `ActiveUIDocument.Document.PathName`/`Title`을 반환한다.
- `RevitMcpBridge.addin.template`와 `scripts/install-addin.ps1`을 추가했다. 빌드 후 install script가 `%APPDATA%\Autodesk\Revit\Addins\{year}\RevitMcpBridge.addin` manifest를 생성한다.
- `dotnet build tools\mcp-bridges\revit-addin-bridge\RevitMcpBridge.csproj -c Release`가 Revit 2025 API DLL 기준으로 경고/오류 없이 통과했다. 빌드 산출물 `bin/obj`는 Git에 넣지 않도록 `.gitignore`에 `tools/**/bin/`, `tools/**/obj/`를 추가하고 로컬 산출물은 정리했다.
- 검증 완료: `pnpm test` 41개 파일/189개 테스트, `pnpm typecheck`, `dotnet build tools\mcp-bridges\revit-addin-bridge\RevitMcpBridge.csproj -c Release`, `git diff --check`, `pnpm --filter @mcp-registry/desktop package:win` 통과. 최신 exe는 `C:\CodexProjects\AI_PROGRAM\release\AI_PROGRAM-win32-x64\AI_PROGRAM.exe`에 다시 생성됐다.

## 2026-07-09 추가 242

- Revit 전용 애드인 브리지가 Revit 기본 `Add-Ins` 탭, 한글 UI 기준 `애드인` 탭에 `AI Program` 패널과 `AI Program MCP` 상태 확인 버튼을 추가하도록 수정했다.
- 상태 확인 버튼은 Revit 안에서 현재 Revit MCP bridge endpoint `http://127.0.0.1:5001/active-file`를 안내하는 `TaskDialog`를 띄운다. 실제 활성 문서명/경로 조회는 기존 `/active-file` endpoint가 계속 담당한다.
- CAD는 현재 AutoCAD COM 브리지로 활성 문서/레이어/선택 객체/window 범위 읽기가 가능하므로 이번 단계에서는 별도 CAD add-in을 추가하지 않는다. 추후 AutoCAD 리본 UI, 이벤트 감지, 대량 쓰기/롤백 같은 기능이 필요할 때 CAD 플러그인을 별도 검토한다.

## 2026-07-09 추가 243

- Revit `AI Program MCP` 버튼 실행 시 `트랜잭션 속성이 지정되지 않아 애드인을 실행할 수 없습니다` 오류가 발생하는 원인을 확인했다.
- Revit `IExternalCommand` 구현체에는 `Autodesk.Revit.Attributes.Transaction` 속성이 필요하므로 `ShowBridgeStatusCommand`에 `[Transaction(TransactionMode.Manual)]`을 추가했다.
- 동일 문제가 재발하지 않도록 `revitAddinBridgeTemplate.test.ts`에 `Autodesk.Revit.Attributes`와 `[Transaction(TransactionMode.Manual)]` 검증을 추가했다.
- 이미 실행 중인 Revit이 기존 DLL을 잠그고 있어 `bin\Release\RevitMcpBridge.dll` 덮어쓰기는 실패했다. 즉시 검증 가능한 새 DLL은 `%APPDATA%\AI Program\RevitMcpBridge\20260709-transaction-fix\RevitMcpBridge.dll`에 빌드했고, `%APPDATA%\Autodesk\Revit\Addins\2025\RevitMcpBridge.addin` manifest가 이 DLL을 보도록 갱신했다.
- 이후 `.addin` manifest가 사라진 상태를 확인했다. DLL은 남아 있었지만 Revit Addins 폴더의 `RevitMcpBridge.addin`이 없어 Revit 탭에서 애드인이 사라진 것이 원인이었다.
- `scripts/install-addin.ps1`에 `-AssemblyPath` 옵션을 추가해, Revit이 잠글 수 있는 개발 `bin\Release` DLL 대신 안정 배포 DLL 경로를 직접 manifest에 기록할 수 있게 했다.
- 현재 설치 manifest는 `%APPDATA%\AI Program\RevitMcpBridge\20260709-transaction-fix\RevitMcpBridge.dll`을 가리키도록 재설치했다.

## 2026-07-09 추가 244

- Revit 재시작 후 애드인이 보이는 상태에서 `http://127.0.0.1:5001/status`와 `/active-file`을 직접 호출해 실제 열린 RVT 파일 경로를 읽는 것을 확인했다.
- 확인된 활성 파일은 `C:\Users\Donggeon\Desktop\평택통복하수처리장_교대반응조_분리됨.rvt`이며, `/active-file` 응답은 `document.name/fullName/path`를 정상 반환했다.
- 앱 registry의 Revit 서버 설정을 일반 PowerShell bridge 실행 방식에서 Revit 애드인이 직접 제공하는 HTTP endpoint 확인 방식으로 정리했다. Revit 서버의 `launchCommand`와 `workingDirectory`는 비워 두고 `http://localhost:5001/mcp` 상태만 확인한다.
- `registryEditor` 검증 로직도 HTTP 서버는 외부 프로그램/애드인이 띄울 수 있으므로 `launchCommand`를 필수로 요구하지 않게 바꿨다. `stdio` 서버만 실행 명령을 필수로 검증한다.
- Revit journal에서 `RevitMcpBridge.addin` 파일명이 사용자 Addins 폴더와 `C:\ProgramData\Autodesk\Revit\Addins\2025`에 중복된 것을 확인했다. ProgramData 쪽은 다른 `Seesum AI` add-in이므로 건드리지 않고, AI Program manifest 파일명을 `AIProgramRevitMcpBridge.addin`으로 변경해 중복 파일명 충돌을 피했다.

## 2026-07-09 추가 245

- Revit 애드인 MCP 브리지의 다음 안전 읽기 명령으로 `revit.list_levels`를 구현했다.
- `/commands`, `/mcp` JSON-RPC `tools/call`, `/tools/revit.list_levels` 경로를 지원한다. AI Program의 `window.toolExecution.run`은 `/mcp tools/call`을 먼저 시도하므로 Revit 애드인 브리지와 앱 실행 경로가 맞아진다.
- `revit.list_levels`는 Revit API 메인 스레드에서 `ExternalEvent`를 통해 실행되고, `FilteredElementCollector(doc).OfClass(typeof(Level))`로 레벨 이름과 높이(`elevationFeet`)만 읽는다. 모델 수정/트랜잭션은 하지 않는다.
- 현재 실행 중인 Revit이 기존 DLL을 잠그고 있어 새 DLL은 `%APPDATA%\AI Program\RevitMcpBridge\20260709-list-levels-v2\RevitMcpBridge.dll`로 빌드했고, `AIProgramRevitMcpBridge.addin` manifest가 이 DLL을 가리키도록 갱신했다. Revit 재시작 후 실제 `/tools/revit.list_levels` 실기 검증이 필요하다.

## 2026-07-09 추가 246

- Revit 재시작 후 `http://127.0.0.1:5001/status`, `/commands`, `/tools/revit.list_levels`, `/mcp` JSON-RPC `tools/call` 경로를 실제 열린 RVT 모델에서 검증했다.
- 활성 파일은 `C:\Users\Donggeon\Desktop\평택통복하수처리장_교대반응조_분리됨.rvt`로 감지됐다.
- `revit.list_levels`는 실제 모델에서 레벨 3개를 반환했다: `벽체 하부슬래브copy`, `벽체 상부슬래브 copy`, `상부슬래브`.
- 이 단계까지는 읽기 전용 안전 명령만 검증했다. Revit 요소 생성/수정 명령은 아직 구현하지 않았다.

## 2026-07-09 추가 247

- Custom Flow에서 바로 사용할 수 있도록 `Revit 레벨 읽기` 기본 Revit 노드를 추가했다.
- 이 노드는 입력 포트 없이 현재 열린 Revit 활성 모델을 읽고, 출력 포트 `레벨`로 다음 노드에 전달한다.
- 노드 실행 요청은 Revit MCP 서버의 `revit.list_levels` available 명령으로 생성된다. Custom Flow `도구` 메뉴에서 클릭/드래그로 캔버스에 추가할 수 있다.
- 회귀 테스트를 추가해 노드 팔레트 순서, `revit.list_levels` 명령 연결, 실행 요청 생성을 확인했다.
- 검증 완료: `pnpm test` 41개 파일/191개 테스트, `pnpm typecheck` 통과.

## 2026-07-09 추가 248

- `Revit 레벨 읽기` 노드 실행 로그/결과 미리보기가 일반 표가 아니라 `Revit 레벨 목록` 작업으로 보이도록 보강했다.
- Custom Flow 실행 요청과 같은 JSON-RPC `tools/call` 형태로 실제 Revit MCP endpoint를 호출해 레벨 3개 응답을 다시 확인했다.
- 검증 완료: Revit MCP 실기 호출 성공, `pnpm test` 41개 파일/192개 테스트, `pnpm typecheck` 통과.

## 2026-07-09 추가 249

- Custom Flow 실행 성공 후 실제 MCP payload를 실행 로그/결과 미리보기 표에 반영하도록 연결했다.
- `revit.list_levels` 응답의 `levels` 배열은 `레벨 ID`, `레벨 이름`, `높이(ft)` 열을 가진 표로 변환된다.
- `결과 미리보기` 보조 노드도 앞 노드에서 받은 실제 payload를 같은 방식으로 표로 보여준다.
- 회귀 테스트를 추가해 실제 Revit 레벨 MCP payload가 미리보기 표 rows/metrics로 변환되는지 확인했다.
- 검증 완료: `pnpm test` 41개 파일/193개 테스트, `pnpm typecheck` 통과.

## 2026-07-09 추가 250

- Revit 시작 시 `AI Program Revit MCP Bridge` 외부 응용프로그램 오류가 뜨는 원인을 확인했다.
- 원인은 이전 Revit/HTTP.sys listener가 `127.0.0.1:5001`을 잡은 상태에서 새 Revit 애드인이 같은 포트를 다시 열려고 하면서 `OnStartup` 예외가 발생한 것이다.
- `RevitMcpBridgeApp.OnStartup`에서 HTTP listener 시작 실패를 잡아도 Revit 애드인 로드 자체는 `Result.Succeeded`로 끝나도록 방어했다. 이제 포트 중복이 있어도 Revit 전체 애드인 시작 오류창으로 번지지 않고, 애드인 버튼에서 상태 메시지를 확인할 수 있다.
- 수정 DLL은 `%APPDATA%\AI Program\RevitMcpBridge\20260709-port-guard\RevitMcpBridge.dll`에 빌드했고, Revit 2025 manifest `AIProgramRevitMcpBridge.addin`이 이 DLL을 가리키도록 갱신했다.
- PowerShell 5 `Get-Content/Set-Content`로 UTF-8 한글 TypeScript 파일을 다시 저장하면 mojibake가 발생할 수 있어, `customFlowModel.ts`는 Git 원본 기준으로 Node UTF-8 처리 방식으로 복구했다.

## 2026-07-09 추가 251

- Revit Journal과 HTTP.sys 상태를 확인해 `Seesum AI` 등 다른 Revit 애드인이 `127.0.0.1:5001`을 이미 잡을 수 있음을 확인했다.
- AI Program Revit MCP 포트를 `5001`에서 `5101`로 변경했다. 앱 registry 기본값, Electron fallback, UI fallback, program-mcp-registrar 스킬 문서, Codex CLI `~/.codex/config.toml`, Revit add-in source/test를 모두 `5101` 기준으로 맞췄다.
- 새 DLL은 `%APPDATA%\AI Program\RevitMcpBridge\20260709-port-5101\RevitMcpBridge.dll`에 빌드했고, Revit 2025 manifest가 이 DLL을 가리키도록 갱신했다.

## 2026-07-09 추가 252

- 앱 사용자 registry `%APPDATA%\ai-program\registry.json`에 기존 `revit-default` 서버가 `5001`로 남아 있어 앱에서는 Revit이 미연결로 보일 수 있음을 확인했다.
- Electron `ensureBundledProgramBridgeServers`가 기존 서버를 찾았을 때 `url`/`port` 변경을 업데이트 조건에 포함하지 않아 기본 포트 변경이 기존 사용자 설정으로 마이그레이션되지 않는 것이 원인이었다.
- `url`과 `port`도 변경 감지 대상에 포함해, 이후 기존 `revit-default`가 있어도 `5101`로 자동 보정되게 수정했다.
- 현재 컴퓨터의 `%APPDATA%\ai-program\registry.json`도 즉시 `http://localhost:5101/mcp`, port `5101`로 갱신했다.

## 2026-07-09 추가 253

- 앱 실행 후 Revit MCP `5101` registry 보정은 정상이며, `revit.list_levels` 명령은 실제 모델에서 레벨 3개를 계속 반환하는 것을 확인했다.
- `/status`는 Revit ExternalEvent 응답을 2초만 기다려 Revit이 바쁜 순간 timeout이 날 수 있음을 확인했다.
- Revit add-in bridge의 active-file/status 및 command wait 시간을 8초로 늘려 상태 확인 안정성을 높였다.
- 새 DLL은 `%APPDATA%\AI Program\RevitMcpBridge\20260709-port-5101-wait8\RevitMcpBridge.dll`에 빌드했고, Revit 2025 manifest가 이 DLL을 가리키도록 갱신했다. 적용은 Revit 재시작 후부터다.

## 2026-07-09 추가 254

- Revit 보안창에서 `AI Program Revit MCP Bridge`를 `항상 로드` 처리한 뒤 Revit MCP `5101` 연결과 실제 모델 레벨 3개 읽기를 다시 확인했다.
- Revit 레벨 데이터를 Excel 내보내기로 넘기는 과정에서 PowerShell 브리지의 `Get-ExcelSourceRows`가 배열을 1개 객체로 다시 감싸 Excel에 1행만 저장하는 문제를 확인했다.
- `Get-ExcelSourceRows`가 IEnumerable을 실제 행 목록으로 펼치도록 수정했고, 실행 중인 release 브리지 스크립트에도 같은 수정 적용 후 5100 브리지를 재시작했다.
- 고정 데이터 기준 Excel 내보내기 3행/3열 저장을 확인했고, 실제 Revit 레벨 값으로 `revit_levels_verified_export_20260709_122922.xlsx` 파일을 생성해 3행 저장을 확인했다.
- Revit이 선택/수정 상태일 때 `ExternalEvent`가 타이밍에 따라 `Timed out while reading Revit model data.`를 반환할 수 있어, Custom Flow 실행에서 Revit timeout 결과는 최대 2회 재시도하도록 보완했다.

## 2026-07-09 추가 255

- 실제 Revit 모델에서 `revit.list_levels` → `excel.write_table` 흐름을 다시 실행해 `revit_levels_custom_flow_20260709_124434.xlsx` 파일에 3행/3열 저장을 확인했다.
- Revit이 선택/수정 명령 상태이면 `ExternalEvent`가 처리되지 않아 타임아웃이 나는 것을 재현했다. ESC로 Revit 명령 상태를 해제하면 즉시 회복되는 것도 확인했다.
- Revit 애드인 브리지 timeout 응답에 `code: revitBusyOrBlocked`, `retryable: true`, `Press ESC in Revit...` 안내를 추가하고 wait 시간을 12초로 늘렸다.
- Custom Flow 실행 실패 메시지도 같은 Revit busy/blocked 상황이면 한국어로 `Revit에서 ESC를 눌러 현재 명령을 취소한 뒤 다시 실행하세요.`라고 표시되도록 보강했다.
- Revit 2025 add-in manifest를 새 빌드 DLL `C:\CodexProjects\AI_PROGRAM\tools\mcp-bridges\revit-addin-bridge\bin\Release\RevitMcpBridge.dll`로 갱신했다. 적용은 Revit 재시작 후부터다.

## 2026-07-09 추가 256

- Revit에서 발견한 timeout 문제를 Revit 전용 예외 처리로만 두지 않고, Custom Flow 전체 실행 공통 안정성 처리로 확장했다.
- MCP 실행이 8초 이상 걸리면 실행 타임라인에 `큰 파일 처리`, `외부 프로그램 선택/수정 상태`, `MCP 서버 응답 지연` 가능성을 안내한다.
- `toolExecutionRecoveryMessage`를 추가해 Revit busy, MCP 연결 실패, 일반 timeout, 앞 노드 결과 없음(`emptySource`) 같은 공통 실패를 사용자에게 이해하기 쉬운 문장으로 변환한다.
- Custom Flow의 실행 실패/예외 처리도 이 공통 복구 메시지를 우선 사용하도록 연결했다. 이후 다른 툴이나 플로우에서도 같은 유형의 문제는 조용히 실패하지 않고 원인과 조치가 표시된다.

## 2026-07-09 추가 257

- AutoCAD program bridge의 도곽 후보 판정 문자열에 깨진 한글(`?꾧낸`, `?쒖젣`)이 남아 있어 실제 도곽/표제 블록 자동 인식에 영향을 줄 수 있음을 확인했다.
- `Looks-LikeTitleBlockName`을 정상 한글 `도곽`, `표제` 기준으로 복구했고, 후보 상세 문자열의 깨진 구분자(`쨌`)도 `/` 구분자로 정리했다.
- `programBridgeScript.test.ts`에 해당 깨진 문자열이 다시 들어오면 실패하는 회귀 테스트를 추가했다.
- `toolExecutionModel.test.ts`도 정상 한글 테스트 데이터 기준으로 정리해 AI/MCP 실행 지시문과 복구 메시지의 한글 품질을 유지하도록 했다.

## 2026-07-09 추가 258

- AutoCAD MCP 브리지에 실제 수정 명령의 첫 단계로 `cad.renumber_selected_text`를 추가했다.
- 이 명령은 도면 전체를 훑지 않고, 사용자가 AutoCAD에서 미리 선택한 TEXT/MTEXT 객체만 대상으로 삼는다.
- 기본은 미리보기이며 실제 도면 수정은 `apply=true`와 `confirmApply=true`가 둘 다 들어온 경우에만 실행한다. 자동 저장은 하지 않는다.
- 순번은 선택된 텍스트를 위에서 아래, 왼쪽에서 오른쪽 순서로 정렬한 뒤 `prefix`, `suffix`, `startNumber`, `padding` 설정을 적용해 만든다.
- 회귀 테스트를 추가해 선택영역 기반 처리, 명시적 적용 확인, 자동 저장 금지, 전체 ModelSpace 순회 금지를 확인했다.

## 2026-07-09 추가 259

- Custom Flow 도구 팔레트에 `CAD 선택 문자 순번 변경` 노드를 추가했다.
- 이 노드는 CAD 입력 포트를 받고, `cad.renumber_selected_text` MCP 명령으로 `prefix`, `suffix`, `startNumber`, `padding`, `apply`, `confirmApply` 설정값을 전달한다.
- 기본 설정은 실제 도면을 바꾸지 않는 미리보기이며, 사용자가 `실제 도면에 적용`과 `적용 확인`을 둘 다 켠 경우에만 브리지에서 수정이 실행된다.
- 실행 결과 미리보기에서 `oldText/newText/applied` 원시 필드를 그대로 보여주지 않고 `기존 문자`, `변경 문자`, `적용 여부` 표로 표시하도록 보강했다.

## 2026-07-09 추가 260

- `cad.renumber_selected_text` 실행 시 선택 객체가 없거나 선택 객체 안에 TEXT/MTEXT가 없을 때 사용자에게 바로 이해되는 복구 안내가 나오도록 공통 MCP 실패 메시지를 보강했다.
- `confirmApplyRequired` 응답도 `적용 확인`을 켜야 실제 도면 수정이 된다는 안내로 변환한다.
- 이 안내는 Custom Flow 실행 로그와 노드 오류 표시에서 함께 사용되므로, CAD 순번 변경 노드가 조용히 실패하지 않고 다음 행동을 알려준다.

## 2026-07-09 추가 261

- `tools/CAD_선택_문자_순번_변경_1.0.0.md`를 추가해 Custom Flow 노드뿐 아니라 CAD 툴 페이지에서도 같은 `cad.renumber_selected_text` 명령을 사용할 수 있게 했다.
- 새 MD 툴은 `미리보기`와 `적용` action을 분리하고, 적용 action은 미리보기 이후 확인을 요구한다.
- MCP 파라미터는 `preview`에서는 `apply=false`, `confirmApply=false`, `apply`에서는 `apply=true`, `confirmApply=true`로 나뉜다.
- `toolSettingsSchema.test.ts`에 새 MD 파일 파싱/런타임 스키마 검증 테스트를 추가했다.

## 2026-07-09 추가 262

- `package-win.mjs`가 `tools/mcp-bridges`만 복사하고 루트의 `tools/*.md` 툴 파일은 release app에 포함하지 않는 문제를 확인했다.
- 이 때문에 새로 추가한 CAD MD 툴이 exe 패키지 안에 들어가지 않았다.
- 패키징 스크립트에 `copyToolMarkdownFiles`를 추가해 `tools` 루트의 `.md` 파일을 `resources/app/tools`로 복사하도록 수정했다.
- `packageWinScript.test.ts`를 추가해 이 누락이 다시 생기면 테스트가 실패하도록 했다.

## 2026-07-09 추가 263

- 최신 release exe 실행 후 AutoCAD/Revit/Excel 브리지 상태를 다시 확인했다.
- AutoCAD 브리지는 실제 DWG에 연결됐고 `cad.renumber_selected_text` 명령이 `/commands`에 `available`로 노출되는 것을 확인했다.
- AutoCAD 임시 새 도면에서 COM으로 만든 named selection/Pickfirst selection은 별도 MCP 브리지 프로세스의 `PickfirstSelectionSet`/`ActiveSelectionSet`으로 전달되지 않았다. 이 경우 명령은 `selectionRequired`로 안전하게 실패하며 도면을 수정하지 않는다.
- 따라서 `cad.renumber_selected_text` 실사용 검증은 AutoCAD 화면에서 사용자가 TEXT/MTEXT를 직접 선택한 상태로 미리보기를 실행해 확인해야 한다.
- Revit 브리지는 실제 모델 `평택통복하수처리장_교대반응조_AR_Donggeon.rvt`에 연결됐고 `revit.list_levels`로 레벨 3개를 읽었다.
- Revit 레벨 3개를 Excel 브리지 `excel.write_table`에 전달해 `C:\Users\Public\Documents\ESTsoft\CreatorTemp\ai_program_revit_levels_20260709_154059.xlsx` 파일로 3행/3열 저장을 확인했다.
